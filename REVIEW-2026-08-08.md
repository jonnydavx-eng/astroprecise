# AstroPrecise — ship decision, 8 August 2026

Principal-engineer reconciliation of four independent reviews (code, product, truth/legal, hygiene).
Written by Claude @ BOOK-T1H4NJ753R. Everything marked MEASURED was re-checked by me tonight at 21:11.

---

## 1. THE ONE-LINE ANSWER

**Yes — you have a shippable product for 12 August, but it is the £2.99 eclipse reading, not the £12 one, and it needs about half a day of copy fixes plus ten minutes of backup work tonight.**

---

## 2. SHIP OR DON'T

**`redesign/editorial-front-screen` — SHIP.** All four reviewers agree, for different reasons, and the measurements hold up. The primary call-to-action moves from 1.12 screens below the fold to above it on a phone. Lint and tests pass. The moon-phase fix is the best-verified thing in the repo (two reviewers independently swept it; one at 126,721 samples). It is strictly better than what is live.

**`feat/orrery-elevate` — DO NOT SHIP.** Hold until after the eclipse. It has a reachable GPU-memory leak (`initEnvironmentIBL` guards synchronously against an async load, and is called twice), MSAA that is asserted and never measured, and it adds ~0.9 MB of download on desktop for three planets that render a few pixels wide. It has never been run on a real phone. Nothing in it earns a single sale on Wednesday, and all of it is on the render hot path.

**The 3.7 MB of orrery textures already sitting on the redesign branch: ship them.** They are not precached, not referenced by any page, and cost only deploy size. Stripping binaries out of a branch four days from a deadline is a worse trade.

---

## 3. CRITICAL PATH TO 12 AUGUST

### Tonight — before anything else (AGENT, 10 min)

1. **Push `redesign/editorial-front-screen` and `feat/orrery-elevate` to `origin`.** MEASURED 21:11: `git ls-remote --heads origin` returns 11 heads, neither branch among them. A full day's work exists only on drive C: of this laptop. Commit `15f10f3` exists in **one place** — MEASURED, `git cat-file -e 15f10f3` in the mirror returns `Not a valid object name`.
2. **Repair the mirror:** `git push --force mirror main feat/orrery-elevate`. Verified safe by the hygiene review; `mirror/main`'s unique content is already held as `rescue/mirror-main-20260807`.
3. **Read `C:\Users\jonny\git-mirror\_guard.log` on the next cycle and confirm it goes quiet.** MEASURED: 93 consecutive `[astroprecise] ... push FAILED` lines, still failing at 21:07 tonight.
4. **Pause repo-guard for astroprecise** (`guard-pause`) for the ship window, or it will keep authoring your git history.

### Saturday — the blockers (≈4 agent hours, ≈1 owner hour)

5. **OWNER, 15 min — put your legal name and postal address into `privacy.html:129,131` and `terms.html:131,132`.** `[FULL LEGAL NAME]` renders in orange on the live site. You are collecting emails today through five forms. This is a live UK GDPR Art. 13 and E-Commerce Regs breach, and it is the cheapest fix on this page.
6. **AGENT, 15 min — point the £12 proof link at the sample that already exists.** MEASURED: `website/sample-reading.html` is a real, complete, ~5,000-word "Sample Deep Natal Reading". MEASURED: homepage line 982 ("Sample published in full") and `deep-reading.html` instead send people to a page that renders seven *locked* chapter stubs under the words "PUBLISHED IN FULL — THE PROOF, NOT A CLAIM". This is live now and is pre-existing (`git diff origin/main..redesign -- website/deep-reading.html` is empty). **Nobody needs to write a sample. The link is wrong.** Fix the link, and reword `deep-reading.html:61,64` so the page stops promising proof it does not show.
7. **AGENT, 15 min — reword the homepage colophon at `index.html:692`.** It currently says your birth minute is "never put in a web address" and invites people to check. It is false three ways. Replace with what is true: *"your birth details are never sent to our server."* Do **not** try to remove the URL writes this week (see cuts).
8. **AGENT, 45 min — fix the eclipse numbers everywhere.** Maximum is **19:10–19:13 BST in England and Wales**, not 19:05 (that is Edinburgh). First contact 18:08–18:17, not "shortly after 18:00". Obscuration **~88–96%** across the UK, not "89–92%". "Deepest since 1999" holds for **southern** Britain only — 2015 was deeper in Scotland. Greatest eclipse is **45 km off western Iceland**, not "over the Arctic". `eclipse.html:302` labels 17:46 as UT when it is the TD value. Also restore a bridging sentence: the redesign deleted the only line reconciling 18:46 with 19:05 (MEASURED: "18:46" appears 0 times in the new `index.html`, "19:05" once). The two numbers are different events — global greatest eclipse vs your local maximum — and the page must say so.
9. **AGENT, 15 min — say on the homepage that checkout is closed.** MEASURED: zero occurrences of "notify", "opens soon" or equivalent in the new `index.html`, under a five-row price table and a section promising unlocks "the moment payment clears".
10. **AGENT, 5 min — delete "£4 AFTER 12 AUG" (`eclipse.html:212`) and "rising to £19" on shop.html.** Nothing can be bought at the lower price, so the discount never existed. Keep "NO FAKE CHECKOUT".
11. **AGENT, 20 min — fix the quiet-gate self-contradiction** (`reading-templates.json:104` + `eclipse-reading.js:212`). It prints "more than 5° from every placement" and then names a 0°25′ contact, for a measured 52% of the 28% of people who see that screen. This is the free teaser that sells the £2.99 — it is the funnel, not a detail.
12. **AGENT, 15 min — make the subscribe form tell success from failure.** Drop `mode:'no-cors'`, read `res.ok`. The premise for `no-cors` was wrong: both reviewers measured the Worker returning `Access-Control-Allow-Origin` today. Email capture is your entire funnel while checkout is shut.
13. **OWNER, 10 min — send one real signup end-to-end and confirm the notification email arrives.** MailChannels withdrew free Cloudflare Workers support; if it is silently failing you have no notifications and no idea.

### Sunday — the embarrassments (≈1.5 agent hours)

14. `"3 body(ies) currently retrograde"` and `"Within ~1 day(s)"` in customer copy (20 min).
15. `"See Your's sky"` — `ap-sky-bridge.js:177`, the default for anyone who skips the optional name field (10 min).
16. Hero says "place and time are optional"; `chart.html` marks Birth City required. Pick one (10 min).
17. Duplicate Moon and Tightest-Pair cards; two different countdowns on one page (30 min).
18. `privacy.html:175-181` says "that is the whole list" and omits MailChannels, Typeform, the cdnjs fallback, and the fact that the Worker stores the full Referer and your IP for an hour (25 min).

### Monday 10 August — freeze and deploy

19. AGENT: merge, deploy, then **verify live** — `GET https://astroprecise.app/sw.js` must show `ap-v815`. MEASURED tonight it serves `ap-v814`; note `STATUS.md` currently claims 813 and is wrong. Run `tools/sweep.mjs`.
20. OWNER, 30 min, optional: paste the six Gumroad permalinks — **into both `js/gumroad-unlock.js:33-39` and `js/ap-gumroad-bridge.js:13-18`**. Twelve slots, two files, no test asserts they agree. If you only want to sell the £2.99 reading, do that one product and leave the rest as "notify me".

**Tuesday is spare. Keep it spare.**

---

## 4. WHAT TO CUT ENTIRELY

- **`feat/orrery-elevate`.** All of it.
- **Removing the `#b=` hash and `updateShareURL`.** Both touch the cast path on every page. Reword the claim instead; do the engineering after the 12th.
- **Referrer headers / the `eclipse.html?from=chart&dob=` links.** Real leak, wrong week.
- **History surgery on the 133 MB of design-lab and marketing media.** Rewriting history on a repo whose only off-machine copy is currently missing is how you lose the work.
- **Making the £12 Deep Reading buyable.** It is a template-backed product (`js/deep-templates.json` contains per-sign stock phrases) sold with the line "no sign-shaped filler". Do not open that till the copy matches the code.
- **Typeform fulfilment, unsubscribe route, CI lint/test wiring, branch deletions.** All post-eclipse. The one gate: **do not send a marketing email until an unsubscribe link exists** (PECR reg. 22).

---

## 5. BRANCH PLAN

1. Tonight: push `redesign/editorial-front-screen` and `feat/orrery-elevate` to `origin`. Force-push `mirror main feat/orrery-elevate`.
2. Tag `pre-eclipse-live-3d0acf7` on the current live commit before anything merges. This is your rollback anchor.
3. Do all fix work as new commits **on `redesign/editorial-front-screen`**, with repo-guard paused.
4. Monday: fast-forward `main` to the redesign tip, push to `origin`, let CI deploy.
5. `feat/orrery-elevate` stays open, unmerged, pushed. Revisit 13 August.
6. Delete after the eclipse, not before: `ap-v683-wave`, `birthplace-geocoding`, the five `worktree-agent-*`, local `gh-pages`. Tag `rescue/mirror-main-20260807`, `backup-pre-scrub-20260731`, `archive/cursor-hymn-book-20260723` as `archaeology/*` first — one of them holds seven lines of orrery visual work that has never been checked.
7. **`gh-pages` is not a rollback.** `origin/gh-pages` is a month stale; the comment in the deploy workflow is wrong. Rollback = revert commit + redeploy.

---

## 6. DISAGREEMENTS, AND MY RULING

**The `#b=` URL write — new or pre-existing?** Code review says pre-existing; truth review says "NEW on this branch". **Code review is right.** MEASURED: the line is byte-identical in `origin/main:website/index.html` and on the redesign branch. It matters: the redesign did not introduce a leak, it introduced a *sentence that makes the existing leak a lie*. That keeps it a blocker but changes the fix from code to copy.

**Which eclipse time?** Product review defends 19:05 and flags the deleted 18:46 line; truth review says 19:05 is Edinburgh and England/Wales is 19:10–19:13. **Truth review wins.** Product review's arithmetic (17:46 UT = 18:46 BST) is correct but describes global greatest eclipse, a different event from UK maximum. Caveat, stated plainly: the city table is single-sourced (BBC Sky at Night) and I did not re-derive it. **Owner: confirm against a second source before it goes on a countdown sticker.** Same for "2015 was deeper in Scotland" — one source (BAA).

**The new `sw.js` activate block.** Code review calls it dead; truth review calls it "genuinely good". **Code review is right** — the line above it deletes every non-current cache, so the eviction pass runs against a fresh empty one. Harmless. Leave it, but do not count it as a fix.

**Live service worker version.** Three reviews say `ap-v814`. `STATUS.md`, stamped today, says `ap-v813`. **MEASURED tonight: `ap-v814`.** STATUS.md is wrong on the day it was written.

**Quiet-gate firing rate.** Truth review self-corrected 0.25% → 28.4% mid-audit. Accept the correction and the honesty.

---

## 7. WHAT WE GOT WRONG

- **We shipped an honesty fix that created a fresh lie.** The coupon leak was fixed properly, then a universal promise was written over it. A narrow fix plus a broad claim is worse than the vague wording it replaced, because it is specific and checkable — and the page invites people to check.
- **We called things "verified" all day while they existed nowhere but this laptop.** One commit still exists in exactly one place tonight.
- **A guard told the truth 93 times into a file nobody opened.** The tool worked; the loop had no reader.
- **The autosnapshotter wrote 14 of the 20 commits, including the whole cache-bust fix, with no message and no rationale.** The pause existed and was not used.
- **Two agents worked in one tree** and corrupted a generator's input; a commit landed 37 minutes after review closed, onto a branch nobody was watching.
- **Four reviewers spent hours on the code and none of us opened the shop properly.** The £12 product's proof page has been broken and live the whole time — and the sample it promises has been sitting at `sample-reading.html`, linked from three other pages, all along. That is the single most expensive defect on the site and its fix is one `href`.

---

## 8. RISK REGISTER

| Risk | Mitigation |
|---|---|
| Laptop dies before Monday; a day's work is gone | Push both branches tonight. Ten minutes. Highest-value action on this page. |
| You broadcast 19:05 and England/Wales misses maximum by 8 minutes | Fix the number, verify against a second source, and publish a range with "depends where you are". |
| A sceptic audits the colophon and finds `#b=` or the Open-Meteo town lookup | Reword before ship. Narrow the claim to what is true. |
| Someone clicks "Sample published in full" and finds a locked list | Re-point the link (item 6). |
| Subscribe fails server-side and still says "SENT" | `res.ok` fix. Also: the Worker rate-limits at 20 POSTs per IP per hour — one office or carrier NAT can trip it silently. |
| No signup notifications because MailChannels dropped Workers | Owner sends one real test signup and confirms the email lands. |
| Returning visitors get old code | `ap-v815` + `skipWaiting` handles most. Coupon arrivals have dirty fields, so the auto-reload is suppressed by design — they keep their input and old JS. Accepted. |
| A bad deploy on the day | Tag `pre-eclipse-live-3d0acf7` first. Rollback is revert + redeploy (~3 min CI), **not** the gh-pages switch the workflow claims. |
| Legal exposure while collecting emails | Name and address (item 5). No marketing send until unsubscribe exists. |
| Orrery crashes a phone during peak traffic | Do not merge `feat/orrery-elevate`. |
