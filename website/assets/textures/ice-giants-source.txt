Ice giant equirectangular maps — Hubble OPAL (NASA/ESA HST)
Local texture replacement only. No SKUs.

Uranus
  File: hlsp_opal_hst_wfc3-uvis_uranus-2025a_f657n-f547m-f467m_v1_globalmap.tif
  URL:  https://archive.stsci.edu/hlsps/opal/cycle33/uranus/hlsp_opal_hst_wfc3-uvis_uranus-2025a_f657n-f547m-f467m_v1_globalmap.tif
  Cycle 33, 2025a color (F657N red / F547M green / F467M blue)

Neptune
  File: hlsp_opal_hst_wfc3-uvis_neptune-2025b_f467m-f547m-f657n_v1_globalmap.tif
  URL:  https://archive.stsci.edu/hlsps/opal/cycle32/neptune/hlsp_opal_hst_wfc3-uvis_neptune-2025b_f467m-f547m-f657n_v1_globalmap.tif
  Cycle 32, 2025b color (F467M / F547M / F657N)

DOI: 10.17909/T9G593
https://dx.doi.org/10.17909/T9G593
Program page: https://archive.stsci.edu/hlsp/opal
License: CC BY 4.0

Credit:
This work used data acquired from the NASA/ESA HST Space Telescope, associated with OPAL program (PI: Simon, GO13937), and archived by the Space Telescope Science Institute, which is operated by the Association of Universities for Research in Astronomy, Inc., under NASA contract NAS 5-26555. All maps are available at http://dx.doi.org/10.17909/T9G593.

Processing: ImageMagick resize only to 2048x1024 (full webp+jpg), 1024x512 (_md webp), 512x256 (_sm webp). No painting, no invented bands.

NEPTUNE ONLY — all-bands-valid mask (2026-08-14). Uranus is NOT masked.
The reported "polar pinch / jagged blue-green wrap seam" at Neptune's north was a real
defect, and it was not a resize artefact: it is the RGB composite of pixels where only
SOME of the three filters have coverage. Measured in the archive TIFF, between about
+72 and +44 degrees latitude there is a broad band where one or two channels are exactly
0 while another carries signal — at +67 deg that is 67% of the row. Composited to RGB
those become saturated pure-blue / pure-green pixels, which is the sawtooth. They are
not measurements of Neptune; they are compositing artefacts.
Rule applied: a pixel is valid only where all three filters have data. Any pixel with a
channel of exactly 0, that is not already fully no-data, is set to no-data (black).
  - Exactly 0 is a safe no-data marker here: it occurs only in that +72..+44 transition
    band and never below +40 deg, where the minimum non-zero channel is 13 and rises to
    76 by +30 deg. So the rule cannot reach genuinely dark methane-band pixels.
  - Nothing is painted in. Invalid pixels are marked absent, not filled.
  - 5.78% of the map moves from partial-channel composite to no-data. Total no-data goes
    12.44% -> 18.2%; the top of valid data moves +71.3 -> +66.6 deg; the south pole
    coverage is unchanged.
  - The well-observed disc is untouched: south of +40 deg the masked source is
    bit-identical to the original (0 differing samples), and after the identical resize
    the shipped file differs from the unmasked one by 0.68/255 mean (JPEG only).
  - The resize method was deliberately left exactly as it was, so this change is the mask
    and nothing else. Longitude seam unchanged at the 87th percentile.
The same rule would move Uranus from 40.29% to 41.04% no-data (its fringe is only 0.75%
of the map). NOT applied: the helm has twice ruled Uranus's coverage edge to be the
official product. Ask before applying it there.

Native resolution:
Both official OPAL global maps are only 721x361 (0.5 deg/px). The shipped 2048x1024 tier is
therefore a ~2.8x upscale of the source — the file is 2K but the detail is not. Verified
against the archive TIFFs at 721x361: correlation 0.9999, mean abs difference 0.35/255, so
the shipped pixels are faithful to the official product. The soft rainbow striping along the
coverage edge is per-filter misregistration in the STScI product, magnified by that upscale.

Coverage note (measured from the archive TIFFs, not guessed):
Uranus 2025a carries data from +90 down to about -19.5 deg latitude only, and rows are fully
populated only to about -9 deg. 40% of the map area is black. Uranus's southern hemisphere is
turned away from Earth and in polar night, so Hubble cannot see it. That is Hubble's real map,
not a convert error. The missing hemisphere was NOT painted in.
Neptune 2025b is NOT full-latitude (an earlier version of this note claimed it was). It runs
from about +71 deg down to the south pole, fully populated only below about +58 deg, leaving
13% of the map black at the north.

On a globe (rendered check, three.js sphere, 2026-08-14):
Neptune reads fine — one small misregistration patch at the north pole. Uranus does not: the
unobserved 40% becomes a black southern cap with a hard rainbow band across the middle of the
disc, which reads as a rendering fault rather than a planet. The pixels are the official
product, so this is a "which product" decision and not a conversion bug. Left as shipped
rather than re-authored, because painting the missing hemisphere would be invention and
swapping the source is the helm's call. The previous Solar System Scope Uranus map is still in
git history at 5ca7352 if the smooth full-globe map is preferred for Act 1.

Grid convention — duplicated prime meridian (checked 2026-08-14, left alone deliberately):
The OPAL maps are 721x361, i.e. longitude 0-360 INCLUSIVE and latitude -90..+90 inclusive.
Column 0 and column 720 are byte-identical in all 361 rows, so the prime meridian is stored
twice. Every other map in this folder (Pluto, and the Solar System Scope set) is exclusive.
Consequence for the shipped 2048-wide resize: u=0 and u=1 land on nearly the same longitude,
so the wrap seam is unusually SMALL — measured at the 26th percentile (Uranus) and 87th
(Neptune) of all ordinary column-to-column steps in the image.
A stricter rebuild was tried and reverted: dropping the duplicate column and resampling
periodically (half-width wrap padding, so the scaler sees a continuous sphere) is
geometrically correct but measurably WORSE to look at. The Uranus seam moves to the 96th
percentile, because the seam then preserves the source's true 0.5 deg step while the interior
columns have been interpolated down to 0.176 deg. Content was identical either way (black
fraction 0.402, mean RGB and coverage bounds unchanged), so there was nothing to gain.
Verdict: no visible seam bug. Do not "fix" this without re-measuring first.

Neptune still recapture was attempted and REFUSED (2026-08-14):
With the mask in place a recapture does render cleanly — the sawtooth is gone — but the
capture came back with an opaque void (cornerAlphaMax 255) instead of the transparent
background every still in this library needs for compositing. Cause: the launch shell no
longer sets the `orrery-full` class, and portrait isolation depends on that hero layout
state; the engine and WebGL are both live regardless. make-engine-stills.mjs now refuses
to overwrite a good still with one that fails the transparency check, so the stale July
still is intentionally still in place. Recapture needs a machine where the shell reaches
its hero layout. Do not hand-key the background transparent: Neptune's no-data cap is
legitimately black and a colour key would delete it.

Engine stills are pre-OPAL (checked 2026-08-14):
img/engine/uranus.webp and img/engine/neptune.webp were captured 2026-07-05, before these
OPAL maps landed, so they still show the retired Solar System Scope maps and do NOT represent
what the engine now loads. Proof: the shipped Neptune still is blue-dominant (mean lit RGB
52/89/164) while neptune.jpg is green-dominant (100/192/156); the shipped Uranus still is 1.5%
black while the OPAL map on a sphere is about 45% black.
The reported "polar pinch" at Neptune's north is therefore not a defect in the shipped still —
it is what a recapture WOULD introduce, because the real scalloped coverage edge crowds into
the pole where all longitudes converge. It cannot be fixed from this folder: the pixels are the
official product, and the pole UV handling lives in orrery-webgl.js. Left for the helm.

Colour note:
These are narrow/medium-band filter composites, not true-colour photographs. Uranus lands
close to its familiar pale cyan. Neptune comes out green-dominant (lit-pixel mean RGB
100/192/156) rather than the iconic deep blue, because F547M dominates the composite. That
green cast is in the official STScI product and has been left alone — it is also closer to
the truth than the poster image: Irwin et al. (2024) reprocessed Voyager 2 data and found
Neptune is a much paler greenish-blue, the deep blue being a contrast-stretch artefact.
Pushing this map toward Voyager blue would be invention, so it was not done.
