# davit_sat_dashboard.py — Satellite Dashboard Repo

*Last verified: 2026-07-02*

Separate repo (`jonnydavx-eng/davit_sat_dashboard.py`), currently an **untouched
Streamlit blank-app template**: `streamlit_app.py` (template stub), `requirements.txt`
(streamlit only), template README, LICENSE. No custom code yet.

Run locally: `pip install -r requirements.txt && streamlit run streamlit_app.py`.

Presumed intent (name): a satellite-tracking dashboard. Nothing built — treat any
mission touching it as greenfield. Update this page when real work lands.
