# Verdict — M11: 10x Wave 3 (evals, branch-per-mission, MCP server) + Wave 2 tooling

- reviewer: verifier worker (Sonnet tier) — different model than owner (Claude/Opus)
- method: piped raw JSON-RPC to the stdio MCP server; ran `check-verdicts.mjs` with
  injected `gated` missions; parsed the maintenance workflow YAML; reproduced the CI
  steps verbatim.
- verdict: CONFIRMED (after fixes)
- findings:
  - CONFIRMED: MCP server implements initialize / tools/list / tools/call / ping /
    error handling correctly; `cortex_validate` → pass:true; `cortex_status` returns
    exactly the 4 owner-blocked items (M4, M5, M6, M10).
  - CONFIRMED: `check-verdicts.mjs` correctly distinguishes grandfathered vs gated
    (injecting `gated:true` without a verdict file → exit 1; adding the file → exit 0).
  - CONFIRMED (bug) → FIXED: the workflow's MCP smoke-test step grepped `'"pass": true'`
    which never matches the escaped JSON in the tool response, so the `checks` job would
    fail on every run. Replaced with `tools/mcp-smoke.mjs`, which spawns the server and
    PARSES responses. Proven green locally.
  - PARTIAL (bug) → FIXED: the `agent` job used `secrets.X` in a job-level `if:`, which
    GitHub does not support. Replaced with a `gate` job that reads the secret into an
    output the `agent` job tests (`needs.gate.outputs.has_key == 'true'`) — GitHub's
    documented pattern. (Live-on-Actions confirmation pending first real run.)
- residual-risk: the secret-gating fix follows GitHub's documented idiom but has not
  yet run on real Actions infrastructure (no CI access in this environment); it will be
  confirmed on the first scheduled/dispatch run once the owner adds the secret.
