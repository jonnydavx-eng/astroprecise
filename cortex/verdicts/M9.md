# Verdict — M9: 10x Wave 1 (dashboard v3, 3-tier memory, contracts, trajectories)

- reviewer: verifier worker (Sonnet tier) — different model than owner (Claude/Opus)
- method: ran all tools in scratch copies with injected breakage; parsed MCP stdio;
  cross-tabulated state.js missions vs project trends; audited dashboard field reads
  against state.js keys; `wc -l` on memory files.
- verdict: CONFIRMED (after fixes)
- findings (all raised issues resolved):
  - CONFIRMED: `validate-state.mjs` catches syntax errors, bad status, bad health,
    duplicate ids (4 injected breaks all rejected).
  - CONFIRMED: `build-index.mjs` produces a byte-identical, non-stale index (28 docs).
  - CONFIRMED: memory files follow 3-tier structure and are within budgets
    (claude 46 / grok 25 / hermes 28 / shared 56 lines).
  - REFUTED → FIXED: `cortex` project `trend` ended at 2 but the real current
    unfinished count was 1 (M11 done). Fixed to `[5,4,2,1]`, and a new invariant in
    `validate-state.mjs` now REJECTS any trend whose endpoint ≠ the live count —
    proven: injecting `9` is rejected, `1` passes. Staleness can no longer be committed.
  - PARTIAL → FIXED: `agents[].status` and `mission.contract.scope` were authored but
    never rendered. Both now render (status chip per agent row; Scope line in the
    expanded contract).
- residual-risk: none for Wave 1. The trend intermediate points are a coarse but
  honest burn-down; only the endpoint is machine-enforced.
