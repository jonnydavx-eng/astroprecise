# raw/

Drop unprocessed source material here (notes, transcripts, research dumps). The next
ingest pass reads it, folds the durable knowledge into `../wiki/`, updates
`../index.md`, and then deletes or archives the raw file. Nothing in here is
authoritative — the wiki is.
