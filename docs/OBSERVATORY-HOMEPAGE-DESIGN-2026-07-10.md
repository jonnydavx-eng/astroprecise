# OBSERVATORY HOMEPAGE — Build Design (expert panel)
**Date:** 2026-07-10 ~23:30 · Owner brief: "start the homepage with the full model, navigation built around it, explore page improves and becomes the homepage."
**Laws:** controls never cover the disc · cast reachable 0-interactions at rest · one Three instance · honesty labels · warm-twilight (NO cyan CTAs — brass/dark-ink treatment specified) · S24 fill budget.


---

## Layout Architecture

The homepage becomes the observatory: index.html keeps its DOM/boot chain (ap-home-bootstrap.js → ap-award-orrery.js → orrery-loader.js, poster→lite→full ladder, Earth rest frame scale 0 True-Time boot) but viewport 1 is recomposed to explore.html's full-bleed stage pattern with one structural upgrade — a true in-flow PLINTH TRAY below the canvas that hosts the deck (owner law: controls never over the disc), a slim overlay masthead bar, and the cast form as a left side-rail card on desktop / the tray's top band on mobile. Zero DOM moves are required for the core structure (stage → hero-copy → deck is already the flex order in #heroChapter); the work is one new CSS sheet (css/ap-observatory-home.css, loaded LAST after ap-model-window.css), an updated critical-CSS block in index.html head, and ~20 lines of tray-height sync JS copied from explore-boot.js. Below the fold the v702 blocks survive in order (path cards → daily strip → shop → footer, ~3.8k px desktop / ~4.2k mobile, inside Grok's gates) with inert data-ap-beat attributes as seams for the future Scale-Ladder slice. explore.html stays live as the #m= deep-link receiver room and adopts the same tray pattern in a small follow-up.

### PAGE ARCHITECTURE (what transfers vs rebuilds)

index.html stays the homepage file (SEO root, schema, boot chain). TRANSFERS VERBATIM from explore.html/explore-page.css: full-bleed stage pattern (.explore-stage rules at css/explore-page.css:29-79 — stage relative, orrery wrap/viewport/canvas absolute inset:0, aspect-ratio:auto), collapsible honest-model legend (explore.html:182-199 markup + the legend() IIFE from js/explore-boot.js:281-294, collapsed default), syncNavHeight tray-measure pattern (explore-boot.js:263-279). ALREADY ON INDEX, KEEP UNTOUCHED: #apAwardOrreryWrap>.ap-award-orrery-frame>#orrery-viewport (poster/earth-loader/lite-launch/canvas/dom-labels, index.html:358-384), .ap-model-window chrome + caption script (index.html:870-907), full deck #orrery-lite-deck with ALL ids (#ap-scale-strip, .orrery-scale-btn, .lite-vp-btn+seals, #lite-date-display/#lite-now-btn/#lite-scrub, #ap-cosmic-journey/#ap-cosmic-flight-launch, #ap-orbits-toggle, #ap-orrery-readout — orrery-loader.js wires by these ids), intro gate script (ap-await-webgl, index.html:191-199), scripts block (ap-home-bootstrap/weekly-sky/home-daily/home-match). REBUILT (CSS-only where possible): masthead → slim overlay bar; .hero-copy → cast dock (rail/band); deck → plinth tray; v640 critical inline CSS block (index.html:156-175) rewritten to the new geometry. NEW FILE: css/ap-observatory-home.css scoped to body.ap-observatory-home (body gets class ADDED, ap-award-511 KEPT — every existing sheet/JS keys off it), linked AFTER css/ap-model-window.css?v (load-order law: it must out-cascade the !important sheets ap-structure-clean.css and ap-model-window.css; match their specificity and use !important).

### VIEWPORT 1 · stage (desktop 1440x900)

#heroChapter .hero-solar-stage becomes the full-bleed observatory: width 100%; height calc(100svh - var(--ap-tray-h, 128px)); min-height 560px; background: var(--ap-jet,#000) center + the explore radial (radial-gradient(120% 90% at 50% 8%, #101014 0%, #000 55%)); overflow hidden; isolation isolate. .ap-award-orrery-wrap: absolute inset 0, width/height 100%, max-width none (kills structure-clean's width:min(50vh,500px) sizing at ap-structure-clean.css:96-133 via later cascade). .orrery-viewport and #orrery-canvas: absolute inset 0, aspect-ratio auto, border-radius 0 (copy explore-page.css:63-79). Initial model state unchanged: Earth rest frame scale 0, True-Time terminator — isHomeHeroEmbed()/setDefaultEarthFrame() boot from ap-v701 fires as-is because the page identity and ids don't change. Model Window frame: inset 10px 12px (reuse the explore mount values from ap-model-window.css:164-167); plate stays the home default bottom-left (left 16px / bottom 12px — base rules ap-model-window.css:91-113, no override needed); plate bottom edge sits ~14px above the tray, over starfield, never the disc. LIVE dot/plate honesty gates (html.orrery-full, html.ap-model-revealed) untouched.

### VIEWPORT 1 · plinth tray (desktop)

#orrery-lite-deck IS the tray — it is already the flex sibling after the stage (order 3, index.html:809). Restyle: position static (in flow), width 100%, height var(--ap-tray-h,128px), box-sizing border-box, background var(--ap-jet,#000), border-top 1px solid rgba(200,205,214,0.22) (the shelf line — visually continues the Model Window hairline), padding 10px clamp(16px,3vw,40px). Layout: CSS grid, 2 rows. ROW 1 (h 48px): .ap-scale-strip with full 7-stop __track re-enabled (override ap-structure-clean.css:190-192 display:none) left-aligned; .ap-scale-strip__caption ('Positions live (VSOP87) · distances schematic') inline right of the track, font 0.66rem IBM Plex Mono, color rgba(232,235,240,.6); #ap-orbits-toggle at track end. ROW 2 (h 52px): .lite-vp-controls (10 planet pills, 44px targets, seals visible ≥1024px) left | .ap-orrery-time (#lite-date-display + #lite-now-btn) center | #lite-scrub flex-1 min-width 260px max-width 420px | .ap-orrery-cosmic-journey + .ap-orrery-cosmic-flight right (re-enable vs ap-structure-clean.css:180-188 hides). All re-enables live in ap-observatory-home.css with !important. The deck's [hidden] feature-detect lifecycle is untouched — on the 2D/low tier the scale strip and journey button simply never unhide and row 1 collapses (grid-auto-rows, row 1 min-content). Sync --ap-tray-h from JS: copy syncNavHeight (explore-boot.js:263-279) into js/ap-home-bootstrap.js, writing --ap-tray-h from deck.getBoundingClientRect().height on resize + orrery-scale-change (+60ms).

### VIEWPORT 1 · cast dock — DESKTOP side rail (≥1024px)

.hero-copy becomes an absolute overlay rail on the stage (it is already absolute-positioned material; container #heroChapter is position:relative): left 32px; top 50%; transform translateY(-50%); width 360px; max-height 440px; z-index 6 (above canvas 1, below legend 8); background rgba(10,10,14,0.88); border 1px solid rgba(200,205,214,0.24); border-radius 16px; padding 1.1rem 1.25rem 1.2rem; text-align left. Contents (existing DOM, index.html:327-352): .eyebrow-rose kept; h1.display clamp(1.3rem,1.8vw,1.6rem) 2 lines max; .standfirst 0.85rem max 34ch; #hero-chart-form date input (min-height 48px, width 100%) stacked above full-width 'Calculate my chart' button (min-height 48px) — stacked, not row, inside the 360px rail; .hero-form-note restored (display:block, 0.72rem, rgba ink .6) — it was hidden by structure-clean:152-157; keep .hero-solar-trust/.hero-action-chips hidden (structure-clean rule stands). CTA COLOR: Calculate button = dark ink #0B0906 on brass gradient linear-gradient(160deg,#E8C872,#C9A227) (the verified 8.2:1 explore-return__primary treatment, explore-page.css:241-252) — NO cyan/aurora. This rail carries the crawlable h1+intro (SEO requirement met in viewport 1). DISC CLEARANCE MATH at 1440x900: stage 1440x772; disc center = stage center (720, 386); disc safe-zone diameter = 0.62 x stageH ≈ 480px → disc left edge x=480; rail right edge = 32+360 = 392 → 88px clearance. Rail is the FIRST candidate; if the measured live disc (gate below) intersects, fall back to Plan B: cast band docked left-half of a 176px tray (spec'd in Risks).

### VIEWPORT 1 · masthead — slim overlay bar

Existing header.masthead#apMasthead (index.html:228-309) restyles to a single-row overlay: position absolute; top 0; left 0; right 0; height 56px (48px ≤640px); z-index 30; display flex; align-items center; justify-content space-between; padding 0 clamp(12px,2vw,32px); background linear-gradient(to bottom, rgba(0,0,0,0.78), rgba(0,0,0,0)); border-bottom 0. Inside: .masthead-logo left (logo-mark 30px, wordmark clamp(1rem,1.4vw,1.15rem), .masthead-rule display:none); .contents-nav right as inline row (5 links — drop the 'Explore' li since home IS the observatory; keep Chart/Sky/Daily(#today)/Shop/My Sky; 0.72rem uppercase, 44px targets, gap 4px); #masthead-dateline display:none in the bar at ALL widths (the fill JS at index.html:1799-1844 still runs harmlessly on the hidden node — do NOT relocate DOM this wave; the live Sun/Moon line re-surfaces in the Scale-Ladder slice). ≤640px: .contents-nav display:none entirely (bottom-nav is the phone nav); bar = logo lockup only, centered-left. main#main-content gets no top padding — the stage runs under the bar by design (disc top edge ≥146px desktop / ≥170px mobile, clear of the 56/48px bar).

### VIEWPORT 1 · honest-model legend (both breakpoints)

Port explore's legend verbatim into #heroChapter markup (aside.explore-legend block, explore.html:182-199, ids kept explore-legend/explore-legend-toggle/explore-legend-body) as the last child of .hero-solar-stage; reuse explore-page.css:130-187 styles by copying them into ap-observatory-home.css under the home scope. Position: top calc(56px + 12px), right 16px, z-index 8, width min(21rem, calc(100vw - 2rem)); STARTS COLLAPSED (copy the legend() IIFE from explore-boot.js:281-294 into ap-home-bootstrap.js whenReady). Mobile: top calc(48px + 8px), right 8px. The ladder copy (Earth→System live / Oort illustrative / Stars schematic / Galaxy Gaia-style / Cosmos decorative) is crawlable DOM text — honesty labels survive structurally. Collapsed toggle chip is ~44px tall in the top-right starfield corner; expanded body max-height 50vh overflow-y auto (existing rule) so it never reaches the disc.

### VIEWPORT 1 · MOBILE 390x844 composition

Vertical budget (fixed bottom-nav 65px stays — existing ap-v699 injection + fixed-bar CSS in ap-structure-clean.css:471-561): overlay bar 48px (overlays stage) · stage calc(100svh - var(--ap-tray-h-m,168px) - var(--bottom-nav-h,65px)) ≈ 611px · tray 168px · bottom-nav 65px. TRAY (mobile) = .hero-copy + #orrery-lite-deck stacked (existing DOM order 2 then 3 — zero moves), both restyled as opaque bands on var(--ap-jet), border-top hairline on .hero-copy only: ROW A cast band (from .hero-copy, h ≈ 76px): h1 compressed to one visible line 0.95rem (SEO-visible, not display:none) + form row (date input flex-1 + Calculate button, both 48px, gap 8px, padding 8px 12px); hide standfirst ≤640px (h1 + button text carry meaning). ROW B planet pills (h 52px): reuse the compact HUD child rules from ap-model-window.css:287-317 (nowrap, overflow-x auto, 44px targets, seals hidden) but SUPERSEDE the wrapper geometry at ap-model-window.css:251-277 — body.ap-observatory-home resets: margin 0 (kills the -108px overlay pull-up), background var(--ap-jet) opaque (kills the gradient scrim), position static, height auto. ROW C (h 40px): .ap-scale-stepper (‹ EARTH ›, re-enabled, left) + #lite-scrub flex-1 + #lite-now-btn 48px right; #lite-date-display 0.62rem under stepper or ellipsized (max-width 30vw). Journey/flight buttons stay hidden ≤640px (current decision stands until the Ladder slice). Model Window mobile plate: override the ≤640 home rules (ap-model-window.css:207-227 put it below-canvas) → top-left INSIDE frame at top calc(48px + 6px), left 10px (explore mobile pattern, ap-model-window.css:189-200) — sits in starfield corner, disc top edge ≈175px, zero overlap. DISC: safe-zone = min(0.62x611, 0.80x390) ≈ 312px → Earth renders ~290-310px vs today's 210px (the mobile 'full model' win). Engraved-wheel/no-JS path: html:not(.ap-lite-hero) keeps deck hidden (ap-structure-clean.css:267-270 stands); cast band + wheel-in-stage still compose.

### VIEWPORT 1 · deck control set per breakpoint (summary table)

≥1024px tray: 7-stop scale track + caption + orbits toggle + 10 planet pills (with seals) + date/Now + scrub + Cosmic journey + Cosmic flight. 641-1023px tray (--ap-tray-h:120px): scale STEPPER (not track) + pills (seals hidden, scrollable) + date/Now + scrub; journeys hidden. ≤640px tray: stepper + pills + scrub + Now (per mobile rows above). ALL controls live in the in-flow tray — measurable guarantee: tray top edge == canvas bottom edge (stage height formula), so control-vs-disc intersection is zero BY CONSTRUCTION; only the four overlays (bar, rail, legend, plate) need the runtime clearance gate. Feature-detect lifecycle untouched: #ap-scale-strip/#ap-cosmic-journey/#ap-orbits-toggle stay [hidden] until orrery-loader detects the WebGL engine; low-tier/2D visitors get pills + scrub + Now only (lite engine supports focusPlanet + setDayOffset).

### Disc-clearance verification gate (owner law, measurable)

Add to tools/visual-check (extend the settled-screenshot suite, ≥1400ms settle per the FareRadar re-score rule): compute disc circle as center=(canvasRect.centerX, canvasRect.centerY), radius=0.31 x min(canvasRect.w, canvasRect.h) x 2 /2 (i.e. diameter 0.62 x shorter canvas side — the engine's Earth-rest framing envelope; recalibrate the 0.62 constant once from a settled screenshot pixel-measure of the terminator disc). Assert ZERO intersection (plus 24px margin) between that circle and getBoundingClientRect() of: #apMasthead, .hero-copy (rail/band), #explore-legend (collapsed AND expanded states), .ap-mw-plate, #orrery-lite-deck. Run at 1440x900 and 390x844. Also re-assert Grok's gates: hero submit.bottom ≤ viewportH-8 (desktop) and ≤ viewportH-65-8 (mobile, above bottom-nav); bodyH ≤5500 desktop / ≤7000 mobile; exactly ONE canvas element #orrery-canvas on the page.

### BELOW VIEWPORT 1 · content flow (static v1 with beat seams)

Keep v702 blocks in existing order, ids, and content — no re-deletion, no new sections: (1) #instrumentsChapter 'Where to go from here' path cards (index.html:915-997) — add data-ap-beat="system" to the section tag (inert attribute, zero CSS/JS this wave); lead copy line updates 'Cast above, then...' → 'Cast in the observatory above...' (single string edit); (2) #dailyChapter with #today anchor + #daily-tool-root (index.html:1000-1015, home-daily.js renders into it unchanged) — data-ap-beat="today"; (3) #shopChapter 3 featured cards + trust strip (index.html:1616-1688) — data-ap-beat="galaxy"; (4) footer.night-watch (index.html:1695-1776) unchanged except footer 'Explore 3D' link relabels 'Observatory' only if the IA vocabulary pass (WOW plan I1) ships in the same wave — otherwise leave. Hidden chapters (skyChapter, compareChapter, methodChapter, skyGuidesWrap, libraryChapter, matchChapter, qaChapter, obsGalleryChapter) stay display:none'd in DOM exactly as structure-clean left them (SEO + future beat cannibalization). #heroChapter gets data-ap-beat="earth". These four data-ap-beat hooks are the ONLY Scale-Ladder seams shipped now — the future IO beat controller binds to [data-ap-beat] without HTML churn. Estimated bodyH: 900 (vp1) + ~800 path + ~700 daily + ~850 shop + ~600 footer ≈ 3.85k desktop; ≈ 4.2k mobile — inside gates with ~1.6k/2.8k headroom for the ladder beats later.

### Critical inline CSS update (index.html head, lines ~156-175)

Rewrite the v640 critical block to prevent first-paint FOUC of the new geometry (this block is the pre-stylesheet skeleton): .ap-award-511.ap-observatory-home #heroChapter{display:flex;flex-direction:column;background:#000;padding:0} · .hero-solar-stage{order:1;position:relative;width:100%;height:calc(100svh - 128px);min-height:560px} · .ap-award-orrery-wrap{position:absolute;inset:0;width:100%;max-width:none;transform:none;left:auto;top:auto} · .hero-copy desktop rail geometry (abs left 32px top 50%) behind @media (min-width:1024px), mobile band geometry in base · #orrery-lite-deck{order:3;min-height:128px;background:#000} · masthead overlay bar rules (position absolute, height 56px). Keep untouched: the ap-await-webgl poster rules (lines 154-155, 166-168 — earth.webp still = LCP element, must not change) and the hero-live-pill keyframes (now unused but harmless). The masthead-structure rules at lines 109-123 shrink to the bar variant.

### css/ap-observatory-home.css — file contract

New sheet, ~220 lines, scoped body.ap-observatory-home (which co-exists with ap-award-511). LINK ORDER LAW: after css/ap-model-window.css in index.html head (it must override both !important sheets; document the law in its header comment like ap-model-window.css:7-10 does). Sections: (1) stage full-bleed (supersedes structure-clean:96-133 + critical block leftovers); (2) tray grid desktop/tablet/mobile incl. re-enables of .lite-vp-controls/.ap-scale-strip__track/journeys (vs structure-clean:180-192); (3) cast rail/band; (4) masthead bar; (5) legend (copied explore-page.css:130-187); (6) mobile HUD supersede block (resets ap-model-window.css:251-277 wrapper geometry, keeps child rules); (7) mobile plate reposition; (8) PRM/no-JS guards (html:not(.ap-lite-hero) → tray shows cast band only; stage min-height 420px with engraved wheel centered). Palette: silver/parchment/brass tokens only (--ap-jet interior, rgba(200,205,214,x) hairlines, brass gradient CTA); the .ap-mw-dot aurora #7EC8E8 LIVE lamp is left as-is this wave (status lamp, not CTA) but add a one-line token override hook (--ap-live-dot) so the owner's cyan review can flip it to #E8C872 in ap-palette-2026.css without touching this sheet.

### JS deltas (all in existing files, ~30 lines total)

(1) index.html body tag: add class ap-observatory-home. (2) js/ap-home-bootstrap.js: append syncTrayHeight() — copy of explore-boot.js:263-279 measuring #orrery-lite-deck (+ .hero-copy on ≤640px) into --ap-tray-h/--ap-tray-h-m, bound to resize + orrery-scale-change(+60ms) + load(+300ms); append the legend() collapse IIFE (explore-boot.js:281-294). (3) NO changes to ap-award-orrery.js, orrery-loader.js, lite-orrery.js, orrery-webgl.js, ap-horizon-features.js (bottom-nav injection), home-daily.js — the whole promotion chain and Earth-rest boot key off ids and page identity that don't change. (4) Do NOT add the #m= deep-link receiver to index this wave (explore.html remains the receiver per IA §5); note as a seam. Version wave discipline: bump ?v= on index.html-linked assets + new sheet, regen SW precache to include css/ap-observatory-home.css, sw.js V LAST (stale-gzip rule), then tools/visual-check npm run all + after_project_edit.ps1 -Project 'AstroPrecise' + signed AGENT-HANDOFF line.

### explore.html follow-up (same wave if time, else next)

Explore adopts the same plinth-tray law so the flagship room also never overlays controls on the disc: .explore-stage height becomes calc(100svh - var(--explore-nav-h) - var(--explore-deck-h)) and .explore-stage .explore-deck goes position:static in-flow below (explore-page.css:190-202 currently pins it as a gradient overlay INSIDE the stage — at Earth/Galaxy zooms it can veil the model's lower arc). explore-boot's existing --explore-deck-h measurement (explore-boot.js:269-273) already feeds the variable. Also apply I7 from the WOW plan: .explore-return keeps 'Cast this sky into your chart' as the single brass-primary pill (already fixed to dark-ink-on-brass in wave A). Everything else on explore stays — it remains the #m= receiver and the 'Observatory' destination for nav/deep links.

**Risks:**
- Disc size at full-bleed is engine-framed, not CSS-framed: Grok's v702 sized the Earth disc via the wrap box (380→450px); with the wrap at inset:0 the disc scales to the engine's Earth-rest framing on a landscape canvas. The 0.62-of-shorter-side safe-zone constant is an estimate from explore's rendering — it MUST be recalibrated from one settled screenshot before the clearance gate is trusted. Plan B if the desktop disc intersects the left rail: drop the rail and dock the cast band into a taller 176px tray (cast left half, deck right half) — zero-overlap by construction, slightly weaker eye-level conversion.
- Cascade war: ap-structure-clean.css and ap-model-window.css are both !important sheets with home-specific hero rules (structure-clean:96-274, model-window:207-356). The new sheet wins only by loading last at equal-or-higher specificity — any future agent inserting a sheet after it, or a ?v= inject re-appending an old sheet (the explore-boot injectCss guard exists for exactly this), silently reverts the layout. Enforce with a load-order comment in index.html head AND a visual-check assertion that #orrery-lite-deck position is static.
- Concurrent churn: another agent is actively finishing Model Window work in the same working tree (branch ap-v683-wave, uncommitted). All ap-model-window.css line references here will drift — implementer must bind to selectors, not line numbers, and re-read that file at implementation time.
- Structure-gate renegotiation: viewport 1 grows from ~480px stage to ~100svh, and the hero h1 moves into an overlay rail. Desktop cast is still first-paint (rail is in viewport 1) but the 'submit fully in first viewport' measurement now depends on 100svh math on short laptops (768px height) — add a @media (max-height:800px) tray compression (--ap-tray-h:112px, rail max-height 380px) and re-run Grok's gate script before claiming PASS.
- LCP/SEO: the LCP element (img/engine/earth.webp poster background) and the crawlable h1/intro survive, but the h1 shrinks visually on mobile to one line inside the cast band — verify Google renders it (it does; it's visible text, not hidden) and keep the FAQ-salvage task (WOW plan I3) separate so index's schema footprint only grows.
- Battery/perf: a ~611px-tall always-visible canvas on S24 renders more pixels than today's 210px wrap. The IS_PHONE DPR clamp must survive (OrbitLab masterplan law) and the offscreen rAF pause (WOW plan P12) becomes worth shipping in the same arc; audit via the Playwright 390px mobile suite (tools/visual-check mobile-structure-audit.mjs) plus an S24-over-Tailscale eye check before merge — CDP cannot render <490px.
- Bottom-nav + tray double chrome on mobile eats 233px of an 844px screen; if the owner's phone eye-check reads it as heavy, the sanctioned relief valve is hiding the bottom-nav while #heroChapter is >60% visible (IO toggle) — do NOT shrink the tray below 44px targets or re-overlay controls on the stage.
- The aurora-cyan LIVE dot and logo diamonds remain the only cool accents in viewport 1 pending the owner's cyan review; the --ap-live-dot token hook is shipped so the flip is a one-line palette edit, but do not swap unilaterally — the logo SVG cyan is brand-level and owner-owned.

---

## Navigation Built Around the Model

Navigation design for the model-first homepage: the masthead becomes transparent frame-edge chrome over a 100svh stage (rebuilt from the existing ap-nav-model.js/renderNav single source, relabelled Observatory/Chart/The Sky/Daily/Shop), the shipped mobile bottom tab bar stays as the primary mobile nav with the stage sized above it, a new ~130-line js/ap-nav-interplay.js gives desktop nav links dwell-gated model responses (chips from getBodyReadout, at most one engine-eased camera call per 2.5s via focusPlanet only, gated on html.orrery-full, chips-only for keyboard/PRM/2D), chrome recedes to whisper opacity via an html.ap-chrome-idle class (opacity-only, focus-within always restores full opacity), and viewport 1 exits through a museum-plate scroll cue in the plinth band plus the cast dock and nav links. All camera work routes through existing public APIs (focusPlanet/getBodyReadout/getScaleLevel/snapToNow) honoring the inertia law; no control ever overlaps the model disc (masthead is a top gradient band, deck/cast live in the plinth tray, cue sits in the tray).

### 1. Vocabulary + targets — js/ap-nav-model.js (single source, one commit, no URL renames)

Edit C:\Users\jonny\OneDrive\astroprecise\website\js\ap-nav-model.js only (app.js renderNav() consumes it; never hand-edit navbar__nav). NAV_PRIMARY = [['index.html','Observatory'],['chart.html','Chart'],['ephemeris.html','The Sky'],['horoscope.html','Daily'],['shop.html','Shop']] — the homepage IS the Observatory, so the explore.html row is replaced by index.html; app.js:442 `location.pathname.split('/').pop() || 'index.html'` already normalizes '/' to index.html so aria-current='page' lands on Observatory on the homepage with zero app.js changes. NAV_BOTTOM_TABS = [['index.html','Observatory','star4'],['chart.html','Chart','spiral'],['ephemeris.html','The Sky','telescope'],['horoscope.html','Daily','crescent']]. NAV_EXTRAS renames per IA doc: "Tonight's Sky"→'Tonight', "This Week's Sky"→'This Week'. NAV_MORE_EXPLORE: 'Readings'→'Cosmic Story', 'Transits'→'Your Transits' (mysky retirement stays blocked on Jonny sign-off — do not touch that row). ALSO update the hardcoded fallback tabs array at js/ap-horizon-features.js:195-199 to the same four rows (it ships when AP_NAV is absent) and the footer 'Around the model' list in the footer model (ap-footer-inject.js source): drop the separate 'Explore 3D'/'Home orrery' pair for one 'Observatory' → index.html row. Grep-verify residuals per the workflow-map rule before declaring done: rg "Explore 3D|Tonight's Sky|This Week's Sky|Full sky tour" website/.

### 2. Desktop masthead — transparent frame-edge bar over a full-bleed stage

Keep the existing .site-header/.navbar markup (explore.html:90-119) and renderNav() output verbatim — placement changes only, in css/explore-page.css (which becomes the homepage stage sheet): body.page-observatory .site-header { position: fixed; top:0; left:0; right:0; z-index:30; background: linear-gradient(to bottom, rgba(7,7,10,.88) 0%, rgba(7,7,10,.55) 55%, rgba(7,7,10,0) 100%); border:0; } and .explore-stage height goes from calc(100svh - var(--explore-nav-h)) to 100svh (min-height 30rem stays). Because the bar now overlays the stage, the museum frame must start under it: .ap-model-window inset-top becomes calc(var(--explore-nav-h, 64px) + 6px) (override in css/ap-model-window.css page-observatory block; the sheet is !important — match specificity), and .explore-legend top becomes calc(var(--explore-nav-h) + 0.9rem). --explore-nav-h is already measured by syncNavHeight() in js/explore-boot.js:263-277 — reuse unchanged. Disc-overlap law: the bar is a top band; the engine's Earth-rest frame centers the disc at ~46-50% viewport so no intersection at 1280x800, but this is a measured gate not an assumption (see element 11). No new nav placements (no corner clusters): logo left = home anchor, five primary links + More right, profile gem — all existing classes (.navbar__logo, .navbar__nav, .navbar__more, .navbar__profile-top).

### 3. Mobile nav — bottom tab bar is THE primary nav; masthead shrinks to logo+gem+burger

The shipped fixed 65px bottom bar (wave-A: injected by ap-horizon-features.js ensureBottomNav() from AP_NAV.NAV_BOTTOM_TABS + pinned Cast item, --bottom-nav-h custom property set at :250) stays exactly as-is and becomes the homepage's primary mobile nav. Reconcile the model-first viewport: ≤640px .explore-stage height = calc(100svh - var(--bottom-nav-h, 65px)) so the stage + plinth tray end above the tabs (tabs never overlap the disc by construction). Top masthead on mobile keeps only logo + profile gem + burger (current behavior — .navbar__nav is desktop-only), same fixed transparent-gradient treatment as desktop. Tab labels: first tab ships as 'Observatory' (11px label, IA doc says it fits but is the longest word) — measured fallback if it wraps/ellipsizes at 390px: tab-only short label 'Home' (do NOT invent a third name; 'Observatory' remains the one name in copy/h1/drawer). Active state: ensureBottomNav's current-page matching marks the index tab active on the homepage. The pinned Cast tab (bottom-nav__item--cast → chart.html) remains the guaranteed viewport-1 cast entry on mobile regardless of what the cast dock does.

### 4. Cast entry seating — nav's contract with the cast dock (conversion gate)

Nav reserves the PRIMARY ACTION SEAT in the plinth/deck band for the cast dock (owned by the cast-dock role; nav must not restyle it): desktop = right end of the deck row inside .explore-deck / #orrery-lite-deck; mobile = a full-width row inside the ap-model-window.css plinth tray (css/ap-model-window.css:253-369 grid — add grid-row 3), NEVER floating over the canvas. Its visual language is the shipped high-contrast pill: dark ink on brass per the wave-A fix (explore-return__primary pattern, 8.2:1 — explore-page.css:241-252), not cyan. Bottom-nav pinned Cast + this dock = two cast entries in viewport 1 on mobile; desktop = dock + 'Chart' primary link. The chrome-recession system (element 7) may dim the cast dock only to 0.85, never to whisper tier — casting stays the one obvious action from first paint.

### 5. Nav↔model interplay — new js/ap-nav-interplay.js (~130 lines, index only, loaded after app.js)

Event-delegated mouseenter/mouseleave/focusin on .navbar__nav .navbar__link + .bottom-nav__item (desktop pointer:fine only for camera; focusin = chips only). INTENT TABLE (href → action): chart.html → chip `Orrery3D.getBodyReadout('sun').label + ' · cast yours'`; camera: ONLY if html.orrery-full && Orrery3D.getScaleLevel() <= 2 && cooldown clear → Orrery3D.focusPlanet('earth') (engine's own eased tween — the inertia law is satisfied by routing through public APIs, never writing camera state). ephemeris.html → chip-only: getBodyReadout('moon').label + ' · ' + getBodyReadout('sun').label + ' · live now' (readouts are computed, never hardcoded — honesty rule). horoscope.html → chip-only: today (toLocaleDateString en-GB) + ' · ' + getBodyReadout('sun').label. shop.html → chip 'wear this sky · prints from this model'; camera: ONLY if getScaleLevel() ∈ {1,2} (Saturn on screen) → focusPlanet('saturn') (the beauty angle); at scale 0 or ≥3 chips-only — never yank across scales on a hover. index.html/logo → no-op (current page). DAMPING/CALM RULES (hard, in this order): (a) dwell gate — camera intent fires only after 300ms continuous hover; mouseleave before that cancels the timer; (b) cooldown — max ONE camera call per 2500ms, tracked module-global; (c) no revert — hover-out never flies back (ping-pong disorients; Earth is the rest frame anyway); (d) user-is-driving lockout — pointerdown/wheel on #orrery-canvas sets a 10s lockout during which all camera intents are ignored (chips still show); (e) suppress while Orrery3D.isIntroActive() returns true or before html.orrery-full exists (2D fallback path = chips-only via the same code path, LiteOrrery untouched); (f) PRM (matchMedia prefers-reduced-motion) = chips-only, zero camera calls ever; (g) touch devices = module inert for camera (no hover), bottom-nav taps navigate normally. CHIP COMPONENT: one element .ap-mw-chip appended inside #apModelWindow (pointer-events:none inherited), styled in css/ap-model-window.css to the museum-plate spec — IBM Plex Mono 400, 0.66rem, letter-spacing .14em, silver var(--ap-silver,#C8CDD6) on var(--ap-jet) with the 1px rgba(200,205,214,.28) border, positioned top-right inside the frame (right:16px; top matches plate baseline), desktop-only (display:none ≤640px), fade 200ms in/300ms out, aria-hidden='true' (hover ephemera, no SR announcements — the same facts exist in page copy). SILVER ONLY — no aurora cyan in any interplay chrome (cyan CTAs are under owner review).

### 6. Interplay honesty + one-Three law compliance

All interplay reads/writes go through the single existing engine instance's public API (window.Orrery3D from js/orrery-webgl.js: getBodyReadout:9094, getScaleLevel:9175, focusPlanet:8563, snapToNow:7861) — no second canvas, no direct camera/quaternion access, so Slice-2's damper/idle-drift work composes underneath untouched (nav writes intents, the engine eases). Chips render only engine-derived strings (bodyReadout returns {label} like 'Mars in Gemini · 14° ℞'); if getBodyReadout returns null (ephemeris not ready) the chip simply doesn't show. Idle behavior between interactions is Slice 2's gentle-drift territory — ap-nav-interplay.js must not add its own idle camera motion.

### 7. Chrome recession — html.ap-chrome-idle (module lives in ap-nav-interplay.js, CSS in explore-page.css)

Idle detector (~35 lines): any pointermove/pointerdown/wheel/keydown/touchstart resets a timer; after 6000ms desktop / 5000ms touch with no input, add html.ap-chrome-idle; remove it on the next input (chrome returns within a 150ms fade-in). TWO TIERS, opacity only, no visibility/display changes ever: whisper tier → .site-header, .explore-legend, .ap-mw-scrollcue, .ap-mw-plate to opacity .4 (transition: opacity .8s ease); presence tier → #orrery-lite-deck/.explore-deck and the cast dock to opacity .85 (conversion + controls stay clearly present); .bottom-nav on mobile joins the presence tier at .85, never whisper. A11Y (non-negotiable): (1) html.ap-chrome-idle .site-header:focus-within, .explore-deck:focus-within, .bottom-nav:focus-within, .explore-legend:focus-within { opacity:1 !important; transition-duration:.15s } — a focused control's container is always full-opacity; (2) keydown is in the reset list so keyboard users exit idle on their first keystroke and tab order/hit targets are never removed (opacity-only rule); (3) pointer-events stay auto on receded chrome — a click on 0.4-opacity chrome both acts and restores. PRM: @media (prefers-reduced-motion: reduce) { transition: none } — states still apply, swaps are instant (matches the ap-model-window.css:372-383 PRM pattern). The LIVE plate's honesty function survives recession (0.4 opacity still legible on jet; it returns to 1 on any input).

### 8. Scroll affordance — .ap-mw-scrollcue in the museum-plate language

One cue, bottom-center of viewport 1, seated in the plinth band BELOW the deck (desktop: centered directly under .explore-deck's bottom edge, inside the stage's last 40px; mobile: the bottom edge of the plinth tray grid, grid-row 4, height 24px) — by construction never over the disc. Markup in index.html: <a class='ap-mw-scrollcue' href='#site-below' aria-label='Continue to the rest of the site'><span class='ap-mw-scrollcue__line' aria-hidden='true'></span><span class='ap-mw-scrollcue__label'>CONTINUE</span></a>. Style in css/ap-model-window.css: label = IBM Plex Mono 400, 0.6rem, letter-spacing .18em, rgba(200,205,214,.55); line = 1px × 22px vertical silver hairline with a 4px dot descending via a 2.4s ease-in-out loop (reuse the apMwPulse cadence — calm, ≤2Hz); PRM = animation:none, static line+chevron. Behavior (5 lines in ap-nav-interplay.js): first scroll past 80px adds .ap-mw-scrollcue--seen (opacity 0, pointer-events none, 400ms); click scrolls to #site-below with behavior:'smooth' unless PRM (then default jump). #site-below = the first below-fold section (cast form full version / daily strip / shop block / footer + the visible h1+intro copy for SEO live down there — content roles own the internals; nav owns the door).

### 9. Retire .explore-return on the homepage; exits become nav + cast + cue

The explore page's corner return-nav (explore.html:258-262, .explore-return) exists because explore was a satellite; on the homepage it duplicates the masthead and violates calm. Do not carry it over to index. Its one valuable member — 'Cast this sky into your chart' primary pill — is superseded by the cast dock (element 4). Viewport-1 exits are exactly: masthead links (desktop), bottom tabs (mobile), cast dock (both), scroll cue (both), and deck journey buttons (#ap-cosmic-journey / #ap-cosmic-flight-launch stay in-model, unchanged). On explore.html itself (until it is stubbed) keep .explore-return as-is.

### 10. Deep-link + skip-link plumbing the nav depends on

(a) #m=<ISO|now>&focus=<body> receiver: parseModelHash/applyModelDeepLink/scheduleDeepLink from js/explore-boot.js:75-195 must ship in the homepage boot (the new index boot is explore-boot's descendant, so this is inclusion not port) — every sitewide 'model moment' emitter then points at ./#m=… instead of explore.html#m=…. (b) explore.html becomes a param-preserving gold stub → ./ (index-lite pattern: canonical + meta refresh + location.replace('./'+location.search+location.hash) — hash MUST be preserved or all shipped #m= emitters break), noindex, removed from ROOT_HTML in tools/generate-sw-precache.mjs, sitemap updated — one burial commit per the IA doc rules, only after the receiver is verified live on index. (c) Skip links at body top of index: 1st 'Skip to cast your chart' → the cast dock id, 2nd 'Skip to the model controls' → #orrery-lite-deck, 3rd 'Skip to site content' → #site-below (reuse .skip-link class from explore.html:84). (d) h1 stays crawlable: either the sr-only h1 pattern (explore.html:131) in viewport 1 plus a real visible h1 in #site-below, or a visible h1 in the legend panel — content role decides, but nav guarantees #site-below exists as the SEO copy anchor.

### 11. Verification gates (owner law + calm) — tools/visual-check additions

(1) Disc-overlap gate at 1280x800 AND 390x844: after ≥1400ms settle (FareRadar settled-screenshot rule), measure the Earth disc bounding circle (from #orrery-canvas center + engine wrap size) vs getBoundingClientRect of .site-header, .explore-legend, .explore-deck, .ap-mw-plate, .ap-mw-chip, .ap-mw-scrollcue, .bottom-nav — zero intersection required. (2) Interplay calm gate: Playwright hovers Chart→Shop→The Sky in <2.5s and asserts at most ONE engine camera transition fired (instrument via a counter monkey-patch on Orrery3D.focusPlanet in the test page). (3) Recession a11y gate: force ap-chrome-idle, Tab to a masthead link, assert computed opacity of .site-header === 1. (4) Keyboard-only pass: every viewport-1 action reachable and visible-focus at full opacity. Run tools/visual-check npm run all + after_project_edit.ps1 -Project 'AstroPrecise' after the wave; SW ?v= bump LAST per the versioning rules.

**Risks:**
- 'Observatory' may wrap or ellipsize in the 390px bottom-tab bar — ship it measured; the sanctioned fallback is a tab-only short label 'Home' (keep 'Observatory' everywhere else), per the IA doc's flagged sign-off item.
- Another agent is actively finishing Model Window work in css/ap-model-window.css and js/ (working tree churn expected) — the plinth-tray grid rows and .ap-mw-chip placement specs here must be re-grepped against the tree at implementation time; anchor on class names, never line numbers.
- The interplay module and Slice-2 camera craft (idle drift, cursor parallax, damper) land in the same camera pipeline — the no-direct-camera-writes rule prevents conflict, but the 10s user-drive lockout and 2.5s cooldown constants should be tuned together once Slice 2 exists.
- explore.html stubbing breaks live #m= deep-link emitters if the receiver is not verified on index first, or if the stub drops location.hash — sequence is receiver-on-index → verify → stub, in separate commits.
- Hover camera moves, even dwell-gated, may still feel busy to the owner (eye comfort is subjective) — build the camera half of the intent table behind a single INTERPLAY_CAMERA=true flag so chips-only mode is a one-line rollback for the S24/desktop eye-check.
- The fixed transparent masthead over a 100svh stage changes the LCP/CLS profile vs the v702/v703 baseline — re-shoot the settled-screenshot baseline and Lighthouse before/after; if the gradient band harms canvas legibility at Earth-rest framing, fall back to the current in-flow masthead with stage = 100svh - nav-h (all other specs survive that fallback).
- Aurora-cyan LIVE dot/tag in ap-model-window.css is under owner review — this design adds no new cyan, but if the owner retires it, the .ap-mw-live color swap should happen in the same wave so the frame chrome ships coherent.

---

## Funnel + Performance

Funnel + performance spec for the model-first homepage: rebuild index.html's hero on the explore.html chassis (full-viewport stage, absolute overlay chrome), with the cast form as an always-visible plinth-tray dock at the stage bottom — 0 interactions from first paint, compressing to a 1-tap pill only during active exploration (scale≥1/journey/flight). LCP stays the existing 92KB earth.webp poster (now preloaded, rendered as a disc not cover-scaled). S24 fill-rate at DPR 1.6 = ~0.73MP, equal to today's already-verified explore stage — inside budget with no engine edits; a new page-side pixel budget via the existing RafCore.hdDPR hook protects hi-DPI desktops (the only genuinely new GPU cost). SEO survives via a visible h1 in the dock + a crawlable below-fold band reusing the v702 daily/path/shop/footer blocks. Ten measurable gates.

### 1. Stage shell — index hero rebuilt on the explore chassis

Replace #heroChapter's in-flow stage with explore.html's composition verbatim: .hero-solar-stage becomes position:relative, height:calc(100svh − var(--ap-masthead-h) − var(--ap-bottomnav-h,0px)), min-height:30rem, overflow:hidden, background:radial void gradient (copy explore-page.css:29-39). Reuse the EXACT wrap markup/ids already shared by both pages (#apAwardOrreryWrap > .ap-award-orrery-frame > #orrery-viewport > #orrery-canvas + #orrery-dom-labels, index.html:358-384) with explore-page.css's inset-0 canvas rules (:42-79) so orrery-loader.js + lite-orrery.js wire unchanged. Measure --ap-masthead-h with explore-boot.js's syncNavHeight pattern (explore-boot.js:263-279), snapshot in px on load + orientationchange only (NOT every resize — svh already absorbs URL-bar; re-measuring per-resize causes canvas re-alloc jank). --ap-bottomnav-h:65px set only when .bottom-nav exists ≤640px (ap-structure-clean.css fixed bar, ap-v699/Wave-A). Keep index's newspaper masthead (critical-path CSS per project memory) — do not swap to explore's .site-header. Earth framing: keep setDefaultEarthFrame scale-0 rest (ap-v701); wrap sizing desktop width:min(52vh,520px), top:41%; mobile width:min(64vw,300px), top:38% — sized so the disc bottom clears the tray (gate 2 verifies). One Three.js instance unchanged; explore.html keeps existing markup as the #m= receiver room.

### 2. Cast dock — desktop composition and placement (THE conversion surface)

Always-visible dock, NOT tap-to-reveal. Argument: the native date input already needs one tap to open its picker, so hiding the form turns a 2-step flow into 3 (+50% steps) and removes the page's only 'what do I do here' affordance; the model converts sightseers only if the ask is co-present with the wow. Composition (one component, .ap-cast-dock): (a) visible h1, 1.0-1.15rem, parchment #ECE6D8: 'Your free birth chart — cast from the real sky' (SEO-bearing, replaces the marketing h1); (b) the EXISTING form verbatim — reuse #hero-chart-form/#hero-birthdate/.hero-form-row/.hero-form-error and the submit handler at index.html:1916-1928 untouched (id contract preserved); (c) one microline 0.7rem: 'Free · private · computed on your device'. Placement: absolute, left:50%, translateX(-50%), bottom:calc(var(--ap-deck-h,96px) + 14px), width:min(560px,92vw), z-index:12 (above deck 10, below nothing), background:rgba(8,8,12,.92), 1px solid rgba(200,205,214,.28), radius 14px, padding .65rem .95rem — reuses the .hero-copy dock skin (index critical CSS :164). Inputs keep 44px min-height (ap-structure-clean.css:163-171). CTA styling: dark ink #0B0906 on brass gradient linear-gradient(160deg,#E8C872,#C9A227) — the Wave-A-verified 8.2:1 pill from explore-page.css:241-246. Do NOT use .btn-press's cyan gradient; ALSO swap the .btn-press definition inside index's inline critical CSS (index.html:99) or the cyan flashes pre-stylesheet (owner has aurora CTAs under review — silver/warm only).

### 3. Cast dock — mobile tray stack (390×844 S24)

Single .ap-stage-tray container: position:absolute, bottom:0, inset-inline:0, z-index:10, display:flex, flex-direction:column, gap:6px, padding:6px 10px calc(10px + env(safe-area-inset-bottom)), background:linear-gradient(to top, rgba(0,0,0,.92) 0%, rgba(0,0,0,.55) 70%, transparent 100%) — the museum plinth band, per owner law art-above/plaque-below. Row order top→bottom: [1] planet pills — reuse the ap-model-window.css compact HUD rules (:287-317: nowrap, overflow-x auto, 44px targets, seals hidden) but drop the negative-margin flow trick (:267) — the tray is absolutely positioned so net flow height is moot; [2] scrub + (date · Now) row (:319-351 rules); [3] CAST ROW LAST — bottom slot is the thumb-primary zone: date input flex:1 min-width:0 + submit flex:0 'Cast my chart', 48px height, brass pill. h1 in the tray shrinks to 0.82rem single line above the cast row (keeps crawlable h1 on mobile without eating budget). Total tray ≤176px. Stage 731px tall (844−48 masthead−65 bottom-nav) leaves ≥555px clear canvas; disc (300px at top 38%) bottom edge ≈ 428px → ≥127px clearance to tray top — gate 2 confirms 0 intersection. Model Window caption plate stays top-left per ap-model-window.css:189-201 (never in the tray).

### 4. Cast dock — visibility state machine (conversion vs exploration)

State A (default, scale 0 Earth rest): full dock visible — 0 interactions to cast. Pointer-drag/scrub at Earth rest DOES NOT hide it (fiddling with Earth is the majority engaged path; hiding the form there hides it from the warmest users). State B (exploring): on orrery-scale-change detail.level ≥1, or journey (aria-pressed on #ap-cosmic-journey), or cosmic-flight overlay open → dock compresses to a single fixed pill '✦ Cast your chart' (48px, brass, bottom-right of tray/dock position, z:12) via a CSS class swap (max-height/opacity transition 240ms; PRM instant). Pill tap → restore State A + focus #hero-birthdate (= 1 interaction, satisfies the gate). Auto-restore: returning to scale 0 (orrery-scale-change level 0) restores the full dock after 600ms — the sightseer who zooms back to Earth re-meets the ask at 0 taps. Implementation: ~40-line listener block in js/ap-home-bootstrap.js (already loaded, index.html:1781); listeners piggyback the same events orrery-loader.js already dispatches — no engine edits.

### 5. Returning-user state

Reuse fillHeroChip (index.html:1855-1908) intact — it already reads ap_charts/ap_natal_pins and composes 'Fri 10 Jul · Jonny, your daily reading is ready ↓'. Two changes: (a) delete the ≥1024px early-return in showHeroTodayChip:1851 so desktop gets it too; (b) re-parent #heroTodayChip into the dock, rendered as a quiet silver chip ABOVE the form row (href stays #today — the daily strip lives below the fold on this page, so the anchor now doubles as the scroll invite). Do NOT replace the form for returning users — a saved chart does not mean they won't cast another (partners/kids are the shop funnel); the form stays primary, the chip is additive. Saved-chart deep link 'See your sky in the model' (explore.html#m=<chart ISO>) is deferred to the Personal Sky slice — not this build.

### 6. Post-submit flow + Personal Sky hook point

Contract unchanged: submit → location.href='chart.html?date='+encodeURIComponent(val) (index.html:1920). The Personal Sky beat (masterplan §2.1a) hooks HERE and ONLY here: flag-gated (localStorage ap_personal_sky_seen), runs only when html.orrery-full && typeof Orrery3D.setDate==='function' && !PRM: setDate(birth noon UT) + caption 'YOUR SKY · <date>', with redirect scheduled unconditionally at ≤3200ms via setTimeout BEFORE the beat starts, and any throw → immediate redirect. Conversion guard is absolute: the beat may never add a failure mode or >3.2s to the funnel; 2D/low-tier/PRM paths redirect instantly as today. Ship the beat as a separate slice AFTER the layout lands and gates pass — not in the same commit.

### 7. Progressive-enhancement form contract (no-JS)

Add action="chart.html" method="get" to #hero-chart-form and rename the input name attribute birthdate→date (id hero-birthdate unchanged — the label for= and all JS reference the id, verified index.html:1912). No-JS submit then lands on chart.html?date=YYYY-MM-DD — the exact URL the JS path produces — instead of today's dead self-GET with ?birthdate=. Keep novalidate + the JS preventDefault path as-is.

### 8. Poster / LCP spec

The poster MUST remain the LCP. Asset: keep img/engine/earth.webp (92,482B, 1024²) — do NOT create a full-viewport cover image (cover-scaling 1024² over 1920×1016 is blurry AND a new multi-hundred-KB asset would blow the LCP gate). Composition: #orrery-lite-poster spans the stage; background stack = (1) CSS radial void gradient (0 bytes), (2) earth.webp as background-image sized min(52vh,520px) desktop / min(64vw,300px) mobile, background-position tuned to the engine's Earth-rest framing (50% / 41% desktop, 50% / 38% mobile) so the orrery-full crossfade has zero positional jump. Update the inline critical rule (index.html:166) with the new size/position — it must stay inline so the poster paints from the first stylesheet-free frame. ADD to <head>: <link rel="preload" as="image" href="img/engine/earth.webp" fetchpriority="high"> — today the image is only discovered when the CSS rule applies; preloading moves the request to the scanner and should net-improve LCP despite the larger paint area. Add <link rel="modulepreload" href="js/orrery-webgl.js?v=NNN"> (explore.html:80 already does; index doesn't) to shorten poster→live without touching LCP priority. Loader ladder untouched: ap-await-webgl inline gate (index.html:191-199) → Earth loader → poster → tier boot.

### 9. Fill-rate budget + DPR clamp (the perf core)

Numbers: S24 390×844 → stage 390×731 CSS; at IS_PHONE DPR clamp 1.6 (orrery-webgl.js:280) = 624×1170 ≈ 0.73MP device px vs today's home hero ~0.11MP (210² × 1.6²) — a 6.5× fill increase, but IDENTICAL to today's explore.html stage which already ships phone-verified (ap-v697) with Pluto+trails off on IS_PHONE (:5934,:5971). Verdict: the existing 1.6 clamp keeps the full-viewport home inside the proven S24 budget — no mobile height cap needed. Desktop is the real new cost: 1080p@DPR1 = 1.95MP (fine); retina 2× laptop = 5.94MP with the bloom composer (heavy on iGPUs); 4K@1.5 scale = 7.8MP (too much). Fix WITHOUT engine edits: orreryDPR() consults RafCore.hdDPR(cap) FIRST (orrery-webgl.js:281 → raf-core.js:35). Amend raf-core.js hdDPR to apply a stage pixel budget: if window.__apStagePixelBudget is set, return max(FLOOR, min(tierClamp, Math.sqrt(budget/(stageW*stageH*real*real))*real)) — concretely: effective = min(existing result, sqrt(BUDGET/cssArea)). Budgets: high tier 6.0e6, mid 3.5e6, IS_PHONE-equivalent (coarse tier) 1.15e6; floors 1.5 desktop / 1.25 phone. Page boot (ap-home-bootstrap.js + explore-boot.js) sets __apStagePixelBudget + stage CSS dims before requesting the engine and after orientationchange, then calls Orrery3D.forceResize(). Check: S24 budget DPR = sqrt(1.15e6/285e3)=2.0 → min(1.6, 2.0)=1.6 — phones unaffected; retina laptop → sqrt(6e6/1.48e6)=2.01 → effectively native 2, 4K-scale clamps to ~1.75. Also add the offscreen pause: IntersectionObserver on #orrery-canvas + document.hidden → engine setSpeed(0)/skip rAF work when the visitor scrolls into the below-fold band (pattern P12 in the WOW plan) — the canvas is now page-permanent so this is what rescues battery + Lighthouse.

### 10. Low-tier / no-WebGL full-viewport composition

Low tier (localPerfTier()==='low', orrery-loader.js:696-706) boots orrery3d.js 2D via tryCanvasFallback (:254) exactly as today — 2D canvas fills the same inset-0 stage (trivial fill cost), opens the Wave-A Earth-forward preset (Earth pinned, zoom 2.4). Deck degradation is already automatic: scale strip + journey stay [hidden] (feature-detect setScaleLevel, :385-389). Model Window frame + LIVE plate NEVER appear (gated on html.orrery-full — honesty law, ap-model-window.css:15-18). Cast dock is pure DOM with zero engine dependency — identical on every tier including no-JS (poster + dock + crawlable band). PRM: both boot scripts already exit (ap-award-orrery.js:9 pattern / explore-boot gate) → static poster disc + dock; add no dock-compress listeners under PRM (State A permanent). The honest fallback caption (explore.html:174-179 pattern) mounts under the tray: 'simplified sky — positions still real, computed from VSOP87'.

### 11. SEO crawlable layer

index.html stays the domain front door: canonical https://astroprecise.app/, existing title/OG/WebSite+WebApplication JSON-LD (index.html:41-71) unchanged. Visible-text plan (NO display:none tricks, nothing aria-hidden that carries copy): (a) h1 lives in the cast dock (element 2/3) — real rendered text on every tier; (b) below the stage, an in-flow crawlable band reusing the v702 blocks VERBATIM: 120-180-word intro paragraph (new — plain description of what the site computes, VSOP87/private/free, warm parchment on void, quiet typography), then #dailyChapter (index.html:1000-1015 as-is, the #today anchor target), the 5-link Path block, #shopChapter, FAQ as collapsed <details> (salvaged from index-full.html:923-944 + FAQPage JSON-LD :126-153 per IA plan I3 — <details> content is crawlable, unlike display:none), footer. Gate: ≥250 visible words. Duplicate-content vs explore.html: keep both indexed — index differentiates via the crawlable band + cast intent; explore.html keeps its Observatory title/canonical and stays the sitewide #m= receiver. Do not port explore's WebApplication schema onto index (it already has its own).

### 12. Deck + overlay chrome layout (disc law compliance)

Z-order (explore convention, ap-model-window.css:166): canvas wrap 1, Model Window 6, legend 8, deck/tray 10, cast dock 12. Desktop: deck = explore's bottom strip (explore-page.css:190-202) with home policy from ap-structure-clean (scale STEPPER not the 7-button track, per :190-195 — the full track returns in the Scale Ladder slice); cast dock floats above it (element 2). Honest-model legend: mount explore's collapsible legend (explore.html:182-199) top-left, starts collapsed (explore-boot.js:281-294 behavior) — honesty labels survive on the homepage, satisfying the binding constraint. Model Window plate: top-center desktop / top-left mobile (existing rules). NOTHING absolute-positioned may sit inside the disc circle: disc = inscribed circle of #apAwardOrreryWrap rect; every overlay rect (dock, tray, deck, legend collapsed header, return links, plate, live pill) must have 0 intersection at 1440×900 AND 390×844 — this is owner law #1 and gate 2. The masthead contents-nav keeps Explore/Chart/Sky/Daily/Shop links (nav built around the model = stage is the hero, nav stays conventional chrome).

### 13. Measurable gates (pass/fail, run in tools/visual-check with the ≥1400ms settled-screenshot rule)

G1 CAST-0: settled first-paint screenshot at 1440×900 + 390×844 → #hero-birthdate AND its submit button rects fully inside viewport, no scroll, no tap. G2 CAST-1: drive setScaleLevel(5); assert compress-pill visible; 1 click → input focused (document.activeElement===hero-birthdate). G3 DISC: inscribed circle of #apAwardOrreryWrap vs getBoundingClientRect of every [data-stage-chrome] element → zero intersection, both breakpoints (verify flagged voids with getBoundingClientRect per the re-score method — no screenshot-only judgments). G4 LCP: Lighthouse mobile+desktop → LCP element is the poster; LCP ≤ v704 baseline × 1.10. G5 FILL: canvas.width*canvas.height ≤ 1.15e6 at 390×844 (expect ~0.73e6) and ≤ 6.0e6 at 1920×1080@DPR2 emulation. G6 BODYH: desktop ≤5500px, mobile ≤7000px (structure-clean gates stand). G7 SEO: Playwright innerText word-count of main+footer excluding the stage ≥250; h1 present and visible. G8 HONESTY: html.ap-model-revealed absent until orrery-full; legend reachable ≤1 tap; 2D path shows fallback caption. G9 zero console errors, npm test green, scrollWidth ≤390 at 390 (Wave-A regression guard). G10 ship hygiene: ?v= bumps + SW precache regen + sw.js V bump LAST (stale-gzip rule), then visual-check npm run all, after_project_edit.ps1 -Project 'AstroPrecise', signed AGENT-HANDOFF line.

**Risks:**
- Working-tree churn: another agent is actively finishing Model Window work in the same files (explore-boot.js, ap-model-window.css, index.html are all in the Wave-A changed-files list awaiting a version-integrity pass). All line numbers cited here WILL drift — implementers must re-grep anchors (ids/classes/API names), never offsets, and coordinate the ?v=/SW bump with the versioning agent so it still lands LAST.
- Conversion regression is the existential risk: today the form is in-flow and unconditionally visible; any bug in the compress/restore state machine (element 4) that leaves the dock hidden at Earth rest silently kills the business. Gate G1/G2 must run on every tier path (webgl, 2D fallback, PRM, no-JS static) — the dock must be provably engine-independent.
- Desktop hi-DPI softness: the 6.0MP budget clamps 4K/scaled displays below native DPR (~1.75) — Earth's limb may look slightly softer on those screens than today's small 2.5×-DPR hero disc. Owner eye-check on a hi-DPI monitor before accepting the budget number; the floor/budget constants are tunable in one place (raf-core.js).
- Battery/thermals: a permanently-visible full-viewport WebGL canvas on the front door is a new sustained cost even inside the fill budget; the offscreen/hidden rAF pause (element 9) is load-bearing, not optional — without it Lighthouse perf and S24 battery both regress.
- Duplicate-content: index and explore become visually near-identical full-model pages; the crawlable band + distinct titles mitigate, but watch Search Console for cannibalisation and be ready to differentiate explore's meta description further.
- iOS Safari date input renders as an inline wheel trigger with its own height quirks inside absolutely-positioned trays; test the tray on iOS (not just the S24) — a broken picker in the bottom band is a hard conversion blocker.
- The svh/masthead measurement snapshot (element 1) assumes the masthead height is stable after fonts load; a late webfont swap can shift it a few px — re-measure once on window load (explore-boot already does this at :277) or the stage can under/overflow by the delta.
- Personal Sky post-submit beat (element 6) delays the redirect up to 3.2s for first-timers; even flag-gated and error-proofed, ship it as a separate slice with its own funnel measurement so any drop-off is attributable and reversible.