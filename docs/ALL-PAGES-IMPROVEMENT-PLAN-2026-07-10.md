# All-Pages Improvement Plan — Expert Sweep
**Date:** 2026-07-10 · **Author:** Claude (6-reviewer expert sweep, ~1.12M tokens, every page scored under the model-as-masterpiece rubric: model-first, content, honesty, conversion, SEO, mobile 390px, eye comfort)
**Companions:** `ORBITLAB-INTEGRATION-MASTERPLAN-2026-07-09.md` (governance) · `MODEL-MASTERPIECE-SPEC-2026-07-10.md` (the four-layer package this plan mounts pages onto).

## Executive synthesis

~40 pages scored. Strongest: why.html 8.5 · chart/explore/moment/compatibility/accuracy 8. Weakest: auth-redirect & index-classic 2 · sample-reading/transits/mysky/links/guides/offline 5. **45 high-impact fixes** across six groups.

### Wave plan (status 2026-07-10)
- **WAVE 0 — honesty hotfixes: ✅ SHIPPED** commit `3a796f4` (live retro calendar, verified eclipse cards, live Plate I markers, shop delivery claims + unified live-predicates, lifepath reframing, llms.txt, chart claim reconciliation, catalogue fonts.css). Chiron fix shipped separately (`867b4ae`).
- **WAVE 1 — conversion quick wins (S-effort):** sample-reading close-for-prospect + CTA + 390px overflow · chart-view "cast your own" CTA · charts.html reading-path strip · weekly-sky body CTA · moonphase→Moment bridge · quiz feeder link · fulfil-redirect Typeform whitelist · links.html dead #lib-paid block.
- **WAVE 2 — model-connection mounts (needs deep-link receiver + Model Window):** #m= receiver on explore/index · deep links from daily-transit, weekly-sky rows, horoscope dial, sign-page heroes (generator!), moment reveal, moonphase card, compatibility Davison tab, lifepath birth-sky, accuracy/why "see the model" mounts · horoscope reading-panel model surface.
- **WAVE 3 — content rebuilds (M/L):** guides crawlable bodies · cosmic-story crawlable copy + shown sky · angel-numbers static render + inbound links · sample-reading dedupe · sign-page magazine pass (via generator, AFTER drift back-port).
- **WAVE 4 — retirements (Jonny sign-off needed):** auth-redirect retire · index-classic → redirect stub · index-full: salvage FAQ+schema onto index.html then retire-or-merge · index-lite keep as gold-pattern stub · index-ephemeris optional · mysky: fix personalization or merge into profile.
- **J3-DAY CHECKLIST (commerce):** static featured-card CTA flip machinery · app.js AP_MON legacy 'Instant PDF' config strings (~lines 1543-1630) · moment-pack detailsForm · end-to-end preview test with one real fulfilUrl before pasting all.

### Standing constraints
Sign pages ONLY via tools/generate-sign-pages.mjs and ONLY after back-porting live-page drift (engine thumbs, Freeze-a-Moment CTA, sign-instrument-note — clobber risk verified). One Three instance per page. Checkout stays honest while dormant. SW precache via generator only.

Per-page findings follow (verbatim from the six reviewers).

---

## Core Funnel (cast → reading → paid)

The core funnel has a genuinely strong engine: chart.html is form-first, honest, and carries the whole paid path (what's-next spine, deep-reading teaser built from real interpretation snippets, dormant-safe CTAs, wallpaper email gate). The leaks are at the two ends — the proof artifact (sample-reading.html) ends with buyer-facing copy, has zero conversion links, repeats identical paragraphs 3-4x, and physically overflows a 390px phone; and the warm-return surfaces (charts.html, chart-view.html) never feed the reading loop. A handful of honesty near-misses (static Plate I markers under a "computed in your browser" caption, $-prices in a £ shop, "~1 arcminute" vs "a few arcminutes" on the same page) are all cheap fixes.

### index.html — score 7/10
*Role:* Home chapter flow: convert model wonder into a first chart cast, then route the spine (Cast → Sky → Keep → Daily → Reading).
1. **Plate I markers are static under a 'computed' caption** (high/S) — The Sky Tonight plate's sun marker (class zc-sun-mark, hardcoded in Cancer, index.html ~line 1197) and moon marker (zc-moon-mk, Scorpio, ~1202) never move, but the figcaption directly beneath reads 'Positions computed in your browser · VSOP87' and the chapter headline is 'Tonight, the planets stand where we say they do.' The inline hydrate() at index.html:1950-1985 already computes live sun/moon longitudes for the cs-chips and the caption epithet — either rotate the two marker groups from that same pos object, or change the caption to 'Schematic instrument — live positions in the chips above.' Binding honesty rule; sun drifts a sign per month.
2. **Label the method pull-quote as a sample reading** (medium/S) — The blockquote 'Your Sun in Cancer in the 10th house…' (index.html:1342-1347, #methodChapter) has no attribution and reads like a real customer's reading/testimonial. Add a figcaption '— from a sample Cosmic Story' next to the existing 'Read a cosmic story →' link.
3. **Deduplicate the two identical trust strips and shorten the scroll** (medium/M) — #compareChapter (lines 1295-1308) and #methodChapter (1349-1362) both end in near-identical ap-mini-trust strips repeating the same VSOP87/arcminute/privacy claims, and both end in a 'cast your chart' CTA — 11 chapters below the hero is a long 390px scroll before the shop chapter. Keep one strip (method's, which adds the DevTools line) and tighten or merge the compare chapter's tail.
4. **Feed the orphaned quiz from the flow** (medium/S) — quiz.html has zero contextual inbound links (grep across *.html: only the nav-drawer NAV_EXTRAS entry in js/ap-nav-model.js:59). Add one quiet feeder line in the chapter flow, e.g. under #compareChapter's spine bridge: 'Not sure where to start? Take the 90-second archetype quiz →'.

### chart.html — score 8/10
*Role:* The funnel engine: cast a free chart, then earn the wallpaper email, the save, and the Deep Reading teaser — form-first and honest throughout.
1. **Remove or retarget the dead 'Open in App' action** (medium/S) — js/chart-page.js:1876-1878 sends users to index.html#app-download, but no #app-download anchor exists in any HTML file (verified by grep) — a mid-funnel result action that ejects a freshly-cast user to the top of the homepage. Remove the #app-btn from the More menu (chart.html lines 536-539) or point it at a real destination.
2. **Reconcile the accuracy claim within the page** (medium/S) — The FAQ JSON-LD (chart.html:65) says 'typically within a few arcminutes' while the badge and educational card (lines 855-861) say 'Arcminute-accurate · ~1 arcminute'. Pick the figure accuracy.html defends and use it in both places — fake-precision drift on the page that most invites verification.
3. **Calm the result-actions row so the reading leads** (medium/M) — Five actions render at the top of results (Download masterpiece, Free wallpaper, Save, Share link, More — chart.html:520-540) before the visitor has read one interpreted word, while the Deep Reading teaser that monetises sits far below. Demote Share into the More menu and add a one-tap 'Read your chart ↓' link in the result header pointing at the Overview tab.
4. **Rewrite the generic educational section and mount the model there** (medium/M) — The 'What Is a Natal Chart?' cards (chart.html:816-864 — 'Mercury governs communication, Venus love…') are the only template-grade copy on the page and have no model connection. This is the natural Model Window mount point on chart.html: an engine still with copy tying the calculator to the same engine as the hero orrery, plus a link to accuracy.html.

### chart-view.html — score 6/10
*Role:* Noindex share-link landing — a recipient's first touch with the brand via someone else's chart.
1. **Add the missing 'cast your own' CTA** (high/S) — The only button ('Explore the full chart →', line 66) opens the SENDER's chart in the calculator. The recipient's natural next step — casting their own chart — has no path. Add a second button 'Cast your own free chart' linking chart.html with no params; this page is the funnel's only viral acquisition surface.
2. **Give the shared wheel a model surface** (medium/M) — The chart renders on a bare stage (.chart-view-stage, line 57) with no model connection despite loading ap-observatory-enchanted.css. Mount the specced Model Window frame or an engine still behind the wheel — this is a first impression page for new visitors and currently the least model-first page in the funnel.
3. **Defer the blocking engine scripts** (low/S) — ephemeris.js and chart-render.js load synchronously (lines 81-82) before the inline controller, which already polls for window.AstroEphemeris with a retry loop (line 122-127). Add defer to both — the retry makes it safe — and fix theme-color #0A0E1A (line 16) to the sitewide #07070A while in there.

### charts.html — score 6.5/10
*Role:* Retention dashboard for saved charts — the warmest returning audience in the funnel.
1. **Give saved-chart owners a path to the reading** (high/M) — js/charts-dashboard.js render() (lines 188-226) shows cards with View/Rename/Delete/Set-active and nothing else — users with saved charts are the best Deep Reading prospects and see no mention of transits, sample, or shop. Append one honest strip after the grid when charts.length > 0: sample-reading.html link + shop.html#deep-reading, dormant-safe in the same pattern as chart-page.js renderDeepTeaser (no buy button).
2. **Drop ephemeris.js from this page** (medium/S) — charts.html loads js/ephemeris.js synchronously (line 241) but charts-dashboard.js only reads saved records via AstroProfile — the engine is never invoked on this page (View links defer computation to chart.html). Removing it (or defer-loading the whole stack: cosmos/effects are also synchronous, lines 238-245) is a large mobile payload win.
3. **Make the 'Today's chart' loop tangible** (medium/S) — The active-chart badge (cardHtml, charts-dashboard.js:125-127) says it powers the daily transit reading but links nowhere. Make the badge or an extra card action link to horoscope.html / transits.html so the dashboard actually drives the daily habit it advertises.
4. **Surface the dashboard after save** (medium/S) — chart.html's save flow only mentions My Charts in toast text ('view it in My Charts or your Profile', chart-page.js:1798) and the page itself is buried in the More drawer (ap-nav-model.js NAV_MORE_EXPLORE). After a successful save, render an inline link chip 'Saved — open My Charts →' near the Save button instead of toast-only.

### sample-reading.html — score 5/10
*Role:* The proof artifact — a full 13-page facsimile that must earn the £12 Deep Reading purchase.
1. **Close for the prospect, not the buyer, and add the missing CTA** (high/S) — Page XII ends 'Questions about your reading? Reply to your delivery email' (line 203) — sample readers have no delivery email — and the entire document contains zero links (chart.html/transits.html appear only as bold text). Add a screen-only final band (display:none in print): 'This is Aurora Vale's sky — yours reads differently. Cast your free chart → chart.html · About the Deep Reading → shop.html#deep-reading'. Links to info pages respect the no-fake-buy-buttons rule. NOTE: the ap-paid-meta comment (line 55, generated 2026-07-04) suggests this file is generated — locate the generator (export-suite/tools) and fix there, like the sign pages.
2. **Fix the 210mm fixed-width overflow on phones** (high/M) — .page{width:210mm;min-height:297mm;padding:26mm 24mm} (line 13) with no @media screen fallback overflows a 390px viewport ~2x — the artifact meant to sell the reading is horizontally-scrolling and unreadable on the Galaxy S24. Add a screen media query (.page{width:auto;min-height:0;padding:8vw 6vw} plus responsive .big3/.foot) while keeping @page print rules intact.
3. **Deduplicate verbatim interpretation paragraphs** (high/L) — The Saturn-in-Capricorn paragraph appears verbatim 4x (pages V, VI, VII, X), Mercury-in-Gemini 3x (II, III, IX), Venus-in-Taurus 3x (IV, V, IX) — a reader evaluating whether 13 pages justify £12 will notice the padding immediately. In the generator, track used interpretation keys: first use gets full text, later uses get a one-line callback ('as the luminaries chapter showed…').
4. **Fix the a/an grammar slip in the generator** (low/S) — 'You are a Air-dominant chart' (line 81) — the element word is interpolated; add an a/an rule keyed on vowel-initial elements (Air, Earth). Small, but it sits on page one of the flagship sample.

### quiz.html — score 6.5/10
*Role:* Playful top-of-funnel doorway (social/SEO entry) that honestly routes every result to the real chart.
1. **Fix the dollar-sign price in a £ shop** (medium/S) — js/quiz.js:336-337 renders product prices as '$' + product.price.toFixed(2) while every price on the site is GBP (£12/£6/£8, and chart.html JSON-LD declares priceCurrency GBP). Render £ or read a currency symbol from AP_MON — a wrong currency on a product row is a fabricated-price honesty issue.
2. **Don't promise wallpapers the dormant path can't deliver** (medium/S) — The result-card email label says 'Want your archetype + 3 cosmic wallpapers? Drop your email.' (quiz.js:358) but in dormant mode the address is saved to localStorage only and nothing is ever sent — an undeliverable promise. Reword to match the note ('be first to know when readings open') or actually generate the wallpapers client-side like chart.html's exportShareImage does.
3. **Add inbound links — the page is an orphan** (medium/S) — No page on the site links to quiz.html except the More-tools nav drawer (ap-nav-model.js NAV_EXTRAS:59). Add feeders from index.html's chapter flow and from the sign-page template (edit tools/generate-sign-pages.mjs, never the 12 pages).
4. **Fix the water-worded option that scores air** (low/S) — quiz.js:171 — option copy 'I adapt instantly — I'm water that finds the gap' grants el:'air'. Users who pick the water metaphor get air points. Reword to an air image ('the breeze that finds the gap') to keep answer→archetype mapping intuitive.

### fulfil-redirect.html — score 6/10
*Role:* Post-purchase utility bridge: forwards buyers (with order params in the URL fragment) to the Typeform birth-details form.
1. **Whitelist the Typeform form ID** (high/S) — The script (line 63) redirects to https://form.typeform.com/to/<any form param> unvalidated — anyone can craft astroprecise.app/fulfil-redirect.html?form=XXXX links that bounce victims to a hostile look-alike Typeform under AstroPrecise's domain trust, harvesting birth data. Validate the form param against the known form ID(s) used by js/shop-commerce.js / AP_MON and show the existing 'Missing form reference' fallback otherwise.
2. **Add a visible manual fallback before/without JS** (medium/S) — If JS is disabled or location.replace is blocked, the buyer sits on 'Redirecting…' forever. Render a 'Continue to your birth-details form →' anchor as part of the same script before calling location.replace, and add a <noscript> block with a shop.html return link so a paying customer is never stranded.

**Cross-cutting themes:**
- The paid path exists only on chart.html post-cast: the proof artifact (sample-reading) and both warm-return surfaces (charts.html, chart-view.html) contain zero routes to the Deep Reading — one honest, dormant-safe strip on each closes the spine.
- Honesty near-misses cluster around static decoration under computed captions: index Plate I's hardcoded sun/moon markers beneath 'Positions computed in your browser', the unlabelled method pull-quote, quiz's undeliverable wallpaper promise. The engine is honest; the framing occasionally over-claims.
- No single source of truth for claims and prices: '~1 arcminute' vs 'a few arcminutes' inside chart.html alone, '$' prices in quiz.js vs '£' sitewide. A tiny shared constants module (or AP_MON fields) for accuracy string + currency would end the drift.
- Script-loading discipline is inconsistent across the group: chart.html carefully defers everything, while charts.html, quiz.html and chart-view.html load ephemeris.js/cosmos.js/effects.js synchronously — charts.html doesn't even use the engine.
- Generated-artifact rule extends beyond sign pages: sample-reading.html carries an ap-paid-meta generation stamp — its fixes (CTA band, mobile CSS, dedup, a/an) belong in whatever generator produced it, or they'll be overwritten on the next regeneration.
**Risks:**
- Other agents are actively editing js/ — chart-page.js, quiz.js and ap-nav-model.js line numbers cited here may drift; re-grep for the quoted tokens (e.g. 'app-download', 'toFixed(2)', 'zc-sun-mark') before implementing.
- sample-reading.html appears generated (ap-paid-meta comment, generated 2026-07-04T09:49Z); editing the HTML directly risks being clobbered — find and fix the generating script first, same constraint as the 12 sign pages.
- The fulfil-redirect whitelist must be maintained in lockstep with shop-commerce.js form IDs, or real buyers hit the 'Missing form reference' fallback at the worst possible moment (immediately post-payment).
- Checkout is dormant: every recommended reading CTA must remain a link to sample/shop info — no buy buttons — and any added strips on charts.html/chart-view.html should reuse the AP_MON dormant-safe pattern already proven in chart-page.js.
- chart.html deep-teaser opens sample-reading.html in a new tab; until the sample's mobile overflow and buyer-voiced ending are fixed, driving MORE traffic to it (per the charts.html/chart-view.html recommendations) could depress rather than lift conversion — sequence the sample fixes first.

---

## Model & Instrument Wing

The model/instrument wing is the strongest-principled part of the site — explore.html, moment.html, moonphase.html and this-weeks-sky.html are honest to a fault, and ephemeris.html is a genuinely rich instrument with clean tonight-first IA. But the wing does not yet behave like rooms of one observatory: only explore.html actually touches the model, nothing links to model moments (the #m= deep-link receiver doesn't exist yet in explore-boot.js), naming collides ("The Sky Tonight" vs tonight.html vs This Week's Sky vs My Sky), and footers/chrome differ per room. The one serious rot is transits.html, where a hardcoded "Retrograde Calendar 2025–2026" and an "Upcoming Eclipses" section are now entirely in the past (today is 2026-07-10) while presented as forward-looking guidance — a direct honesty violation on a precision-branded page.

### explore.html — score 8/10
*Role:* The flagship room — full-viewport 3D model (Earth→Cosmos), the destination every other page's model links should point at.
1. **Implement the #m=<ISO>&focus=<body> deep-link receiver** (high/M) — explore-boot.js (C:\Users\jonny\OneDrive\astroprecise\website\js\explore-boot.js) never parses location.hash. Parse #m=/focus= on boot and hand time to the lite/WebGL engine (lite-orrery time-row + __requestFullOrrery path) and focus to the data-lite-planet pill logic. Every recommended model link in this wing (weekly events, Moment reveal, moonphase, transit ticker) is blocked on this landing first.
2. **Fix the stale duplicate orrery-visual.css injection** (medium/S) — explore.html head loads css/orrery-visual.css?v=680 (no id); explore-boot.js line ~104 injects css/orrery-visual.css?v=567 guarded by id 'ap-orrery-visual-css', which never matches the head link — so an OLDER copy is appended after the newer one and wins the cascade at equal specificity (and can serve stale gzip per the ?v rule). Either give the head link that id or drop the injectCss call; also align lite-orrery.js?v=578 with the page's v=684 set.
3. **Give the exit nav one primary funnel step** (medium/S) — The .explore-return pill row (explore.html lines 243–247) has three equal-weight links (Home / Tonight's sky / Your birth chart). Style 'Your birth chart' as the primary pill (brass border/fill) and reword to 'Cast this sky into your chart' so the room has ONE clear next step toward chart→reading.
4. **Rename the 'Tonight's sky' return link** (low/S) — explore-return labels ephemeris.html as 'Tonight's sky' while a separate tonight.html ('What's visible tonight') exists — two rooms sharing a name. Label it 'The Sky' (matching nav) to keep the observatory map coherent.

### mysky.html — score 5/10
*Role:* Hub/dashboard for the free spine (Explore→Cast→Sky→Keep→Daily→Shop) — currently a static links page.
**RETIRE CANDIDATE:** Conditional: if the personalization fix below isn't wanted, merge into profile.html/charts.html and point the nav 'Hub' badge there — as a static link list it duplicates the nav drawer and under-delivers its own title.
1. **Make 'My Sky' actually show something of mine** (high/L) — Title promises 'Your Cosmic Dashboard' but the page reads no user state. Read AstroProfile.getCharts() / saved Moments from localStorage (the pattern moment-page.js loadSavedChartPlace uses) and surface saved chart big-three + last Moment card at the top; fall back to the current spine cards for first-timers. This is the gap between the page's name and its content.
2. **Rewrite the dev-voice copy in the living-sky card** (medium/S) — Visible copy says 'engine stills and the cinema system plate. Interactive photoreal WebGL lives on Home and Explore (one Three.js stack, no conflicts)' and 'Why no second WebGL here: the homepage already owns the flagship engine' (lines 91–94, 141–143). That's architecture notes leaking to users — the HTML comment block at line 38 already holds the real rule. Replace with warm user language ('These frames are captured from the same living model you can fly through on Explore').
3. **Fix the mistyped <source type="image/webp"> pointing at a .jpg** (low/S) — Line 97: <source type="image/webp" srcset="img/marketing-system-cinema-silver.jpg"> — the type attribute lies about the format. Point it at a real .webp derivative or delete the <source> and keep the plain <img>.
4. **Deep-link the six engine stills to model moments** (medium/S) — Once explore-boot.js accepts #focus=, wrap each .mysky-engine-grid figure (earth/saturn/jupiter/mars/sun/earth-moon) in a link to explore.html#focus=<body> so the stills become doors into the model rather than decoration.

### moment.html — score 8/10
*Role:* Free keepsake tool (freeze a date's sky → free card → Moment Pack upsell) — the wing's cleanest funnel page.
1. **Add 'Open this night in the 3D model' to the reveal** (high/S) — renderReveal() in js/moment-page.js has moment.utc in hand; add a ghost button in the .mom-keep row linking to explore.html#m=<moment.utc ISO> (behind the deep-link receiver). A frozen Moment is the single most natural model-moment deep link on the site.
2. **Label the lifestyle teaser image as a render** (medium/S) — The gallery figure (moment.html lines 147–153) alt text says 'example lifestyle render' but the visible figcaption doesn't — under the honesty law decorative/mocked visuals should be labelled where users read. Add 'example render' to the figcaption ('Free card uses the same void-and-silver + engine plate language').
3. **Give the hero sub one more beat of warmth** (low/S) — 'Freeze any night that mattered — birth, first date, wedding, memorial. Free private card on your device.' — the second sentence reads clipped/telegraphic next to the site's quietly-poetic voice. One line like 'A free card, made privately on your device — the real star that stood overhead.' keeps form-first while sounding like the rest of the room.

### this-weeks-sky.html — score 6/10
*Role:* Weekly sky calendar (phases, ingresses, stations, exact aspects) computed live — honest almanac room. (Reviewed as the group's 'sky.html'; no bare sky.html exists.)
1. **Add a body CTA — the page is a dead end** (high/S) — Between the honesty panel (line ~154) and the footer there is no next step at all. Add one closer after .wk-honesty: 'See how this week lands on your chart → transits.html' (primary) with a quiet 'Cast your chart' secondary — the calendar earns trust it never spends.
2. **Deep-link each event row into the model** (high/M) — In the inline controller's render loop (row.innerHTML around line 367), every event has ev.tDate and a planet — append a small '→ in the model' link to explore.html#m=<ISO>&focus=<planet> per row (behind the receiver). This is the wing's richest source of model moments and currently its most model-less page.
3. **Cross-link the Moon strip to Moon Phase** (medium/S) — The 'Moon This Week' section (#wk-moonstrip) never mentions moonphase.html. One footer line under the strip — 'What Moon were you born under? → moonphase.html' — turns rhythm-watchers into tool users.
4. **Give the room one model surface** (medium/M) — The page is pure text on starfield — the only room with zero engine imagery. Mount a single small Model Window frame or engine still beside the hero (img/engine/ set), labelled as an engine capture, consistent with mysky/moment.

### moonphase.html — score 7/10
*Role:* Shareable birthday-Moon tool + honest two-Moon resonance (no fake %) — SEO/social entry point.
1. **Bridge the result card to Moment** (high/S) — renderSingle() in js/moonphase.js ends with only a 'Copy shareable text' action. Add 'Freeze this night as a keepsake → moment.html' beside it (prefill via query param or just link) — the user has already given a meaningful date; today that intent dies on the page.
2. **Put an engine Moon where a model surface belongs** (medium/M) — The result is a generic (if correct) SVG disc. img/engine/moon.webp exists — use it as the textured base of the result card (phase mask drawn over the engine still, captioned 'engine texture · phase from real elongation'), or at minimum mount it beside the educational cycle section. Weak/generic imagery where a model surface belongs is exactly the brief's finding.
3. **Tokenise the retired-palette hexes in moonSVG** (low/S) — js/moonphase.js hardcodes rgba(201,162,39,0.10) halo and var(--gold-text,#D9BC5C) fallback (old warm-gold set) plus #fbf7ec lit stops — swap to --ap-* tokens per DESIGN.md so the wing survives palette work without forking.

### transits.html — score 5/10
*Role:* Live positions + personal transit forecast — the personalisation on-ramp; live parts are honest, static parts have rotted.
1. **HIGH honesty: 'Upcoming Eclipses' are all in the past** (high/M) — Section #eclipses-heading (lines 383–419) lists Mar 14/29 2025, Sep 7/21 2025, Feb 17 2026 — every one before today (2026-07-10) — under the heading 'Upcoming Eclipses' with copy urging preparation. Curate/compute a current list or retitle with honest dating; as-is it's invented urgency about events that already happened.
2. **HIGH honesty: retrograde calendar is entirely expired** (high/M) — #retrograde-calendar 'Retrograde Calendar 2025–2026' (lines 317–378): all six hardcoded windows end by Mar 10 2026, yet the intro says 'Plan major decisions, launches, and commitments around these reversal windows.' weekly-sky.js already finds stations by bisection — scan ±12 months live (with the ≈ approximation labels it uses), or prune past windows and link retrograde.html. Stale hand-written data under a precision brand is the wing's worst trust leak.
3. **Mount a model link on the live ticker** (medium/M) — The page has zero model connection despite being about the sky's motion. Make each .transit-pill (or the planet cards in #current-sky) link to explore.html#focus=<planet> once the receiver lands — the live positions are literally the model's data.
4. **Voice pass on the static astrology copy** (medium/S) — 'Eclipse seasons mark the most powerful turning points in astrology — fated beginnings' and similar lines read as woo hype against the site's 'precise, honest, quietly poetic' register (contrast the excellent no-contact copy in the forecast controller). Rewrite the eclipse/retro card texts in the honest-symbolic frame the rest of the page uses.

### ephemeris.html — score 7/10
*Role:* 'The Instrument' — tonight-first sky room with personalised deep layers (light-cone, zenith, echoes, daimon) and advanced folds; dense but coherent.
1. **Retitle + add FAQPage JSON-LD** (high/S) — Title 'The Sky | Astro Precise' wastes the richest page in the wing. Use 'Tonight's Sky — Live Moon, Planets & Sky Mood | Astro Precise' and add FAQPage JSON-LD (planetary hours, sky-mood composite, tropical vs sidereal, feed honesty) — the answers already exist as body copy in the folds; this-weeks-sky.html shows the house pattern.
2. **Extract the inline clock-of-spheres script and tokenise it** (medium/M) — The ~140-line planetary-hours IIFE lives inline in ephemeris.html (lines 615–757) with hardcoded hexes (#A8B0BC, #A8841E, #E8C872, #A89E88, #E8E0D0) that bypass ap-palette tokens. Move it to js/ (it's a signature instrument, not page glue) and paint from --ap-* vars so palette work doesn't fork this page. Same for the widespread style="" attributes in the deep-layer sections.
3. **Fix the 'Align with Orrery' dead end** (medium/S) — The #ph-align handler tells users 'visit the homepage and move the time scrubber first' when astro_current_demo_time is absent. Fall back to aligning with now (which it already renders) and say 'aligned to this moment — scrub time on the home orrery or Explore to bring a different sky here', linking explore.html. Never send a user away to configure a button.
4. **Add the model-moment mount in the closer** (medium/S) — The closer (#sky-closer-heading) hands to chart + Deep Reading — right funnel, but this sky page never shows the model. Add a third quiet link 'See tonight in the 3D model → explore.html#m=now' (or an engine still in a Model Window frame above the closer) so the instrument room visibly belongs to the same observatory.
5. **Disambiguate 'The Sky Tonight' vs tonight.html** (medium/S) — This page's h1 is 'The Sky Tonight' while tonight.html is 'Tonight's Sky' in nav extras and is linked here as 'What's visible tonight'. Pick one naming rule (this page = the instrument/reading; tonight.html = visibility planner) and apply it to the h1, the sky-tools-row label, and nav labels together.

**Cross-cutting themes:**
- Model-moment deep links have no receiver: explore-boot.js doesn't parse #m=<ISO>&focus=<body>. Land that one change first — five of the seven pages then gain cheap, safe model links (stills + deep links, no second Three.js anywhere).
- Naming collision across the wing: 'Sky' (ephemeris nav label), 'The Sky Tonight' (ephemeris h1), 'Tonight's Sky' (tonight.html), 'This Week's Sky', 'My Sky' — and explore.html labels ephemeris 'Tonight's sky'. One taxonomy pass across h1s, nav labels and cross-links is needed for the rooms-of-one-observatory feel.
- Footer chrome splits the wing: explore.html has a bespoke model footer, mysky/moment have minimal inline-styled footers, weekly/moonphase/transits/ephemeris share the big 4-column footer. Pick one (the data-ap-footer-model + ap-footer-inject.js pattern) so every room ends the same way.
- Compute-don't-curate: transits.html's hand-written retro/eclipse calendars rotted while the wing's own weekly-sky.js computes stations/ingresses honestly. Rule for this wing: any dated list must come from the engine (with ≈ labels) or carry a visible as-of date.
- Non-token hexes keep leaking into page-inline SVG/JS (ephemeris wheel, moonphase.js SVG, explore-page.css fallbacks like #7EC8E8) — a token sweep across the wing protects Jonny's eye-comfort palette work from forking.
- Honesty is the wing's differentiator and mostly superb (explore's 'honest model' legend, weekly honesty panel, moonphase's no-fake-% FAQ, fieldweather feed labels) — the transit calendar staleness is the only real breach; fix it before anything cosmetic.
**Risks:**
- The group listed 'sky.html', which does not exist in website/; I reviewed this-weeks-sky.html as the evident referent (nav label "This Week's Sky"). tonight.html (611 lines, same wing) was NOT reviewed — if 'sky.html' meant tonight.html, that page still needs a pass.
- Other agents are editing js/ right now: the explore-boot.js version-skew finding (?v=567/578 vs ?v=684) and any controller line numbers may already be moving — re-verify before implementing.
- Fixing transits.html by computing retrogrades/eclipses live must not introduce fake precision: outer-planet station times ride truncated series — carry the ≈ labels exactly as weekly-sky.js/this-weeks-sky.html do, and eclipse computation is NOT currently in the engine (curate with sources or scope the math properly).
- Any new links/assets added to these pages require the SW precache regeneration (tools/generate-sw-precache.mjs) and the ?v= bump LAST after all edits (stale-gzip gotcha), plus after_project_edit.ps1 for launch readiness.
- moonphase.html and transits.html load ap-ai-assistant/ap-daily-bridge panels ([hidden] divs) I did not audit — recommendations here don't cover that surface.

---

## Horoscope + 12 Sign Pages

The horoscope-signs group is the site's healthiest content engine: the 12 generated sign pages carry genuinely strong, layered, honest copy (character -> astronomy -> in-your-chart -> chart bridge) anchored on photoreal engine stills with honest captions, and horoscope.html is a real transit-computed daily hub with exemplary honesty patterns (methodNote, computed-not-canned placeholders, no fake retrograde dates). The single biggest problem is systemic: the generator (tools/generate-sign-pages.mjs + footer-model.mjs) has fallen ~6 changes behind its own output — I regenerated into a scratchpad and diffed: all 12 live pages carry post-generation edits (color-scheme/white-flash guard, silver palette hexes, hero CTA swap to "Cast free chart", instrument note, engine-still thumb grid, "Around the model" footer) that a single innocent regeneration would silently revert. Model-first is half-landed — engine stills everywhere, but not one deep link into the live 3D model from the entire group — and the daily content bank that feeds every sign page's "today" section expires 2026-09-11, after which the pages degrade to generic fallback copy under a "computed from live planetary positions" claim.

### horoscope.html — score 7.5/10
*Role:* The daily-ritual hub: live transit-computed readings for all 12 signs, the habit loop that feeds the cast-chart funnel.
1. **Reading panel paints generic painterly art where a model surface belongs** (high/S) — js/horoscope-page.js:614 sets #srp-card-thumb to assets/images/zodiac-cards/<sign>.jpg — the old painterly card — while the sign pages just moved their thumb grids to img/engine/*.webp engine stills. Swap the panel thumb to the sign's ruler engine still (RULER mapping already exists in ap-zodiac-constants/sign pages) with the engraved seal overlay. The HTML default is already img/engine/earth.webp; only the JS override is stale.
2. **No deep link into the 3D model from the dial** (high/S) — The dial-intro (#dial-intro, line 262) says 'Explore the live sky' but never links to the model; the only explore.html link is buried in the closer. Add a model-moment chip near .sphere-controls: 'Open this sky in the 3D model' -> explore.html#m=<todayISO> using the already-specced deep-link format. The 2D canvas dial (zodiac-sphere.js, no Three.js) stays; this is the safe stills+links pattern.
3. **'Updated Daily at Midnight UTC' is factually wrong** (medium/S) — Line 168 eyebrow claims midnight UTC, but content-service.js isoDate() keys daily packs by LOCAL date and the engine badge says 'VSOP87 sky at local noon'. Readings roll at local midnight. Reword to 'New reading each day — computed for your local date'. Honesty is this site's binding law; this is its one soft spot on the page.
4. **Closer CTA field competes with itself** (medium/M) — The deep-reading-closer (line 744) offers 5 buttons (Cast, Explore 3D, Keep a Moment, Sky instrument, My Sky hub) after .sphere-ctas already offered 3 (chart/compatibility/transits). Keep Cast (primary) + Explore 3D (secondary), demote the rest to a text-link row so the page has one clear next step.
5. **Audit-mode UA sniff can degrade real users** (low/S) — Head script (line 84-96) flags typeof window.chrome==='undefined' && /Chrome/i.test(ua) as audit mode -> no webfonts, static poster instead of the live dial. Chromium-UA browsers with anti-fingerprinting (window.chrome deleted) silently get the degraded path. Scope that clause to webdriver/HeadlessChrome/lite=1 only.

### tools/generate-sign-pages.mjs — score 5/10
*Role:* Single source of truth for the 12 SEO landing pages — currently a source of un-truth: its output no longer matches the live pages.
1. **Back-port live-page drift BEFORE anyone regenerates (blocks everything else)** (high/M) — Fresh generator output vs live pages (verified by regenerating to scratchpad and diffing; identical drift on all 12): (a) generator lacks <meta name=color-scheme dark> + <style>html{background:#07070A}</style> and emits theme-color #0C1016 vs live #07070A — regen reintroduces the white-flash Jonny explicitly dislikes; (b) inline CSS + constellations.mjs still use brass #C2A05E/#ECE6D8 vs live silver #A8B0BC/#E8EBF0; (c) hero actions: generator 'Open in Daily Hub' primary vs live 'Cast free chart' primary; (d) generator lacks the .sign-instrument-note honesty span in About; (e) closer CTA row: generator emits shop.html links vs live 'Freeze a Moment'/'Daily sky'; (f) thumb grid: generator zodiac-cards/*.jpg vs live img/engine/*.webp; (g) footer-model.mjs emits the retired 'Tools' footer vs live 'Around the model'. Apply whichever palette is finally approved, but the structural deltas (a,c,d,e,f,g) must land in the generator now. Add a drift check to the audit suite: regen to a temp dir + diff against website/*.html, fail on mismatch — the tools/fix-*.mjs batch sweeps are what keep mutating generated pages.
2. **Content-bank expiry will break the honesty claim on all 12 pages** (high/M) — data/content-bank/manifest.json: daily packs end 2026-09-11 (monthly 2026-11), generated once on 2026-06-14 by tools/build-content-bank.mjs. After expiry, sign pages fall back to SignDaily generic copy + hash 'Lucky Number' + static 'Celestial Gold' under the hardcoded subtitle 'Computed from live planetary positions — same VSOP87 engine as the orrery' (generator line 772) — a HIGH honesty violation in waiting. Regenerate the bank on a schedule, and make the subtitle part of what JS paints so the claim only shows when a bank/engine reading is actually used.
3. **Add the model-moment deep link to the hero (the one missing model-first piece)** (high/S) — The hero engine still (RULER_STILL, line 227) is honest and beautiful but dead-ends. Under the figcaption add: <a href="explore.html#focus=${RULER_STILL[s.key].still}">See ${label} live in the model -></a> — stills + deep links, no new 3D, matches the specced #m=<ISO>&focus=<body> pattern. This makes all 12 SEO pages entry points to the masterpiece.
4. **Copy and JSON-LD nits in the SIGNS data** (medium/S) — (1) Line ~167 Aquarius friendship: 'A Aquarius will champion your causes' -> 'An Aquarius'. (2) FAQ answer 4 grammar: 'known for being courageous and decisive, honest to the point of bluntness, natural leadership instinct' — third item breaks the 'being' frame; rephrase the join. (3) breadcrumbLd (line 451) has Home->Sign but the visible breadcrumb is Home->Daily->Sign; add the Daily level. (4) Article JSON-LD lacks image (og image exists) and datePublished/dateModified.
5. **Engine-still thumb grid gives five sign pairs identical faces** (medium/M) — The live thumb-grid swap to ruler stills means Aries/Scorpio both show Mars, Taurus/Libra Venus, Gemini/Virgo Mercury, Capricorn/Aquarius Saturn, Sagittarius/Pisces Jupiter — 10 of 12 'Explore Every Sign' cards are visually ambiguous, and alt='Aries' on a Mars render is a small honesty wobble. When back-porting, overlay the engraved zodiac seal (data-celestial-seal='zodiac:<key>' already mounts via ap-page-boot) on each thumb, and fix alt to 'Aries — ruled by Mars'.

### aries.html — score 7.5/10
*Role:* Template exemplar of the 12 SEO landing pages: 'aries horoscope today' + evergreen sign guide, funneling to cast-chart.
1. **Hero Mars still should deep-link to the model (via generator)** (high/S) — The figure caption 'Mars · ruler of Aries · our engine render' (line ~280) is honest but terminal. Route the fix through generate-sign-pages.mjs (never this file): link the still to explore.html#focus=mars. Aries and Scorpio currently share the identical Mars hero AND Mars thumb — the seal overlay fix in the generator entry resolves the ambiguity.
2. **Today-card has no next step inside it** (medium/S) — js/sign-page-boot.js paint() renders overview/love/career/wellness + luckies + 'Deterministic for this date' note but no anchor — the daily ritual dead-ends in the card. Add one link into the hub ('Full reading with weekly outlook -> horoscope.html?sign=aries') inside paint(); the bank data even includes weekly/bestDay fields the card ignores.

### taurus.html — score 7.5/10
*Role:* Same generated template; 'taurus horoscope today' SEO entry with the strongest earthy copy of the four spot-checked.
1. **Fallback copy exposes raw filenames to readers** (medium/S) — js/sign-daily.js FALLBACK_OVERVIEWS (lines 12-23) and love/career/health fallbacks say 'Open horoscope.html for...' as plain prose — users would read a literal filename. Since paint() innerHTML-renders these, either reword to 'our daily horoscope page' or emit a real <a href="horoscope.html?sign=taurus"> in the fallback strings. Only visible when the bank misses a date — exactly the degraded state that needs the most care.
2. **Permanent 520px reserve leaves a void after short readings** (low/S) — Generator inline CSS sets #today-reading{min-height:520px} unconditionally (not scoped to .is-loading; sign-page.css uses the correct .is-loading scoping at 280px). Desktop bank readings render ~400px, leaving ~100px of dead space mid-page. Scope the 520px to .is-loading in the generator's inline style block.

### gemini.html — score 7.5/10
*Role:* Same generated template; 'gemini horoscope today' SEO entry — copy quality (twins/integration shadow section) is site-best.
1. **FAQ hard dates vs astronomy section's honest 'around' dates** (low/S) — The astronomy card carefully says 'The Sun crosses it around 21 May - 20 Jun... shifts by about a day year to year', but the FAQ + FAQPage JSON-LD state 'Gemini season runs May 21 - June 20' as exact. Align the FAQ answer with the hedged phrasing in the generator's faqSection()/faqLd (one string, used twice) — small, but this site wins by being the honest one.
2. **H1 is just the sign name** (medium/S) — h1 'Gemini' (generator line 751) wastes the page's one h1 on a single word while the title tag carries all the intent. 'Gemini — Zodiac Sign Guide & Today's Horoscope' (or keyword + dates subtitle promoted into the h1 block) would better match the queries the page ranks for. Generator-level, applies to all 12.

### cancer.html — score 7.5/10
*Role:* Same generated template; 'cancer horoscope today' SEO entry, with the Moon engine still as the strongest hero of the twelve.
1. **Moon hero should cross-link the lunar surfaces** (medium/S) — Cancer's hero is 'the Moon · ruler of Cancer · our engine render' — the site's most-loved body, and the page never links moonphase.html or a model moment. Via the generator, let the RULER_STILL entry carry an optional related-tool link (cancer -> moonphase.html or explore.html#focus=moon) rendered under the caption.
2. **Hero stack is 2+ screens at 390px** (medium/S) — On a Galaxy S24 the mobile hero stacks: planet still (62vw ~242px) + engraved seal (~80px) + constellation SVG (175px reserved) + h1 + dates + keyword + 4 facts + 2 buttons ~ 1000px before any content. In the generator's inline CSS @media(max-width:640px), hide .sign-hero__constellation (it is decorative, aria-labelled, and duplicated by the seal) to pull the daily reading above the second fold.

**Cross-cutting themes:**
- Generator/output drift is the group's defining risk: tools/fix-*.mjs and apply-audit-batch.mjs sweeps mutate the generated pages in place, so generate-sign-pages.mjs + footer-model.mjs have silently fallen behind; the 'edit the script, never the pages' invariant is currently inverted. A regen-and-diff gate in the audit suite would make this structural.
- Model-first is exactly half-landed across the group: engine stills are now the hero on all 12 sign pages and the thumb grids, but there is not a single deep link into explore.html/the live model from any of the 6 reviewed surfaces — the specced #m=<ISO>&focus=<body> pattern has zero mount points here despite being the safest page type for it (stills + links, no second 3D stack).
- Honesty discipline is genuinely excellent (methodNote on every reading, 'computing...' placeholders instead of canned positions, retrograde section refuses to show stale windows) — the two soft edges are both time bombs, not present violations: the content-bank expiry on 2026-09-11 under a 'computed from live positions' subtitle, and 'Updated Daily at Midnight UTC' vs local-date keying.
- Lucky Number / Lucky Color / Best Day are hash-seeded furniture in both horoscope-engine.js and the sign-daily fallback; the mood gauge was carefully made honest ('60 / 100', transit-derived, no fabricated fallback) but the luckies got no such treatment — either label them as traditional charms seeded from the date, or drop them to match the site's own bar.
- The hub<->sign-page funnel loop is well built and worth protecting: ?sign= deep links auto-open the panel, srp-guide-link routes back to the sign guide, footer cross-links all 24 destinations, and every page converges on chart.html as the single primary CTA.
**Risks:**
- Do NOT run tools/generate-sign-pages.mjs until the drift back-port lands — it would silently revert all 12 live pages (palette flip, white-flash guard, CTA swap, instrument note, thumb grid, footer) in one command; other agents' in-flight js/ work would survive but their page edits would not.
- Daily content bank runs out 2026-09-11 (monthly 2026-11); regeneration tool is tools/build-content-bank.mjs at repo root — needs a scheduled run or a pre-deploy check.
- Other agents are actively modifying js/ files; the controller findings (sign-page-boot paint(), horoscope-page.js srp-card-thumb, sign-daily fallbacks) may already be moving — re-grep before implementing.
- The Jet/Silver palette flip on these pages is applied but unapproved; the generator back-port must wait for (or parameterize) the palette decision, and half-flipped artifacts remain regardless (css/sign-page.css .sign-hero__glyph gradient still mixes gold #f2dfa7/#8c6a2f with a gold drop-shadow around silver).
- Any regeneration of sign pages requires tools/generate-sw-precache.mjs + SW cache-version bump (pages are precached in sw.js), then after_project_edit.ps1 -Project per house rules.

---

## Commerce

The commerce group is architecturally honest — the dormant/live dual-copy machinery, PreOrder-gated schema, and never-a-dead-link checkout resolution in shop-commerce.js are genuinely well built. But the revenue room has three silent go-live blockers (static hero CTAs never flip to Add-to-basket, two conflicting "live" predicates between isLive() and hasCheckout(), moment-pack has no birth-details form), links.html's entire monetisation block is dead code due to a script-order bug that suppresses the LIVE email-signup and Ko-fi links, and catalogue.html never loads its own display fonts for JS-enabled visitors. Model-first is half-delivered: product art is engine-still-derived and captioned honestly, but there are zero model-moment deep links (#m=) and no Model Window mounts anywhere in the group.

### shop.html — score 7/10
*Role:* Primary revenue room — converts chart-holders into Deep Reading £12 / poster PDF £6 (email capture while checkout is dormant), with Moment as the free on-ramp.
1. **Fix the 'instant download' delivery claims (honesty law)** (high/S) — shop.html compare table row 'Delivery' (line ~272: 'Instant PDF download') and the footnote at line ~562 ('the poster PDF downloads instantly') contradict the actual PayPal-link + Typeform fulfilment pipeline (fulfil-redirect.html → manual generation) AND contradict catalogue.html, which truthfully says 'Delivered within 24–48 hours'. Nothing downloads instantly in this architecture. Change both spots (and the data-when-live span) to '24–48h by email' until an automated delivery pipeline exists.
2. **J3 blocker: static featured-card CTAs never flip to purchase** (high/M) — The baked hero cards (lines ~185 and ~208) have <button data-ap-open-email="shop_featured">Notify me</button> with no data-when-live/data-when-dormant wrapper. When fulfilUrls are pasted, updateLiveCounts() (js/shop-commerce.js:922) flips headers to 'Available now — 2 live PDFs' but the primary card buttons still open the email modal — the only buy path is the buried quick-view modal. Wrap the CTA: dormant span = current Notify button; live span = <button data-add-cart="deep-reading">Add to basket</button> (bindFeatured at shop-commerce.js:963 already wires [data-add-cart] on the static host).
3. **Unify the two 'live' predicates before J3** (high/S) — js/shop-commerce.js:161 isLive() checks only p.fulfilUrl, but hasCheckout()/schemaAvailability() (lines 446/173) resolve via payhipUrl() which also accepts PAYHIP_URLS (lines 348–361) and p.paypalUrl. If a URL is pasted into PAYHIP_URLS only, JSON-LD advertises buyable InStock/OnlineOnly offers while the page stays dormant and cart.add() blocks. Make isLive(p) = hasCheckout(p), or delete the PAYHIP_URLS side-channel and document fulfilUrl in AP_MON (js/app.js:1549/1599) as the single paste point. Also: moment-pack (app.js:1582) has detailsForm:'' — pasting its fulfilUrl would take money with no birth-details capture; add its Typeform first or exclude it from J3.
4. **Mount the Model Window + model-moment deep links** (medium/M) — The hero has no model surface and no #m= links despite the product stills being engine-derived. Mount the specced Model Window frame after the shop-journey Path nav (or beside the hero lede): still + deep link explore.html#m=<saved-chart ISO>&focus=sun captioned 'the sky your PDFs are drawn from — see it in the model' (shop-page-boot loads profile.js, so AstroProfile.getCharts()/savedChart() is available; fall back to a tonight-moment link when no chart is saved). Add the same one-line deep link under each featured card. Stills + links only — no second Three.js instance.
5. **Link the Lookbook from the body + trim dormant dead weight** (medium/M) — catalogue.html is only reachable via the JS drawer 'More tools' entry (js/ap-nav-model.js:52) — no in-body link anywhere on shop.html. Add a quiet 'Browse the full Lookbook — prints, apparel & jewellery return soon →' after the compare fold. Separately, ~600 lines of hidden markup (art library, 9-card coming-soon grid, curated affiliate shelf, contribute panel, lines ~296–943) ship to every mobile visitor and show priced-but-unbuildable SKUs to noscript users; prune to match the pdf-only phase since catalogue.html now carries the full-range story.

### catalogue.html — score 6/10
*Role:* Aspirational lookbook — editorial desire-builder for the full personalised range (17 pieces across 5 collections) while shop.html runs pdf-only; feeds chart.html and shop.html.
1. **fonts.css never loads with JS enabled — the entire editorial typography silently falls back to Georgia** (high/S) — Line 39 loads css/fonts.css inside <noscript> only; no defer-page-css.js, and the ap-page-boot.js chain injects six CSS files but not fonts. All @font-face rules (Cinzel, Cormorant Garamond) live in css/fonts.css, so every real visitor sees the 'ceremonial Cinzel display' masthead in Georgia. Copy links.html's pattern: <link rel="stylesheet" href="css/fonts.css" media="print" onload="this.media='all'">.
2. **Add one email-capture affordance for 17 'Opening soon' pieces** (high/S) — Every piece ends at an inert 'Opening soon' pill — the page has zero notify path, wasting its best-qualified pre-launch demand. Add a single quiet button in the .cat-close section (line ~690): <button data-ap-open-email="lookbook">Tell me when the doors open</button> — app.js's wireEmailOpenDelegation() is already active here via ap-page-boot, so it costs one element. Keep 'Cast your free chart' as the primary CTA.
3. **Fix the palette-flip remnant on the 'Opening soon' pill** (medium/S) — .cat-piece__soon (lines 153–156) sets color:var(--cat-terracotta) (now #6AB0FF, blue) but keeps hardcoded warm literals border:rgba(217,123,82,.5) and background:rgba(217,123,82,.08) — blue text in an orange-tinted pill, repeated ~17 times. Tokenise the rgba values (or derive from --cat-terracotta). Same file also names silver/blue values '--cat-brass' — rename when the palette decision lands, but the mixed pill is a visible bug now.
4. **Soften fabricated social proof and caption the stills as model moments** (medium/S) — 'A proven anniversary and wedding keepsake' (Two Skies, line ~535; same phrase in shop.html:556 and app.js:1684) claims a track record that doesn't exist pre-launch — change to 'made for anniversaries and weddings'. Positively: the alt texts already admit the art is engine stills ('photoreal Earth… still', 'Jupiter still') — surface that as a small on-card caption ('engine still · example chart') and add one #m= deep link per collection head ('see this sky live in the model') to serve the model-first brief without new components.

### links.html — score 5/10
*Role:* Link-in-bio entry point — routes social traffic into the free funnel (chart, Moment, horoscope, synastry, Sky) plus email/tip monetisation links.
1. **The #lib-paid monetisation block is dead code — live email + Ko-fi links never render** (high/S) — The inline script (lines 103–133) reads window.AP_MON at HTML parse time, but AP_MON is defined in js/app.js which only loads AFTER DOMContentLoaded via deferred js/ap-page-boot.js. M is always {} so add() never fires — the LIVE emailUrl (https://list.astroprecise.app/subscribe) and LIVE tipUrl (ko-fi.com/astroprecise) links are permanently suppressed on the one page whose whole job is links. Fix: poll for window.AP_MON exactly like the AstroSocial block directly below it already does (renderLibSocial's 40ms retry), or move the render into a DOMContentLoaded handler that waits on AP_PAGE_BOOT.
2. **Ten 'coming soon' social buttons under Follow** (medium/S) — renderSocialRow is called with showAll:true while every AP_SOCIAL handle is '' (js/app.js:1243–1255) — visitors get a 'Follow' heading over ten disabled placeholder icons on a page built for one-tap decisions. Pass showAll:false here (js/affiliate-social.js:82 already filters to live URLs) and hide the 'Follow' group label when zero channels are set.
3. **Make the banner a model link** (medium/S) — The masthead image IS a model still (og-banner-silver.jpg — photoreal Earth from the live solar-system model) but it's aria-hidden decoration; the page has no model connection at all. Wrap .lib__banner in a link to explore.html (or a tonight #m= moment) with the label 'See the live model' — the strongest asset on the page becomes the model's front door for social traffic.
4. **Waitlist aside: warm-remnant wells and pure-white input ink** (low/S) — The cw-waitlist inline styles (lines 80–88) hardcode warm rgba(20,16,10,.45)/rgba(13,10,7,.7) backgrounds and color:#fff on the email input — clashing with the cool-void tokens and Jonny's eye-comfort preference (lifted deep-navy wells, ~90% ink). Swap to var(--void-raised)-family tokens and var(--parchment)/silver ink; move the styles into css/links-page.css while at it.

**Cross-cutting themes:**
- Go-live (J3) is undertested end-to-end: pasting a fulfilUrl on a preview copy would have exposed all three blockers at once — hero CTAs that don't flip to Add-to-basket, the isLive()/hasCheckout() predicate split, and moment-pack's missing detailsForm. Recommend a scripted J3 dry-run (dummy PayPal URL in AP_MON, walk card → cart → checkout → details follow-up) as a pre-launch gate.
- Boot-order fragility around late-loaded config: everything in this group depends on AP_MON defined in js/app.js, which arrives after DOMContentLoaded via ap-page-boot.js. links.html reads it too early (dead revenue links); catalogue.html falls outside the defer-css system entirely (fonts never load). A tiny 'ap:config-ready' event dispatched by app.js would let static pages consume config safely.
- Poster-PDF delivery copy contradicts itself across the group: shop.html says 'Instant PDF download', catalogue.html says '24–48 hours'. One truth (24–48h, matching the Typeform pipeline) should be enforced everywhere — this is the group's only real honesty-law violation.
- Model-first is stills-without-links: all product art is engine-derived and honestly captioned in alt text, but no page in the group carries a #m= deep link or a Model Window mount. Cheapest wins: linkified banner on links.html, one Model Window mount on shop.html hero, per-collection deep links on catalogue.html — stills + links only, never a second 3D instance.
- Email capture is the dormant-phase currency but coverage is uneven: shop.html has five entry points, catalogue.html has zero, links.html's is broken. Fixing the two gaps makes the whole group harvest pre-launch demand consistently.
**Risks:**
- Other agents are editing js/ files right now — line numbers cited for app.js/shop-commerce.js may drift; re-verify isLive()/PAYHIP_URLS and the AP_MON product block against HEAD before implementing.
- JSON-LD offers flip to InStock/OnlineOnly purely via JS gates; if URLs are pasted before the fulfilment pipeline is rehearsed, Google may index buyable offers ahead of actual delivery capability — reputational/refund risk on day one.
- GitHub Pages ToS: the on-site PayPal Buttons path (checkout.paypalClientId) is a documented breach while hosted on Pages (MONETIZATION.md warning in shop-commerce.js) — J3 must stay link-out only; don't let the promo's PayPal.Me flow normalise on-site collection either.
- shop.html ships ~600 lines of hidden full-catalogue markup with prices for available:false SKUs; noscript users and scrapers see priced products that cannot be built — low probability but it's the group's most likely future honesty complaint.
- The dormant cart FAB is a mild dead end (empty drawer + a 'Checkout securely' button that no-ops while the trust list says 'opening soon') — cosmetic now, but confusing if a screenshot circulates; consider hiding the FAB under body.shop--dormant.

---

## Long-tail & Trust Pages

The longtail-seo group splits cleanly in two: the trust pages (why.html, accuracy.html) and the synastry calculator (compatibility.html) are genuinely strong — crawlable, honest (the fabricated % is verifiably gone; harmony ring shows a real count/ratio from synastry-shared.js), form-first, single-funnel. The numerology/editorial tail (lifepath, angel-numbers, name-numerology, cosmic-story, guides) is functionally rich but leaks SEO value badly: most of its best copy lives only in JS (guides.html fetches everything from data/sky_guides.json with hash-only deep links; angel-numbers and name-numerology render meanings client-side behind a webdriver bail-out), and lifepath.html presents metaphysical claims as flat fact on an accuracy-branded site. Not one of the eight pages touches the 3D model — no engine still, no Model Window mount, no #m= deep link — which is glaring on why/accuracy/cosmic-story, three pages whose entire pitch is "the picture of the sky is real."

### compatibility.html — score 8/10
*Role:* High-volume 'zodiac compatibility calculator' landing page; form-first synastry tool feeding chart → Two Skies Map / Deep Reading.
1. **Add a model-moment deep link from the Davison tab** (high/S) — The Davison panel (#panel-davison, populated by renderDavison in js/compatibility-page.js) computes a real midpoint instant in spacetime — the single best model moment on the site. Append a 'See this moment in the model' link using the specced deep-link pattern (#m=<Davison ISO>&focus=sun) next to #davison-midpoint. Pure link, no 3D added to this page.
2. **Add FAQPage JSON-LD + matching visible Q&A** (medium/S) — The page has strong crawlable synastry copy but no FAQ schema. Add 4-5 questions (What is synastry? Is there a compatibility percentage? — great honesty story — What are composite vs Davison charts? Do you need birth times?) as visible HTML below the 'What Is Synastry?' section, mirrored in JSON-LD, as name-numerology.html already does correctly.
3. **Soften pure-white inline ink** (low/S) — The synastry aspect table and 'Most Significant Synastry Connections' list hardcode color:var(--white) on ~10 inline styles (lines ~674-717). Swap to the ~90% ink token per the eye-comfort standard; this is page-specific CSS, not the global palette.
4. **Align h1 with the money keyword** (medium/S) — Title targets 'Zodiac Compatibility Calculator — Free Synastry Report' but the h1 is 'Cosmic Compatibility'. Change the h1 (id=page-title, line 150) to 'Zodiac Compatibility Calculator' with 'Cosmic Compatibility' as the eyebrow, keeping the poetic register in the subline.
5. **Watch the mobile CLS reservations** (low/S) — Inline critical CSS reserves min-height:603px for the hero and min-height:1680px for #compat-form at ≤768px (head style block, lines 60-78). On a 390px S24 any form shrinkage leaves dead space; verify against the rendered form height after the current js/ edits land rather than hand-tuning now.

### lifepath.html — score 6.5/10
*Role:* 'Life path calculator' long-tail entry; numerology tool with transparent arithmetic feeding chart.html via dated crosslinks.
1. **Honesty: frame the number meanings as reflective tradition, not fact** (high/S) — The LP data object (inline script, lines ~521-630) states unhedged metaphysical claims as fact on an accuracy-branded site: 'You carry wisdom from many lifetimes' (LP 9), 'Profound, cellular healing energy' (LP 33), 'Life Path 2 vibrates with the Moon'. Adopt the angel-numbers.html pattern verbatim: add a one-paragraph 'How to read these' framing block above #lp-results ('numerology is a symbolic tradition — a mirror for reflection, not a measured property of you') and soften the two worst lines. The arithmetic badges are honest; the meanings aren't framed at all.
2. **Honesty: fix the fake-precision compatibility claim** (high/S) — 'Compatibility is calculated from life path vibration resonance' (line ~204) is untrue — data.compatibles is a hardcoded lookup list. Reword to 'traditional pairings from the Pythagorean tradition — a starting point, not a verdict', and link the strip to compatibility.html for the measured version.
3. **Make 'Sky on your birth day' a real model-moment link** (high/S) — renderCrosslinks (inline script, line ~733) links to plain ephemeris.html, discarding the birth date it already has. Pass the model-moment deep link (#m=<birthDate ISO>&focus=earth) so the crosslink lands on the user's actual sky — the page's only natural model connection and currently wasted.
4. **Add FAQPage schema + crawlable meanings** (medium/M) — No FAQ schema despite prime question inventory (what is a life path number / how is it calculated / what are master numbers). The full method + example calc are crawlable but all 12 number meanings live inside inline JS. Add a 4-6 question visible FAQ + JSON-LD, and consider emitting the 12 title/tagline cards as static HTML (the LP object is static data).
5. **Funnel: the result screen dead-ends short of the reading** (medium/S) — After a result the page offers crosslinks and a free PNG download but never mentions the Deep Reading. Add one dormant-aware tile (reuse the renderDeepReading pattern from name-numerology.html's inline script, which correctly falls back when AP_MON.deepReadingUrl is unset) under #lp-crosslinks.

### angel-numbers.html — score 7/10
*Role:* Highest-search-volume long-tail page in the group ('1111 meaning' etc.); best-in-class honesty framing; funnels to lifepath/chart.
1. **Render the 12 number meanings as static HTML** (high/M) — The entire ranking content (meanings for 111-1212) lives in js/angel-numbers.js NUMBERS and is injected client-side — static HTML ships 12 empty skeleton <article>s (lines 165-178), and the loader is skipped entirely under webdriver. The data is fully static; bake the cards into the HTML (or a small generator like tools/generate-sign-pages.mjs) and let JS only wire the clock. This also de-risks the FAQPage rich result, whose answers must be visible on-page.
2. **Fix the orphaned internal-link graph** (high/S) — No other static HTML page links to angel-numbers.html (verified sitewide grep — only sitemap.xml and JS-injected drawer entries in app.js/ap-nav-model.js NAV_EXTRAS). Add static contextual links from lifepath.html's 'Related Tools' teaser and name-numerology.html's funnel section; both currently link to each other but never to this page.
3. **Connect mirror moments to the model** (medium/S) — The Synchronicity Clock already computes a real instant with a real planetary hour. When a mirror moment fires (tick() in the inline script, ~line 438), add 'see this exact sky in the model' via the #m=<now ISO> deep-link pattern — a live, honest model tie-in with zero 3D on-page.
4. **Tokenise the deep-link highlight** (low/S) — The #num-XXXX hash highlight hardcodes target.style.borderColor='rgba(196,146,10,0.6)' (inline script, line ~485) — a retired warm-gold hex. Use the current accent token so the highlight matches whichever palette wins.

### name-numerology.html — score 7.5/10
*Role:* 'Name numerology calculator' long-tail entry; the group's cleanest SEO execution (visible FAQ matching JSON-LD) and correct dormant-checkout funnel.
1. **Fix the stale birth-date cap** (medium/S) — #nn-birthdate hardcodes max="2025-12-31" (line 174) — it is 2026; anyone born this year (parents casting for babies is a real numerology use-case) is blocked. Set max dynamically like lifepath.html does (dateInput.max = new Date().toISOString().slice(0,10)).
2. **Emit the 12 meanings statically** (medium/M) — Same defect as angel-numbers: #nn-meanings-grid ships as skeletons and renderMeanings() only runs when js/name-numerology.js loads (skipped under webdriver). NN.meaning() data is static — bake the grid into the HTML and drop 12 skeleton divs.
3. **Add one model touchpoint in the funnel** (medium/S) — The 'Your Name Is Only Half the Story' section sells the natal chart with text only. When the specced Model Window frame component lands, this section is the mount point (engine still + 'the sky is the other half' caption); until then, add a #m= birth-sky deep link when a birth date was entered.
4. **Cross-link the numerology cluster statically** (low/S) — The funnel links lifepath + chart but not angel-numbers.html; add it as a third quiet link to close the cluster (see angel-numbers orphan finding).

### cosmic-story.html — score 6/10
*Role:* Free sample narrative reading — the emotional bridge from cast chart to £12 Deep Reading.
1. **Add crawlable body copy** (high/M) — The page ranks on nothing: everything between hero and footer is 'Reading the sky…' until JS runs. Add a static explainer section (what a cosmic story is, the six movements, one short sample excerpt as real HTML) below #cstory-out so the page has indexable substance; the personalised narrative then replaces/augments it.
2. **Use the Mars and Saturn data the page already extracts** (medium/S) — The inline extract() pulls mercury/venus/mars/saturn, but CosmicStory.build() in js/cosmic-story.js never reads d.mars or d.saturn — the story jumps from Mercury/Venus to temperament. Add movement 'V · Mars & Saturn' (drive and discipline) using the existing interp() corpus; two placements of depth for ~20 lines of code.
3. **Show the sky the story claims** (high/M) — The footer asserts 'Every line above is drawn from your real VSOP87 chart' with no visual proof. This is the group's most natural Model Window mount: an engine still of the birth sky above movement I, plus a 'see the sky of your first breath' #m=<birth ISO> deep link. Stills + links only — the page has no Three.js and must not gain one.
4. **Retire the warm-gold hardcodes in page CSS** (low/S) — The inline style block leans on retired warm-palette fallbacks (#E6C24A, #F0E9DA) and .cstory-cta hardcodes a gold gradient (#E6C24A→#a8841e, line ~103) that ignores tokens entirely. Point at --ap-* tokens with token-consistent fallbacks; structural fix independent of the unapproved palette flip.

### guides.html — score 5/10
*Role:* Library hub meant to catch informational queries and route them into tools; currently a client-side app wearing an SEO page's URL.
1. **Make guides crawlable — static bodies or per-guide pages** (high/L) — All 11 guides (titles, deks, 3-paragraph bodies, tool links) are fetched from data/sky_guides.json and rendered by js/sky-guides.js; deep links are hash routes (#guides?story=id) that crawlers ignore, so zero guide content can rank. Highest-impact fix in the group: a generator script (pattern of tools/generate-sign-pages.mjs) emitting either static cards+bodies into guides.html or 11 small /guide-<id>.html pages with canonical URLs, keeping the overlay reader as progressive enhancement. Regenerate SW precache (tools/generate-sw-precache.mjs) after.
2. **Replace gradient card art with model stills** (medium/M) — THEME_GRADS in js/sky-guides.js paints every hero/card with abstract CSS gradients — exactly the 'weak/generic imagery where a model surface belongs' case. Swap to a set of captioned engine stills (Saturn for saturn-return-prep, Moon phase for moon-phase-living, etc.) referenced per-guide in sky_guides.json; label them as renders from the model.
3. **Deepen the thinnest guides** (medium/M) — Every guide is exactly 3 paragraphs (verified in sky_guides.json). For the two with real search demand — saturn-return-prep and retrograde-survival — expand to 6-8 paragraphs with concrete dates/mechanics the engine can back, keeping the 'ends at a computed tool' promise.
4. **Give the page a failure-state fallback with links** (low/S) — If the JSON fetch fails, the entire page body is 'Could not load guides / Retry' (init() catch in sky-guides.js) — a dead end with no links. Fall back to a static list of guide titles linking to the relevant tools.

### accuracy.html — score 8/10
*Role:* Trust/proof page for the 'is it accurate' query; converts sceptics to chart.html.
1. **Show the model it describes** (high/M) — Six cards explain VSOP87/ELP2000 with typographic icons (⊙ ☽) and not one pixel of the model. When the Model Window frame lands, mount it under the 'Professional-Grade Astronomy' header with a captioned engine still ('a frame from the live model — the same math this page describes') and a #m= deep link into the current sky. This is the brief's flagship gap: the accuracy page is where the masterpiece proves itself.
2. **Drop the dead-weight JS payload** (medium/S) — A fully static page loads ephemeris.js (74KB), chart-render.js (77KB), cosmos.js, profile.js, icons.js — all WITHOUT defer (lines 437-445) — plus css/chart.css, none of which the page's DOM uses. Trim to raf-core/cosmos/effects deferred + page-boot/footer-inject; meaningful mobile win on the reference S24.
3. **Cross-link the trust pair** (medium/S) — why.html links here ('See the accuracy page →') but accuracy.html never links back to why.html — the measured-vs-computed table and honesty rule live there. Add a ghost link after 'The Ethos' blockquote ('Read the full method — measured vs computed →').
4. **Substantiate 'we publish the validation'** (medium/M) — why.html says 'We publish the validation separately' and points here, but this page offers no actual comparison figures. Add one small honest table (e.g. three dates, our longitude vs reference ephemeris, delta in arcminutes) — computed, checkable, on-brand.

### why.html — score 8.5/10
*Role:* Manifesto/differentiator page — the honesty rule, measured-vs-computed, and the anti-fabrication argument; the group's strongest voice.
1. **Make 'Verify It Yourself' point at the model** (high/S) — The #verify section invites readers to open dev tools but never shows the thing being verified. Add a fifth verify item + Model Window mount: 'Watch the model — the live solar system this site draws from' with a #m=<today ISO> deep link. On the page whose thesis is 'make the picture true — and let you prove it', the picture itself is absent.
2. **Link the sources table rows to their surfaces** (medium/S) — The measured/computed table (#measured-computed) names feeds but links nowhere. Link 'Solar wind, Kp…' to the field-weather surface on ephemeris.html, 'Planet & Moon positions' to chart.html, 'Star positions' to the star catalogue surface — turning the manifesto into an internal-linking hub for the exact pages that need equity.
3. **Reconcile the reviewed-readings claim with the dormant shop** (low/S) — 'Who Makes This' promises readings are 'personally reviewed before they're sent' but never links to the product; with checkout dormant, add a soft link to shop.html#deep-reading only when live (AP_MON gate, as name-numerology.html does) so the claim and the buyable thing stay in sync.
4. **Tokenise the mailto link colour** (low/S) — The hello@ link hardcodes var(--gold-light,#E8C872) (line 212) — a retired warm fallback; swap fallback to the live token family.

**Cross-cutting themes:**
- Zero model presence across all eight pages: no live canvas, no engine still, no #m=<ISO>&focus= deep link anywhere in the group — even on why/accuracy/cosmic-story, whose copy is entirely about the model being real. Cheapest wins are deep links (lifepath crosslink, Davison midpoint, angel-numbers mirror moments); Model Window mounts belong on accuracy, why, and cosmic-story once the specced component ships.
- Static-data-rendered-by-JS antipattern: angel-numbers, name-numerology, lifepath (meanings), guides (everything), and cosmic-story ship skeletons or empty divs in static HTML and hydrate purely static content client-side — with loader scripts that bail under navigator.webdriver, so audit tooling sees blank pages too. Baking static data into HTML (or generators per the sign-pages pattern) fixes SEO, noscript, and audit fidelity in one move.
- Honesty framing is inconsistent across the numerology cluster: angel-numbers.html sets the gold standard ('a mirror for reflection, never a prediction' at every layer) while lifepath.html states reincarnation and healing-energy claims as flat fact two clicks from the honesty manifesto. Adopt the angel-numbers framing pattern as the cluster-wide standard.
- Internal-link graph is thin at the edges: angel-numbers.html has zero static inbound links (JS drawer + sitemap only); accuracy↛why is one-directional; the numerology trio doesn't reliably interlink; guides' link pills exist only inside client-rendered JSON. The trust pair (why/accuracy) should also serve as the linking hub for the tail.
- Retired warm-gold hex codes (#E6C24A, rgba(196,146,10,…), #E8C872) survive in page-specific inline CSS/JS on cosmic-story, lifepath (download card + element themes mislabelled as gold), angel-numbers (hash highlight), and why (mailto) — independent of the unapproved Jet/Silver flip, these should point at tokens.
- Funnel discipline is good group-wide (form-first everywhere, dormant checkout honoured via the AP_MON gate on name-numerology and compatibility) — but lifepath's result screen is the one page that never mentions the reading product.
**Risks:**
- Other agents are editing js/ right now — controller-level observations (compatibility-page.js upsell, sky-guides.js renderer, synastry-shared.js character bands) were reviewed as of 2026-07-10 and may drift before fixes are implemented; re-grep before acting.
- Every recommendation to bake JS-rendered content into static HTML increases page weight and requires regenerating the SW precache (tools/generate-sw-precache.mjs) and, for new per-guide pages, sitemap.xml — skipping either breaks one-click launch/caching.
- The webdriver/HeadlessChrome bail-outs mean any Playwright-based visual audit of angel-numbers, name-numerology, lifepath, and guides scores empty skeletons — audits of this group must account for the ap-audit-path, or findings will be artifacts.
- FAQPage rich results (angel-numbers, name-numerology) depend on answers being visible on-page; angel-numbers currently satisfies this only after client render — if Google tightens JS-rendered FAQ eligibility, those snippets are at risk until the static-render fix lands.
- Model-moment deep links (#m=<ISO>&focus=) are specced but I did not verify the receiving page's parser is live; ship the links only after confirming ephemeris/explore handles the fragment, or they degrade to plain page loads.
- Do not add live Three.js to any page in this group — none currently carries an instance, and cosmic-story/guides load other heavy stacks; stills + deep links are the safe pattern per the one-instance rule.

---

## Infrastructure & Index Variants

The housekeeping wing is in decent hygiene shape (robots/sitemap/manifest all functional, redirect stubs mostly correct, SW offline fallback wired) but the v684 homepage redesign silently orphaned the entire index-variant family: index.html no longer links to index-full.html (the ?full=1 cinematic path is dead from live nav — only the internal QA page phone-cosmic-viewer.html and the orphaned index-classic.html reach it), index-classic.html is a fully-functional 682-line snapshot of the pre-redesign homepage with stale branding, and auth-redirect.html serves no auth flow the site has ever had. Meanwhile every visitor still precaches index-full.html + index-lite.html via sw.js (tools/generate-sw-precache.mjs ROOT_HTML list, line 26). The machine-readable surfaces (llms.txt especially) have drifted behind the redesign — llms.txt still claims "Clock of the Spheres" naming, a potentialAction that index.html no longer emits, and omits moment/mysky/explore, the model-first flagship pages. 404.html is a genuine little brand moment; offline.html is honest but plain and undersells the offline-capable PWA it fronts.

### 404.html — score 7/10
*Role:* GitHub Pages error catch — turn a dead URL into a brand moment and route the visitor back into the funnel
1. **Mount a model surface instead of the generic star glyph** (medium/S) — The .glyph block (line 50) is a plain 4-point star SVG where a model still belongs. 404 is only ever served online, so swap in a small engine still (img/engine/earth.webp — already excluded from precache, fine here) or the specced Model Window frame, and make the single CTA a model-moment deep link (e.g. 'Return to the sky' -> /#m=<today>&focus=earth) so the recovery IS the masterpiece.
2. **Retokenise the page-specific palette** (medium/S) — The inline <style> (lines 31-44) hardcodes the retired warm-black set (#0D0A07/#050406/#030203 radial, #E8E0D0/#A89E88 ink) mixed with silver #A8B0BC — off the token system entirely. Replace with --ap-* tokens (or at least the lifted deep-navy well values) per the no-hardcoded-hex rule in DESIGN.md; 404 pages are exempt from the palette-flip freeze because this is page-local CSS, not the global theme.
3. **Add one quiet funnel line under the CTA** (medium/S) — After the 'Return to the sky' button (line 56), add a single small-caps secondary link 'or cast your free chart -> chart.html'. Keeps ONE primary CTA but stops 404 traffic from having home as its only exit. Copy voice is already excellent ('precessed beyond the visible sky') — keep it.
4. **Drop the canonical/OG pointing at /404.html** (low/S) — Line 11 canonicalises https://astroprecise.app/404.html and lines 12-24 emit full OG/Twitter cards for a noindexed error page. Nobody shares a 404; the canonical actively invites indexing signals the robots meta then fights. Delete canonical + OG block, keep title/description/noindex.

### offline.html — score 5/10
*Role:* Service-worker navigation fallback (sw.js line 608) — reassure and route to what still works offline
1. **Point to the tools that actually work offline** (high/S) — The copy promises 'Once cached, the orrery and charts work without the network' but the only action is 'Try home' (line 25). chart.html and horoscope.html are both precached — add two links labelled honestly ('Open your saved chart — works offline', 'Today's reading — works offline'). That converts the dead end into proof of the PWA's core promise.
2. **Give it the brand, self-contained** (medium/S) — It renders in Georgia fallback with a legacy cyan accent (#7EC8E8, line 17 — neither current palette). Since network is absent, inline a small SVG of the orrery meridian ring (the hero__meridian rings from index-full.html lines 292-307 are pure SVG, trivially inlined) and use the token hex values inline. No external requests needed, instant brand moment.
3. **Show connection state honestly** (low/S) — The auto-redirect on 'online' (lines 27-30) is good; surface it — a one-line status ('Watching for a connection — this page will return to the sky by itself') so users know they can leave the tab open rather than hammering refresh.

### auth-redirect.html — score 2/10
*Role:* Orphaned redirect stub — no auth flow exists on a site whose whole pitch is 'no account'
**RETIRE CANDIDATE:** Retire. Nothing references it (only an exclusion regex in js/ap-engine-visuals.js line 160), the site has no accounts by design, and its very filename contradicts the privacy story. It is not in the SW precache, so deletion is clean; traffic (if any external link exists) can 404 into the well-branded 404.html.
1. **Delete the file** (medium/S) — Remove auth-redirect.html; keep the exclusion entry in ap-engine-visuals.js harmlessly or tidy it in the same pass. No precache regen strictly required (it is not listed in sw.js), but run tools/generate-sw-precache.mjs anyway as the standing rule after root-HTML changes.
2. **If kept, fix the white flash and dropped params** (low/S) — Unlike index-lite.html it has no <style>html{background:#07070A}</style> and no color-scheme meta, so dark-mode users get a white flash; and location.replace('index.html') (line 9) drops location.search + location.hash, unlike index-lite's param-preserving stub. Copy index-lite.html's pattern verbatim if any inbound link is discovered.

### index-lite.html — score 5/10
*Role:* Legacy redirect stub from the old lite-shell era — preserves inbound links to the retired /index-lite URL
**RETIRE CANDIDATE:** Keep as a redirect stub (it is the best-built stub of the set: dark bg, canonical to /, preserves query+hash) but remove it from the shipped app payload — it is listed as 'root HTML shipped to users' in tools/generate-sw-precache.mjs line 26 and precached for every visitor via sw.js line 382, which is dead weight for a 17-line redirect.
1. **Remove from SW precache** (medium/S) — Delete 'index-lite.html' from the ROOT_HTML list in tools/generate-sw-precache.mjs (line 26), regenerate, bump V in sw.js. A redirect stub never needs to work offline — the offline fallback chain already lands on index.html.
2. **Add noindex** (low/S) — It has canonical -> / but no robots meta; a meta refresh page with a canonical is usually fine, but every other stub in this family (index-ephemeris, auth-redirect) carries noindex — add it for consistency and to guarantee it never appears in results.

### index-full.html — score 6/10
*Role:* The former cinematic homepage (?full=1 opt-in) — now orphaned from live navigation by the v684 index.html redesign
**RETIRE CANDIDATE:** Retire-or-merge decision needed. As code stands, no live page links to it: index.html mentions it only in an importmap comment (line 203); its only consumers are phone-cosmic-viewer.html (internal QA, noindex) and the orphaned index-classic.html; its cinematic role is superseded by explore.html ('Full sky tour' chip, index.html line 346). Yet all 2,411 lines are precached for every visitor (sw.js line 381). Traffic should go to / (general) and explore.html (cinematic intent). BEFORE deleting: (a) salvage the FAQ section (lines 923-944) + its FAQPage JSON-LD (lines 126-153) — that honest objection-handling copy and schema now exist ONLY on this noindexed orphan, and the indexable homepage has no FAQ at all; (b) update lite-shell-boot.js (fetches index-full.html at line 132), affiliate-social.js line 158, app.js line 1284, phone-cosmic-viewer.html line 52, and the ROOT_HTML list in tools/generate-sw-precache.mjs line 26, then regenerate precache and bump V. Replace the file with a param-preserving redirect stub (index-lite pattern) pointing at explore.html since ?full=1 links were shared publicly from the old hero.
1. **Salvage the FAQ + FAQPage schema onto index.html before retiring** (high/M) — index.html (the indexable homepage) currently emits WebSite + WebApplication JSON-LD but no FAQPage, and has no visible FAQ. Port the four-question FAQ (index-full.html lines 930-941) and its matching JSON-LD verbatim — the copy is the strongest honesty/conversion writing in the group ('It's never a subscription', arcminute claim with dates). This is a merge, not new content.
2. **Decide the page's fate explicitly and cut the precache cost** (high/M) — Either re-link it (restore a 'Full cinematic experience' link on index.html, which the redesign dropped) or retire it to a redirect stub at explore.html. The current state — unreachable but precached, maintained, and listed in two JS page arrays — is the worst of both. If retiring, follow the consumer-update checklist in the retire note; the SW uses Promise.allSettled (sw.js line 549) so a stale entry won't brick installs, but it will 404 on every install until the generator list is fixed.
3. **Fix the verbatim-claim drift in the FAQ JSON-LD** (low/S) — The head comment (line 116) asserts JSON-LD answers 'match the visible FAQ verbatim (honesty rule)', but the Deep Reading answer differs ('same interpretations engine as the free chart tools' in JSON-LD line 139 vs 'same engine as your free chart' + sample link in visible line 934). Trivial, but on a site whose law is honesty, a self-certifying comment that is no longer true should be corrected wherever this block lands.

### index-classic.html — score 2/10
*Role:* Orphaned 682-line snapshot of the pre-redesign lite homepage shell — dead weight and agent-confusion hazard
**RETIRE CANDIDATE:** Retire now. Nothing links to it, it is noindexed and not precached, but it is a fully functional obsolete homepage: it loads live controllers (lite-shell-boot.js, ap-nav-model.js, lite-orrery chain), fetches index-full.html to inject a tail, still advertises 'Full cinematic experience -> index-full.html?full=1' (line 272), and carries superseded copy ('A precision instrument wearing the skin of an astrology site', line 439) and the old SearchAction JSON-LD (chart.html?date= — not a real search). On a three-agent machine, a live-looking second homepage is exactly the stale-copy trap CLAUDE.md rule 3 exists for. Replace with the standard redirect stub to / (index-lite pattern), or delete outright; traffic goes to /. This also orphans lite-shell-boot.js (its only consumer) — flag that to whoever is in js/ right now rather than deleting the JS in the same pass.
1. **Convert to redirect stub** (high/S) — Replace file contents with the index-lite.html param-preserving stub pointing at ./ — keeps any historical inbound link alive, removes 680 lines of divergent homepage. Log it in AGENT-HANDOFF.md since Grok/Hermes may hold references.
2. **Note the knock-on: lite-shell-boot.js becomes dead** (medium/S) — js/lite-shell-boot.js (fetch of index-full.html, line 132) and its sw.js network-first 'critical' entry (sw.js line 584 regex includes lite-shell-boot.js) have no remaining consumer after this retirement. Don't delete mid-flight — other agents are in js/ — but record it for the next precache regen.

### index-ephemeris.html — score 6/10
*Role:* Redirect stub from the retired 'Ephemeris redesign' preview URL — correctly built, near-zero cost
**RETIRE CANDIDATE:** Optional retire. It is the model stub of the family (dark bg, noindex, canonical to /, meta refresh + JS replace, visible fallback link) and is excluded from precache. Its only flaw: location.replace('./') (line 19) drops query/hash unlike index-lite's stub. Keep for 6 months of inbound-link decay, then delete; traffic goes to /.
1. **Preserve params in the JS redirect** (low/S) — Change line 19 to location.replace('./' + location.search + location.hash) to match index-lite.html's pattern — costless correctness if anyone shared a parameterised preview link.
2. **Set a deletion date** (low/S) — Add a dated comment (it already documents its purpose at line 14 — extend it: 'delete after 2027-01') so the next housekeeping pass can remove it without re-investigating.

### robots.txt — score 8/10
*Role:* Crawler policy — allow-all with sitemap + llms.txt pointers
1. **Disallow the internal QA/ops pages** (low/S) — phone-audit.html, phone-cosmic-viewer.html and outreach.html rely solely on meta noindex,nofollow, which still costs a crawl each. Add Disallow lines for those three (they hold no link equity worth passing). Keep 404/offline crawlable — they're self-managing via meta.
2. **Leave everything else alone** (low/S) — The file is correctly minimal: Allow all, absolute Sitemap URL, and the llms.txt pointer comment is a nice AI-crawler touch. No further action.

### sitemap.xml — score 7/10
*Role:* Crawl map — 44 URLs, correctly excludes all noindexed variants and utility pages
1. **Refresh mass-stamped lastmod dates or generate them** (medium/M) — Nearly every entry says 2026-07-02 including the homepage, which shipped a full redesign (~v684) since. Bulk-identical lastmod values are the metadata cousin of fake precision and crawlers learn to ignore them. Add a tiny tools/generate-sitemap.mjs that stamps lastmod from git log per file (the repo already has the generator pattern with generate-sw-precache.mjs).
2. **Fix the mysky.html entry** (low/S) — Line 262-266: it is the only URL missing lastmod, and it sits after a stray blank line — clearly hand-appended. Add lastmod and fold it into date order; a generator (fix above) makes this class of drift impossible.
3. **Confirm coverage stays in lockstep with new pages** (low/S) — Current coverage is actually complete (moment, mysky, explore, catalogue, guides all present; noindexed pages all correctly absent), but this was manually maintained — the moment/mysky/explore trio arrived with three different lastmod conventions. Same generator solves it.

### manifest.json — score 7/10
*Role:* PWA install manifest — identity, icons, shortcuts for the installable app
1. **Slim the icon payloads** (medium/S) — img/icon-192.png is 102KB and icon-512.png is 641KB (icon-maskable-512 429KB) — over 1.1MB of icons fetched at install and precached. A flat dark logo mark should compress to <30KB each at these sizes; re-export with proper PNG quantisation. Biggest single win in this file.
2. **Align background_color with the shell** (low/S) — background_color is #000000 (line 9) while every page paints html at #07070A and theme_color is #07070A — the splash-to-app transition flashes pure black then lifts. Set background_color to the html background value so launch is seamless (matches the 'canvas never flashes' comfort rule).
3. **Add screenshots and a Sky shortcut** (medium/M) — No 'screenshots' member, so Chrome shows the plain install prompt instead of the rich sheet — two captures (orrery hero, chart wheel) are the model-first surface at the exact moment of install commitment. Also consider adding ephemeris.html ('Sky') to shortcuts alongside horoscope/chart/transits; it is the signature feature.
4. **Verify start_url ?app=1 is intentional** (low/S) — start_url 'index.html?app=1' — no JS reads app=1 anywhere in js/ (verified by grep). Harmless (SW nav fallback covers the query-variant cache miss), but either use it (e.g. suppress the install nudge in installed mode) or drop the param to keep the honest no-tracking story literal.

### llms.txt — score 6/10
*Role:* AI-crawler site summary — the machine-readable face of the honesty brand
1. **Fix the stale naming and false schema claim** (high/S) — Line 16 calls ephemeris.html 'Clock of the Spheres (Instrument)' — its actual title is 'The Sky'; and line 73 claims 'WebSite + potentialAction on index' but index.html emits no potentialAction (verified — it has WebSite + WebApplication). For a file whose entire purpose is being quoted verbatim by AI assistants, these are honesty findings, not nits.
2. **Add the model-first pages** (high/S) — explore.html (the flagship 3D model tour the homepage now promotes), moment.html (sitemap priority 0.85) and mysky.html are all absent. Add them to Primary tools with one-line descriptions — an AI recommending this site should be pointed at the masterpiece, not just the calculators.
3. **Surface the Deep Reading funnel** (medium/S) — The conversion offer appears only obliquely ('digital readings' in the Shop line). Add a line for the Deep Reading with the sample: 'Deep Natal Reading (paid PDF, one-time): sample at https://astroprecise.app/sample-reading.html' — sample-reading.html is noindex but llms.txt readers can still fetch it, and it is the exact page an assistant should cite when a user asks what the paid product is.
4. **Refresh the freshness section** (low/S) — Line 77's 'Freshness via dateModified (2026-06-17 era)' is self-describing staleness three weeks and one redesign later. Either maintain this section on every deploy or replace it with evergreen wording ('canonicals + lastmod maintained in sitemap.xml').

**Cross-cutting themes:**
- The homepage redesign severed the variant family without a burial: index-full.html lost its only live entry link, index-classic.html became a functioning ghost homepage, and lite-shell-boot.js lost its purpose — yet index-full + index-lite remain in the SW precache shipped to every visitor (tools/generate-sw-precache.mjs line 26 ROOT_HTML list). One retirement pass (stub the pages, salvage the FAQ, edit the generator list, regenerate, bump V in sw.js) closes all of it.
- Redirect-stub quality is inconsistent: index-lite.html is the gold pattern (dark html bg, canonical, preserves search+hash), index-ephemeris.html drops params, auth-redirect.html drops params AND flashes white. Standardise on the index-lite pattern for any stub that survives.
- Machine-readable surfaces drift behind the fast-moving homepage because they are all hand-maintained: sitemap lastmod mass-stamped 2026-07-02, llms.txt describing the pre-redesign site, JSON-LD claims (potentialAction, 'verbatim' FAQ) no longer true. The site's honesty law should extend to metadata — a small generator (sitemap from git mtimes) plus an llms.txt check in the deploy gate would hold the line.
- 404.html and offline.html are the only two pages in the group with zero model connection — both are cheap Model Window / inline-SVG wins, and offline.html in particular should prove the PWA promise by linking the precached chart and horoscope tools.
- Page-local CSS on 404.html and offline.html hardcodes retired palette hexes (warm blacks #0D0A07/#050406, legacy cyan #7EC8E8) instead of --ap-* tokens — independent of the unapproved global palette flip, these files were never tokenised at all.
**Risks:**
- Other agents are actively editing js/ right now — lite-shell-boot.js and lite-sky-dock.js sit exactly on the retirement boundary (lite-shell-boot's only consumer is the orphaned index-classic.html; lite-sky-dock is ALSO used by index-full's hero). Do not delete either JS file in the same pass as the page retirements; log intent in AGENT-HANDOFF.md first.
- index-full.html?full=1 was publicly linked from the live hero for weeks (the old shell's 'Full cinematic experience' link) — external/shared links exist, so retirement must be a redirect stub, never a deletion, or those visitors hit 404.
- Any file removed from the site while still listed in the sw.js PRECACHE array will 404 on every install until tools/generate-sw-precache.mjs is re-run and V is bumped — installs won't brick (Promise.allSettled, sw.js line 549) but offline coverage silently degrades; always regenerate in the same commit.
- Salvaging the index-full FAQ onto index.html adds a FAQPage JSON-LD to the indexed homepage — keep the visible copy and the schema strictly identical (the honesty rule) and re-run the visual-check audit suite before deploy, since index.html's critical-path head is fragile (see the ap-award-critical-palette comment).
- The 12 sign pages and SW precache are generated artifacts — none of this group's fixes touch them, but the retirement commit must not hand-edit sw.js's PRECACHE block directly (regenerate via the tool) or the next generator run reverts it.