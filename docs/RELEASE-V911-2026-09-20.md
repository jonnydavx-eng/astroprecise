# AstroPrecise v911 release evidence — 20 September 2026

Candidate branch: codex/market-release-20260920. Base and rollback source: b81cc1fd0a4343222a33b82128942cb14b41a552 (public v906 before this release).

This integrates the canonical website artwork, readings and page improvements while preserving main's GitHub Pages workflow and remote-only controllers. Existing canonical dirty work is preserved in its original tree. Asset query identities and worker are v911; the generated launch shell has 84 entries and approximately 2.82 MB before minification.

Release fixes: unknown-time handoffs clear stale time and skip old-chart auto-restore; accessible model labels describe the selected view and time; artwork shelf downloads retain blob URLs while visible; storage failure is distinguished from an empty shelf and download initiation; saved feedback waits for IndexedDB. Product illustrations are explicitly distinguished from actual PDF samples. Accuracy copy distinguishes coordinate resolution from model error. Packaging gains its missing hash helper and directory guards.

## Verification

- Syntax: 147/147 JavaScript files.
- All 13 engine/content/bridge/chart checks run for this release passed; adapter suite also passed.
- Profile-save audit: 32/32.
- Focused handoff, library and packaging tests: 29/29.
- Deep-reading test passed after updating retired presentation assertions while retaining content/unknown-time/privacy checks.
- Real mouse/orbit/time-scrub check passed.
- Birth-hour still: nonblank PNG, correct date/name, known-time and unknown-time captions passed on Home, the sole general WebGL owner.
- Production build passed. Production desktop 1440 and phone 390 smoke: one canvas, no horizontal overflow, Earth selection, nine key routes passed.
- Real service worker installed with ap-v911 cache; offline Home, Chart and Shop passed.

## Limits and remaining work

Paid checkout remains disabled and URLs null. Seller facts, authenticated payment evidence and complete customer fulfilment remain unresolved. No transaction or marketing send was performed. This is a public free-experience release, not a paid-launch certification or a 100/100 rating.

The inherited npm test graph contains absent test entrypoints; test:fulfil cannot complete because four historical fulfilment tests are absent from main. Packaging itself passes 5/5. test-release-honesty stops at an obsolete runbook command-count assertion. The old _v833-living-sky harness expects overlaid controls and a System button, whereas this design has adjacent controls and Overview; the new production smoke covers the current layout. No claim that these old aggregate suites pass.

The original canonical tree retains internal Adobe/source/operational material; only the public website and directly needed packaging/test changes are part of this release. Deployment success must be checked against the final GitHub main ref and public worker/assets after merge.
