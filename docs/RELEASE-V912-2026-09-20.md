# AstroPrecise v912 release — 20 September 2026

Public tip bump of the already-merged free 11:11 / mirror-hour / presence / synchronicity work (PR 45, merge `fd6461e13beac864975b5e05460da953d72f7d50`). PR 45 left the cache tip at `ap-v911` on purpose until this release.

This tree sets:

- `website/sw.js` `const V = "ap-v912"`
- `website/js/ap-asset-v.js` `AP_ASSET_V = '912'`
- matching `?v=912` query pins on authored pages, injectors, and generated sign pages

No checkout, ads, SKU, or hosting change. Launch-shell precache already includes `ap-mirror-hour.js`, `ap-mirror-presence.js`, and `ap-mirror-hour.css` from PR 45. The generator was not re-run: the shell is already over the 84-entry budget, and regenerating would either fail or drop files.

## Deploy path (existing, no new spend)

Live door is GitHub Pages via `.github/workflows/deploy-pages.yml` on push to `main` touching `website/**`. Custom domain `https://astroprecise.app`. There is no `gh-pages` branch and no Cloudflare production cutover for this ship.

1. Merge this tip bump to `main`.
2. Watch [Actions → Deploy to GitHub Pages](https://github.com/jonnydavx-eng/astroprecise/actions/workflows/deploy-pages.yml) for the `ap-v912` commit. Concurrency group `pages` does not cancel an in-flight run, so the PR 45 v911 deploy may finish first and this run queues behind it.
3. Confirm live:
   - `https://astroprecise.app/sw.js` contains `ap-v912`
   - `https://astroprecise.app/js/ap-mirror-hour.js?v=912` returns HTTP 200
   - Home / chart / sky-card HTML pin `?v=912`

PR 45 Pages run (still tip 911, adds the 11:11 assets): https://github.com/jonnydavx-eng/astroprecise/actions/runs/35494410957

## Limits

Paid checkout remains disabled. This is a public free-experience cache tip, not a paid-launch certification.
