# Local agent coordination — machine-specific, stays out of the public repo
AGENT-HANDOFF.md
OVERHAUL-PLAN.md

# Secrets (Cloudflare API token, etc.)
secrets/.env.local
.env.local

# Android / Gradle
.gradle/
build/
app/build/
local.properties
.idea/
*.iml
captures/

# Node
node_modules/

# Build output (generated by `npm run build` from website/)
dist/

# Agent scratch worktrees (never commit — full repo copies). Keep .claude/launch.json tracked.
.claude/worktrees/

# Lighthouse / visual-check throwaway Chrome profiles + run artifacts
tools/visual-check/tmp/

# OS noise
.DS_Store
Thumbs.db
desktop.ini

# Python
__pycache__/

# Visual-check run outputs (screenshots/reports — hundreds of MB, regenerate on demand)
tools/visual-check/out/

# Stray audit-suite copy inside the served website dir (2026-07-10: 227MB, must never deploy — see docs/ORBITLAB-INTEGRATION-MASTERPLAN-2026-07-09.md)
website/tools/visual-check/
