# AGENTS.md — AstroPrecise
Slug: astroprecise
Updated: 2026-08-28
Scope: this repository and its child folders unless a nearer `AGENTS.md` narrows it.

## Grounding

- Canonical repository: `C:\Users\jonny\dev\astroprecise`.
- Supported estate agents start with PROJECT-FIRST using the exact atlas name
  `AstroPrecise`, then read current `STATUS.md` and the newest
  `AGENT-HANDOFF.md` entry. Preserve any active Coherence wave boundary.
- The active atlas is `C:\Users\jonny\dev\_estate\PROJECTS.md`. Do not use
  missing `C:\Shared` pointers or another project's folder. Stop if the atlas,
  repository identity, wave manifest, or handoff disagree.
- Measure the current host, branch, HEAD, remotes, dirty and ignored files.
  Preserve user-owned work and keep one writer per tree.

## Public-state and safety gates

- Preview assignments `8790` and `8794` are not live proof. Verify the exact
  local process or public URL before reporting current state.
- Recheck the remote ref and public asset identity before a release claim.
- Do not push GitHub `main`, publish a listing, open checkout, change production
  configuration, or deploy without the required owner and governance authority.
- Keep all secrets, credentials, customer data, and security material outside
  this public repository. Never write plaintext values into code or handoffs.
- Run the declared proof for the exact candidate and record results in
  `STATUS.md` or `AGENT-HANDOFF.md`; same-lineage review is advisory, not
  independent verification.
