# AstroPrecise — Multi-Agent Audit & Improvements (2026-06-16)

**Local build:** ap-v313 · **Preview:** localhost:8790 · **Viewport:** mobile 390×844

## Full audit scores

### Site summary (deep audit)

| Category | Score |
|----------|------:|
| **Overall** | **92/100** |
| Performance (avg) | 78 |
| Accessibility (LH) | 100 |
| Best practices | 100 |
| SEO | 100 |
| Axe (avg) | 93 |

### Per-page Lighthouse + axe

| Page | Perf | A11y | BP | SEO | Axe | CLS | TBT | SI |
|------|-----:|-----:|---:|----:|----:|----:|----:|---:|
| index `/?lite=1` | **99** | 100 | 100 | 100 | 100 | 0 | 0ms | 1.6s |
| chart | 82 | 100 | 100 | 100 | 82 | 0.004 | 0ms | 2.5s |
| horoscope | 70 | 100 | 100 | 100 | 77 | 0.001 | 187ms | 2.6s |
| compatibility | 80 | 100 | 100 | 100 | 100 | 0.007 | 0ms | 3.1s |
| ephemeris | 73 | 100 | 100 | 100 | 100 | **0.13** | 0ms | 3.2s |
| shop | **66** | 100 | 100 | 100 | 100 | **0.60** | 53ms | 2.3s |

### Visual-check pass/fail

| Suite | Result | Notes |
|-------|--------|-------|
| Preloader (cosmic) | ✅ pass | 44.6s intro, all phases OK |
| Page captures | ⚠️ 2 false positives | lite index (no starfield by design); hero-entered targets wrong URL |
| Axe (5 pages) | ❌ 2 serious | chart aria, horoscope link contrast |
| Perf (Playwright) | ✅ pass | index 431ms, chart 180ms load |
| Lighthouse CLI | ⚠️ EPERM cleanup | JSON written; scores valid |
| Visual regression | ⚠️ baseline drift | preloader redesigned; pages ~4–6% animation noise |

---

## P0 fixes (do first)

### Performance — shop CLS 0.60 → <0.05, perf 66 → ~85

1. Remove `min-height: 100vh` from `.shop-hero--compact` (`shop-critical.css`)
2. Reserve height for `#shopc-grid`, `#shopc-filters`, `#shop-art-library`
3. First featured image: real `src` + `preload` (not `data-src` only)
4. Add `.ap-art-grid` layout rules to `shop-critical.css`
5. IO-defer `renderGrid()` until grid scrolls into view

**Expected site perf avg: 78 → ~86**

### Accessibility — axe 93 → 100

1. **chart.html:406** — add `role="group"` to `#big-three` (aria-prohibited-attr)
2. **horoscope.html:339** — underline/weight on lifepath link (link-in-text-block)
3. **app.js** — unique `aria-label` on email banner vs sticky (`landmark-unique`)
4. **horoscope.html** — move `#ap-affiliate-slot` inside `<main>`
5. **chart.html:375** — `aria-hidden` on `#result-name` until populated

### Conversion — highest revenue leverage

1. **chart-page.js** — add paid tile in `renderWhatsNext()`: Deep Reading + Poster £16
2. Poster export upsell: “Print-ready PDF — £6” after free download
3. Fix shop hero CLS (same as perf P0)

---

## P1 fixes (next sprint)

- Horoscope: remove hero JPG from `horoscope-critical.css`; slim script chain
- Ephemeris: drop `gradient-text` on h1; weather-out skeleton; defer heavy scripts
- Chart: hero JPG out of critical CSS; defer `celestial-seals.css`
- Wire `ap-page-bridge.js` on shop.html
- Set `AP_MON.affiliateTag` for Amazon picks

---

## Audit tooling (not site bugs)

1. `capture-pages.mjs` — exempt lite index from starfield check
2. `hero-entered` — target `index-full.html?full=1`, not lite shell
3. Freeze starfield before screenshots (`CosmosEngine.setPreloaderPause`)
4. Update baseline manifest to `01-cosmos-hold` + `04-earth-land`
5. `baseline:save` after script fixes (not before)

---

## Medium projects

- Contextual shop modules (compat → Two Skies, transits → Year Ahead)
- Shop IA simplification (featured + trending default; affiliate below fold)
- Postiz channel connection (0 integrations = no traffic funnel)
- LS post-purchase redirect to `chart.html?thanks=1`

---

*Generated by 4-agent parallel scan: perf, a11y, visual QA, UX/monetization.*