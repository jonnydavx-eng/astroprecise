# SHIP PLAN — one clean site, eclipse-ready

Written by Claude @ BOOK-T1H4NJ753R, 2026-08-08 morning, from a 15-agent audit
(7 read-only experts: pages, performance, code, SEO, funnel, accuracy, release;
8 highest-stakes recommendations then attacked by independent verifiers).
Every claim below carries file:line evidence in the audit record; the verifiers
confirmed the evidence on all 8 checked items and OVERTURNED the proposed fix
on 4 of them — the plan below is the post-verification version.

Legend: **[V-SAFE]** verifier confirmed safe pre-eclipse · **[V-RIDE]** verifier
rejected it standalone but it rides the one deploy that is happening anyway ·
**[UNVERIFIED]** small/low-risk, not adversarially checked · **[OWNER]** only
Jonny can do it.

Timeline anchor: eclipse Wednesday 12 Aug, greatest eclipse 17:46 UT (~19:05 BST).
Deploy pipeline: push to origin main = production in 5–12 min behind 12 hard test
gates. **The content-bank cron (refresh-content-bank.yml) commits to main and
regenerates the SW precache Monday 10 Aug 04:00 UTC — it will deploy by itself.**

---

## 1. OWNER-NOW — nothing ships around these

1. **Create the 6 Gumroad products** — the gate on all commerce. Site prices are
   the truth: eclipse-reading £2.99, eclipse-set £6, full-reading £12,
   deep-reading £12 (same product as full-reading — same code goes in both
   slots), plate £14, sky-pass £5. Kimi's runbook prices (£29/£21/£33) were
   wrong; corrected in ECLIPSE-RUNBOOK.md §0.
2. **Per-product Gumroad redirects** [V-SAFE]: eclipse-reading →
   astroprecise.app/eclipse.html; full-reading → astroprecise.app/deep-reading.html;
   set/plate/sky-pass → plain thank-you page, NOT eclipse.html. A single shared
   redirect strands a £12 buyer at "KEY NOT RECOGNISED" (eclipse.html:396 only
   verifies eclipse-reading).
3. **Decide Set/Plate/Sky-Pass** [V-SAFE as option b]: (b) launch with
   eclipse-reading + full-reading only, keep the other three dormant — zero
   code, risk-free, recommended; or (a) ~6 lines in eclipse.html to accept Set
   keys + Gumroad file attachments — needs a cast smoke test after.
4. **Legal**: real name + postal address into privacy.html:129/131 and
   terms.html:131/132 — blocks a lawful first sale. While in there, approve
   deleting the analytics paragraph (privacy.html:150-151 claims GA4+Umami run;
   measured: the site loads no analytics at all — the policy currently
   over-discloses data sharing that does not happen).
5. **Ratify the freeze**: everything ships by 11 Aug 20:00 UT; from 12 Aug
   00:00 UT origin pushes are emergency-only (rollback command pre-typed);
   nothing ships 16:00–19:00 UT on eclipse day unless the site is down.
   Precedent: the pipeline's own platform (GitHub) was down for three
   consecutive deploys on 06 Aug.
6. **Delete the audit probe** from the mailing list before the dawn send:
   subscriber `probe+shipaudit@example.invalid` (tag eclipse-dawn). The capture
   endpoint itself measured healthy — 200, dedupe works, 0.38 s.
7. **Decide SW registration reach** [OWNER, informed]: the service worker only
   registers on 7 pages (app.js:1987) — home and eclipse.html never install it.
   One line on eclipse.html gives eclipse-night visitors offline resilience at
   ~2.83 MB per new install (GitHub Pages soft cap 100 GB/month ≈ 35k installs).
   Default if undecided: leave as-is; the precache fix below still protects the
   chart/horoscope users who already have the SW.

## 2. THE ONE PRE-ECLIPSE DEPLOY — target Sun 09 / Mon 10 Aug

One deploy, one SW version bump, then soak. Every SW bump makes returning users
re-pull 2.83 MB, so nothing else touches sw.js this week.

Must (in this order, as one wave):
- **Fix in the GENERATOR, not sw.js** [verifier-corrected]: add eclipse.html,
  js/reading-templates.json, deep-reading.html to
  website/tools/generate-sw-precache.mjs (HTML_INCLUDE/REQUIRED); remove
  outreach.html + js/outreach-content.js (internal toolkit currently installed
  into every visitor's cache, 34 KB). Hand-editing sw.js gets silently reverted
  by Monday's cron — the panel's own first proposal failed verification on
  exactly this.
- **Run the generator once** (bumps V→ap-v814 and syncs AP_ASSET_V — never
  hand-edit either), commit sw.js + ap-asset-v.js together.
- **ESLint sourceType override** for the 8 ESM files + delete the duplicate
  `startSpaceFlight,` at orrery-webgl.js:9553 [V, reproduced]. Zero runtime
  change; it makes the exact files the flip will hand-edit lintable BEFORE
  permalinks get pasted. `npm run lint` must exit 0 after.
- **NOTIFY_TAGS** two lines (shop-commerce.js:1357): tag eclipse-reading and
  deep-reading notify signups — until deployed, any flip email goes to the FULL
  list, not segments. [UNVERIFIED, 2 lines]
- **Sitemap**: add observatory.html, refresh eclipse.html lastmod (IndexNow
  pings are built from sitemap.xml, so search engines have not been told the
  eclipse page changed since 17 Jul). [UNVERIFIED, data file]
- **Event JSON-LD** on eclipse.html: add the image property, align start/end
  with the page's own FAQ times (17:00 vs 18:00 contradiction). [UNVERIFIED,
  one-line JSON]
- **Brand sweep**: llms.txt + sample-reading.html say "AstroPrecise"; the brand
  is "Astro Precise". [UNVERIFIED, text]

Ride-along, drop without argument if anything feels heavy:
- sky-events.html nav line [V-RIDE — no-ops without a bump; the bump is
  happening for the eclipse fix anyway].
- Cart drawer dead button → email capture (checkoutNow empty-cart guard);
  shop notify form fetch-submit instead of the raw-JSON _blank tab.
  [UNVERIFIED, small]

Deliberately NOT in this deploy (verifier-rejected for pre-eclipse):
- **Timezone/DST fix** — real defect (a UK summer birth can be an hour out on
  the eclipse page; can flip the 5° sale gate), but the fix touches the cast
  path on both funnel pages with no NaN guard at eclipse.html:291. First
  post-eclipse item.
- **renderFeatured() refactor** — the dynamic branch is untested dead code.
  Instead: hand-edit the 5 static shop cards in the flip commit.

After the deploy: watch to green (5–12 min), then `node tools/sweep.mjs`
(400 paths, exit 0) and `node tools/byte-audit.mjs` at the deployed SHA. These
two are now the mandatory post-deploy gate for every ship.

Note: the current 6-commit unpushed queue (docs, marketing, tools — zero
website/ runtime files except a pre-existing .md) rides this same push.
Check `git log origin/main..main --name-only` first — autosnapshots ride along.

## 3. FLIP DAY — the moment the permalinks arrive (10–11 Aug, never eclipse night)

1. Paste codes into all 12 slots: gumroad-unlock.js:33-39 AND
   ap-gumroad-bridge.js:13-18 (full-reading and deep-reading take the SAME code).
2. `npm run lint` — the config fix above makes typos in these exact files
   visible. A bad paste kills the eclipse page: eclipse.html:255 imports
   gumroad-unlock.js in the same module as the cast flow.
3. Hand-edit the 5 static shop cards ("Coming soon" → live copy + price).
4. Gumroad side: license keys ON, per-product redirects (§1.2), DOB custom
   fields on set/plate/sky-pass if launching them.
5. Push; watch green; sweep + byte-audit.
6. **Real £2.99 test purchase** + key unlock on eclipse.html + **cast a
   reading** (the verifier's addition: a buy test alone misses a broken cast
   path).
7. Send the flip email to the FULL list.
No SW bump needed — both commerce files are network-first (sw.js:497), flip
propagates in ~10 min.

## 4. ECLIPSE DAY — Wed 12 Aug

- Frozen from 00:00 UT. Emergency HTML fixes are safe WITHOUT touching sw.js
  (navigations are network-first); never touch sw.js or the generator.
- Rollback, pre-typed: `git revert <bad-sha> && git push origin main` — green
  in 5–12 min, byte-audit after. Never force-push, never hand-edit V. Within
  ~24 h "Re-run all jobs" on the last green run is the fallback.
- Monitoring (the site has zero live telemetry — AP_MON is monetisation
  config, not monitoring): `node tools/sweep.mjs` at T-2h, T-30m, then hourly;
  countdown eyeball on phone + laptop; `AP_getClientErrors()` in DevTools on
  own devices.
- 17:46 UT: eclipse. The site does the rest.

## 5. POST-ECLIPSE — the consolidation backlog, in order

1. **13 Aug: repurpose eclipse.html IN PLACE** — retrospective + "where it fell
   in your chart" + next-eclipse (2 Aug 2027). No 301s exist on GitHub Pages;
   the page will be the domain's most-linked URL — never redirect it away.
2. **Price flip £2.99→£4** as ONE prepared commit (~17 hardcoded surfaces:
   shop og/JSON-LD/copy + eclipse.html + 3 JS files) executed together with the
   Gumroad price change — never flip Gumroad alone. If checkout opened late,
   honour £2.99 for the notify list.
3. **Timezone/DST fix** (§2's deferral): auto option from Intl timeZone,
   resolve via the ianaOffsetHours() helper already live at eclipse.html:406,
   NaN-guard the parseFloat sites, copy helper to index.html. Test with a
   winter UK birth + a US birth.
4. **Page retirements**: index-classic.html → redirect stub (delete
   lite-shell-boot.js with it); observatory.html → stub to index.html;
   index-full.html → stub after cleaning 4 inbound refs; then the precache diet
   (~217 KB exclusive assets + dead ap-eclipse-page.js, ap-home-bootstrap.js,
   ap-award-orrery.js chain) in ONE SW bump.
5. **Engine consolidation, strict order**: drop dead bootstrap chain →
   precache orrery3d.js (60 KB) as the offline engine → only then retire
   orrery.js and repoint adapter rung 4. orrery3d/lite-orrery/orrery-loader are
   load-bearing and stay.
6. **Precache diet**: zodiac webps + Inter weights out of install-time precache
   (2.83 → ~2.0 MB), runtime cache-first already covers them.
7. **SEO harvest**: 12 build-time monthly-horoscope pages from the content bank
   (2,412 written readings, zero indexable URLs today); numerology cross-links
   or sitemap removal.
8. **chart.html privacy**: share-URL writes birth data into the query string
   (history + server logs on reload); move to location.hash, keep ?d= reading
   compatibility.
9. Lint tidy of the 7 benign errors; delete orphaned ap-eclipse-page.js (its
   quiet-chart logic contradicts the shipped honesty gate).

## 6. Verification record

15 agents, 48 findings, 8 adversarially verified: evidence held 8/8; fix
overturned or re-scoped 4/8 (sw.js hand-edit → generator; nav line → ride-only;
tz fix → post-eclipse; renderFeatured refactor → hand-edit cards). Items marked
[UNVERIFIED] were judged too small to burn a verifier on — treat them as
droppable ballast, not commitments. Full structured findings: the workflow
record in the estate session logs, 2026-08-08.
