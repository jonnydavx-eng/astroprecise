# AstroPrecise — handoff, 2026-08-08 23:26

Claude @ BOOK-T1H4NJ753R. Eclipse is **Wednesday 12 August**, four days out.
Everything below marked MEASURED was checked tonight. Nothing here is inferred unless it says so.

---

## 1. WHERE IT STANDS IN ONE PARAGRAPH

The live site is unchanged and healthy. A redesigned front screen is built, independently
reviewed and ready, sitting on a branch — it has not shipped, and shipping it is the owner's
call. The launch-blocking work is done. What remains is mostly owner actions: Gumroad products,
a legal name, and social accounts that do not exist yet.

**There IS a shippable product for 12 August** — the £2.99 eclipse reading, not the £12 one.

---

## 2. STATE, MEASURED 23:26

- **Live:** https://astroprecise.app serving `ap-v814`. Last deploy 12:17 today; all 842 paths
  returned 200 and byte-audit found 0 mismatches at the deployed commit.
- **Branches:** `main` 7b2d3c9 · `redesign/editorial-front-screen` d5306f5 · `feat/orrery-elevate` 15f10f3.
  **Both feature branches are now on origin** — they were single-copy on this laptop until tonight.
- **Working tree:** 3 files dirty on the redesign branch.
- **Higgsfield credits: 130.5** left of 912.5. Spend was heavier than intended — see §6.

---

## 3. THE DECISION WAITING FOR THE OWNER

Two candidates exist. Four independent reviewers agreed on both calls:

**SHIP `redesign/editorial-front-screen`.** It is strictly better than what is live. The primary
call-to-action moves from 1.12 screens below the fold to above it on a phone. Lint 0 errors,
tests pass, Playwright clean at four widths.

**DO NOT SHIP `feat/orrery-elevate`** before the eclipse. It adds 171 lines to the WebGL orrery
(nebula IBL, MSAA, PBR 2K textures) from a parallel session, after review closed. It has a
reachable GPU-memory leak, has never run on a phone, and `npm test` does not exercise WebGL at
all. It earns nothing on Wednesday and sits on the render hot path.

Full reasoning: `REVIEW-2026-08-08.md`.

---

## 4. WHAT CHANGED TODAY

**Shipped to production at 12:17** (safe batch): eclipse.html and deep-reading.html added to the
offline cache via the generator — they were the only revenue pages not precached while their own
JS was; the internal outreach toolkit removed from every visitor's cache (34 KB); ESLint green
for the first time (0 errors) so the files hand-edited at checkout-flip finally have a tripwire;
sitemap, Event JSON-LD image, brand sweep.

**Built on the branch, not shipped:** the Editorial front screen. The promise is now the largest
text (68px) instead of a 26px headline beside a 100px decorative numeral. The
"WAKING THE ENGINE…" placeholder is gone from both engine paths. The form carries birth place and
can actually produce a chart; `chart-page.js` reads date, time and city.

**Four honesty defects fixed:** privacy.html claimed Umami + GA4 that the site does not load;
shop.html said the Sun goes dark over Britain; eclipse.html had twin identical CTAs and no nav;
the email form showed success unconditionally.

**Two serious bugs caught by review, both fixed:**
1. The redesign itself **introduced a privacy leak** — the new GET form put birth date, time and
   town in the URL query string, into GitHub Pages and Cloudflare logs, on a page promising
   nothing leaves the device. Now sessionStorage with a fragment fallback; verified across 454
   requests with zero birth data in any request line, header or body. The guarantee lives in the
   markup — no form control has a `name` — so it holds with JS off.
2. `ap-sky-news.js` folded Sun–Moon elongation into 0–180, destroying the waxing/waning sign, so
   the front page could **never** say "waning" and contradicted its own correct answer further
   down. Fixed and locked with a regression test verified against 126,721 samples.

---

## 5. WHAT I GOT WRONG TODAY

Stated plainly, because the pattern matters more than the individual errors.

- **I raised background-task chips for work I then did myself in the same session.** Two started
  and became a second writer on my own files; one had already edited the canonical kit before I
  archived it. Rule: never raise a chip for work you are about to do.
- **The honesty pass created a fresh false claim.** The new colophon says the birth minute is
  "never put in a web address" and invites the reader to check — while pre-existing code at
  `index.html:1340` writes `#b=<dob>|<time>|<tz>` on every cast. My fix made the page *more*
  specific and *more* wrong. Still outstanding: see §7.
- **I claimed the homepage engine was broken.** It was not — the canvas was rendering; a 10px
  status line had failed to clear. I corrected it within the hour but I led with the alarm.
- **I called the promo video's crescent beautiful without noticing it is a Moon, not an eclipse.**
  Thin warm rim, dark night side, deep-blue post-sunset sky. A 91% partial Sun is a searing sliver
  in daylight. On a brand built on astronomical accuracy that is the worst possible error, and I
  looked straight at those frames and missed it.
- **I told the owner I could not watch video.** ffmpeg was on the machine the whole time. Extract
  frames, tile them, read the image. Hours of "I can size it but not watch it" were unnecessary.
- **I under-specified an agent brief and it cost real money.** One workflow generated three videos
  and nine images against a budget of one video.

---

## 6. MARKETING AND CREATIVE

- 25 social posts across TikTok / Instagram / X in `marketing\social-2026-08-12\`, plus 8
  platform images. A director caught and we fixed: a fabricated "that has cost us sales" claim
  (nothing has ever been sold), a filming guide certifying frames that contain prices, and a
  false "Sun sets still eclipsed" claim.
- **Objective creative data — Virality Predictor, job d7db83f6:** overall 56, hook 42.
  Visual attention peaks at t=2 then falls 40% by t=11 while mind-wandering rises. The apparent
  "peak at second 12" is a file-boundary artifact, not a climax.
- **Audience research overturned the visual direction.** Brass sextants and armillary spheres
  signal institutional authority to a post-institutional audience. Ban `sextile`, `orb`,
  `midheaven`, `ephemeris`, `arcminute` from anything on screen. Their vocabulary is big three,
  placements, moon, rising, birth chart. Point the anti-woo at the industry, never the viewer —
  the most engaged segment is queer users and women under 50, sincere believers.
- **"Slop" was Merriam-Webster's 2025 word of the year** and consumer distrust of brand AI output
  has roughly doubled. Shipping obviously-AI film for an honesty product is a live risk.
- **Best current asset:** `design-lab\creative-reset\lookdown-v2-hand-10s.mp4` — overhead decking
  covered in crescent-shaped sun dapples, a phone among them, a hand lifts it and the screen
  wakes. Real motion, product appears, needs no astrology vocabulary. Awaiting owner verdict.

---

## 7. OUTSTANDING — OWNER

1. **Legal name and postal address** into `privacy.html:129,131` and `terms.html:131,132`.
   `[FULL LEGAL NAME]` renders in orange on the live site while five forms collect email.
   Reviewer calls it a live UK GDPR Art.13 breach. 15 minutes, cheapest fix on the page.
2. **Create the six Gumroad products.** Site prices are the truth: eclipse-reading £2.99,
   eclipse-set £6, full-reading £12, deep-reading £12, plate £14, sky-pass £5. Kimi's runbook
   prices (£29/£21/£33) were wrong and are corrected in `ECLIPSE-RUNBOOK.md §0`.
   **Twelve slots across TWO files** — `js/gumroad-unlock.js:33-39` AND
   `js/ap-gumroad-bridge.js:13-18`. No test asserts they agree.
3. **Per-product Gumroad redirects.** One shared redirect strands a £12 buyer on
   "KEY NOT RECOGNISED".
4. **Choose the ship candidate** (§3).
5. **Social accounts do not exist.** X, TikTok, Instagram all unticked in
   `marketing\SOCIAL-ACCOUNTS-SETUP.md`. Three finished plans, nowhere to post.
6. **Rotate the exposed JWT** at `Documents\kimi\workspace\ap-mirror\upload.jwt` (969 bytes, plaintext).
7. **Mirror force-push decision** — `git push --force mirror main`. The astroprecise mirror push
   has failed every 15 min for ~93 consecutive cycles (non-fast-forward after a rebase). Both old
   tips are preserved as `rescue/*` branches, so it is safe. Until done, the vault cannot get current.
8. **Delete list subscriber** `probe+shipaudit@example.invalid` before the dawn send.

## 8. OUTSTANDING — AGENT

- Fix or reword the `#b=` URL write at `index.html:1340` (§5). Do not attempt to remove the write
  this week; reword the colophon to what is true: "never sent to our server".
- Point the £12 proof link at `sample-reading.html`, which is a complete ~5,000-word sample that
  already exists. The current link shows seven locked stubs under "PUBLISHED IN FULL".
- Eclipse times are wrong sitewide: **London max 19:12 at 90%, Edinburgh 19:05, Truro 19:16 at
  ~95%**; first contact 18:08–18:17; obscuration 88–96%; "deepest since 1999" holds for southern
  Britain only. Greatest eclipse is 45 km off western Iceland, and 17:46 is TD not UT.
  ⚠️ **That last clause was wrong — corrected 2026-08-09.** 17:46 is **UT**; TD is 17:47:06
  (NASA/GSFC Besselian elements), and ΔT for mid-2026 is 69.1 s (USNO/IERS), giving
  17:45:57 UT = **18:46 BST**. Acting on this line put a "17:46 TD" mislabel onto the live
  page and into the marketing tree. Canonical set: `marketing/social-2026-08-12/ECLIPSE-FACTS.md`.
- Say on the homepage that checkout is closed — it currently shows a five-row price table with no
  "notify" anywhere.
- Fix the quiet-gate self-contradiction (`reading-templates.json:104` + `eclipse-reading.js:212`):
  it prints "more than 5° from every placement" then names a 0°25′ contact.
- Drop `mode:'no-cors'` on the subscribe form — both reviewers measured the Worker returning CORS
  headers today, so the original premise was wrong.

## 9. RISKS FOR ECLIPSE DAY

- **The deploy pipeline's own platform failed three times on 6 August** during a GitHub outage,
  needing a manual retrigger. Ship by 11 Aug 20:00 UT, then freeze.
- **Rollback, pre-typed:** `git revert <bad-sha> && git push origin main`, green in 5–12 min,
  then byte-audit. Never force-push, never hand-edit sw.js `V`.
- **No live telemetry exists.** `AP_MON` is monetisation config, not monitoring. Watch with
  `node tools/sweep.mjs` at T-2h, T-30m, then hourly.
- **Any SW version bump re-downloads 2.83 MB** for every returning visitor. One bump, then freeze.
- Emergency HTML fixes on the day are safe *without* touching `sw.js` — navigations are
  network-first.

---

Sign **Claude @ BOOK-T1H4NJ753R**
